//! ⁡⁢⁣⁢ЦИКЛ «FOR»⁡
{
	// ⁡⁣⁣⁢Более сложный, но при этом самый распространённый цикл — цикл for. Обыкновенный цикл for имеет следующий синтаксис:⁡
	for (начало; условие; шаг) {
		// ⁡⁣⁢⁡⁢⁢⁢... тело цикла ...⁡
	}
	// ⁡⁣⁢⁡⁢⁢⁢Давайте разберёмся, что означает каждая часть, на примере. Цикл ниже выполняет alert(i) для i от 0 до (но не включая) 3:⁡
	for (let i = 0; i < 3; i++) {
		// ⁡⁣⁢⁡⁢⁢⁢выведет 0, затем 1, затем 2⁡
		alert(i);
	}
}
//! «FOR OF»⁡
{
	// ⁡⁣⁣⁡⁣⁣⁡⁣⁣⁢Оператор for...of выполняет цикл обхода итерируемых объектов (включая Array, Map, Set, объект аргументов и подобных), вызывая на каждом шаге итерации операторы для каждого значения из различных свойств объекта. Работает с value.⁡
	// ⁡⁢⁢⁢Синтаксис:⁡
	for (переменная of итерабильный_объект) {
		заявление;
	}
	// ⁡⁢⁢⁢Например, чтобы пройтись по массиву и вывести его элементы, можно использовать следующий код:⁡
	let array = [10, 20, 30];
	// ⁡⁢⁢⁢В этом примере переменная value будет принимать значения 10, 20 и 30 на каждом шаге цикла.⁡
	for (let el of array) {
		el += 1; // ⁡⁢⁢⁢Увеличиваем значение на 1⁡
		console.log(el);
	} // ⁡⁢⁢⁢Вывод: 11, 21, 31⁡
}
//! «FOR AWAIT OF»⁡
{
	// ⁡⁣⁣⁢Выражение for await...of создаёт цикл, проходящий через асинхронные итерируемые объекты, а также синхронные итерируемые сущности, включающие: встроенные String, Array, Array-подобные объекты (например., arguments или NodeList), TypedArray, Map, Set, а также определяемые пользователем асинхронные/синхронные сущности. Он вызывает пользовательский итерационный хук с инструкциями, которые должны быть выполнены для значения каждого отдельного свойства объекта.⁡
	for await (переменная of итерабильный) {
		заявление;
	}
}
//! «FOR IN»⁡
{
	// ⁡⁣⁣⁡⁣⁣⁢Цикл for...in проходит только по перечисляемым свойствам. Объекты, созданные встроенными конструкторами, такими как Array и Object имеют неперечисляемые свойства от Object.prototype и String.prototype, например, от String-это indexOf(), а от Object - метод toString(). Цикл пройдёт по всем перечисляемым свойствам объекта, а также тем, что он унаследует от конструктора прототипа (свойства объекта в цепи прототипа). Работает и с key.⁡
	// ⁡⁢⁢⁡⁣⁣⁢Cинтаксис:⁡⁡
	for (переменная in объект) {
		заявление;
	}
	// ⁡⁢⁢⁢Например, чтобы пройтись по объекту и вывести его свойства, можно использовать следующий код:⁡
	let object = { a: 1, b: 2, c: 3 };
	for (let el in object) {
		console.log(el + " = " + object[el]);
		// ⁡⁢⁢⁢el ключи объекта⁡
		// ⁡⁢⁢⁢object[el] значение объекта⁡
	}
	// a = 1
	// b = 2
	// c = 3
}
//! ЦИКЛ «WHILE»
{
	// ⁡⁣⁣⁢Цикл while имеет следующий синтаксис:⁡
	while (condition) {
		// код
		// также называемый "телом цикла"
	}
	// Код из тела цикла выполняется, пока условие condition истинно.
	// Например, цикл ниже выводит i, пока i < 3:
	let ind = 0;
	while (ind < 3) {
		// выводит 0, затем 1, затем 2
		alert(ind);
		i++;
	}
}
//! ⁡⁢⁣⁢ЦИКЛ «DO…WHILE»⁡
{
	// ⁡⁣⁣⁢Проверку условия можно разместить под телом цикла, используя специальный синтаксис do..while:⁡
	do {
		// тело цикла
	} while (condition);
	// Цикл сначала выполнит тело, а затем проверит условие condition, и пока его значение равно true, он будет выполняться снова и снова.
	// Например:
	let i = 0;
	do {
		alert(i);
		i++;
	} while (i < 3);
	// Такая форма синтаксиса оправдана, если вы хотите, чтобы тело цикла выполнилось хотя бы один раз, даже если условие окажется ложным. На практике чаще используется форма с предусловием: while(…) {…}.
}
// ⁡⁢⁣⁢МЕТОДЫ МАССИВОВ ДЛЯ ИТЕРАЦИЙ⁡
{
	// 🔳 ⁡⁢⁣⁣forEach()⁡
	{
		// ☑️ ⁡⁣⁢⁢не возвращает новый массив;⁡
		// ☑️ ⁡⁣⁢⁢нельзя прервать с break;⁡
		// ☑️ ⁡⁣⁢⁢пропускает пропущенные элементы в разреженных массивах.⁡
		const numbers = [1, 2, 3];
		numbers.forEach((num, index) => {
			console.log(`Индекс ${index}: ${num}`);
		});
		// Индекс 0: 1
		// Индекс 1: 2
		// Индекс 2: 3
	}
	// 🔳 ⁡⁢⁣⁣map()⁡
	{
		// ☑️ ⁡⁣⁢⁢Создаёт новый массив с результатами вызова функции для каждого элемента.⁡
		const doubled = [1, 2, 3].map((num) => num * 2);
		console.log(doubled); // [2, 4, 6]
	}
	// 🔳 ⁡⁢⁣⁡⁢⁣⁣filter()⁡
	{
		// ☑️ ⁡⁣⁢⁢Создаёт новый массив со всеми элементами, прошедшими проверку в функции.⁡
		const even = [1, 2, 3].filter((num) => num % 2 === 0);
		console.log(even); // [2]
	}
	// 🔳 ⁡⁢⁣⁣reduce()⁡
	{
		// ☑️ ⁡⁣⁢⁢Применяет функцию к аккумулятору и каждому элементу массива, сводя его к одному значению.⁡
		const sum = [1, 2, 3].reduce((acc, num) => acc + num, 0);
		console.log(sum); // 6
	}
	// 🔳 ⁡⁢⁣⁣some()⁡
	{
		// ☑️ ⁡⁣⁢⁢Проверяет, удовлетворяет ли хотя бы один элемент условию. Возвращает true/false.⁡
		const hasEven = [1, 2, 3].some((num) => num % 2 === 0);
		console.log(hasEven); // true
	}
	// 🔳 ⁡⁢⁣⁣every()⁡
	{
		// ☑️ ⁡⁣⁢⁢Проверяет, удовлетворяют ли все элементы условию. Возвращает true/false.⁡
		const allPositive = [1, 2, 3].every((num) => num > 0);
		console.log(allPositive); // true
	}
	// 🔳 ⁡⁢⁣⁣find()⁡
	{
		// ☑️ ⁡⁣⁢⁢Возвращает первый элемент, удовлетворяющий условию. Если не найдено — undefined.⁡
		const firstEven = [1, 2, 3].find((num) => num % 2 === 0);
		console.log(firstEven); // 2
	}
	// 🔳 ⁡⁢⁣⁣findIndex()⁡
	{
		// ☑️ ⁡⁣⁢⁢Возвращает индекс первого элемента, удовлетворяющего условию. Если не найдено — -1.⁡
		const indexOfEven = [1, 2, 3].findIndex((num) => num % 2 === 0);
		console.log(indexOfEven); // 1
	}
	// 🔳 ⁡⁢⁣⁣keys(), values(), entries()⁡
	{
		// ☑️ ⁡⁣⁢⁢Возвращают итераторы для ключей, значений и пар [ключ, значение].⁡
		// Ключи (индексы)
		for (const key of [1, 2, 3].keys()) {
			console.log(key); // 0, 1, 2
		}

		// Значения
		for (const value of [1, 2, 3].values()) {
			console.log(value); // 1, 2, 3
		}

		// Пары [ключ, значение]
		for (const [index, value] of [1, 2, 3].entries()) {
			console.log(`${index}: ${value}`); // 0: 1, 1: 2, 2: 3
		}
	}
}
