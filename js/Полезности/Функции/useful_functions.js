
//* Вернуть при ширине
function activeWidth() {
	if (window.screen.width > 1500.98) {
		return 4; // 👈 Большие экраны
	}
	if (window.screen.width < 1500.98 && window.screen.width > 1280.98) {
		return 4; // 👈 Ноутбуки
	}
	if (window.screen.width < 1280.98 && window.screen.width > 1024.98) {
		return 3; // 👈 Планшеты
	}
	if (window.screen.width < 1024.98 && window.screen.width > 768.98) {
		return 4; // 👈 Мобильные устройства
	}
}
// 🔳 ⁡⁢⁣⁣ЗАДЕРЖКА ВЫПОЛНЕНИЯ⁡
{
	// ⭐ ⁡⁣⁣⁢Дебаунс (debouncing) — это техника оптимизации, которая задерживает выполнение функции до тех пор, пока не пройдет определенное время без новых вызовов. Она используется для обработки частых событий, чтобы снизить нагрузку и избежать избыточных вычислений.⁡
	// 🔹 ⁡⁣⁢⁢Для чего нужен дебаунс? Как работает дебаунс?⁡
	{
		// ✅ ⁡⁢⁢⁢Поиск с автодополнением: Ожидание завершения ввода пользователя перед отправкой⁡
		// ✅ ⁡⁢⁢⁢Обработка изменений размера окна: Обновление интерфейса только после окончания ресайза.⁡
		// ✅ ⁡⁢⁢⁢Кнопки отправки формы: Предотвращение повторных кликов.⁡
		// ✅ ⁡⁢⁢⁢Скролл-события: Выполнение действий только после остановки скролла.⁡
		// ✅ ⁡⁢⁢⁢При первом вызове функции запускается таймер.⁡
		// ✅ ⁡⁢⁢⁢При каждом новом вызове таймер сбрасывается.⁡
		// ✅ ⁡⁢⁢⁢Функция выполняется только после того, как вызовы прекратились на заданное время.⁡
		{
			function debounce(func, delay) {
				let timeoutId;
				return function (...args) {
					// Очищаем предыдущий таймер
					clearTimeout(timeoutId);
					// Устанавливаем новый таймер
					timeoutId = setTimeout(() => {
						func.apply(this, args);
					}, delay);
				};
			}
			// Пример использования
			// <input type="text" id="search-input" placeholder="Поиск...">
			const searchInput = document.getElementById("search-input");
			// Функция для выполнения поиска
			function performSearch(query) {
				console.log("Отправка запроса:", query);
			}
			// Дебаунс-версия функции поиска
			const debouncedSearch = debounce(performSearch, 500);
			// Обработчик ввода
			searchInput.addEventListener("input", (e) => {
				debouncedSearch(e.target.value);
			});
		}
	}
	// 🔹 ⁡⁣⁢⁢Разница между дебаунсом и троттлингом⁡
	{
		// ⁡⁢⁣⁣КРИТЕРИЙ	     ДЕБАУНС	                        ТРОТТЛИНГ⁡
		// ⁡⁣⁣⁢Принцип	     Выполняет функцию после паузы	    Выполняет функцию не чаще X мс⁡
		// ⁡⁣⁣⁢Пример	     Поиск при остановке ввода	        Обработка скролла каждые 200 мс⁡
	}
	// 🔹 ⁡⁣⁢⁢Где еще применяется?⁡
	{
		// ✅ ⁡⁢⁢⁢Игровые механики: Обработка быстрых нажатий клавиш.⁡
		// ✅ ⁡⁢⁢⁢Canvas-анимации: Обновление только после завершения взаимодействия.⁡
		// ✅ ⁡⁢⁢⁢API-запросы: Защита от DDoS-атак со стороны клиента.⁡
	}
	// ⭐ ⁡⁣⁣⁢Дебаунс — мощный инструмент для оптимизации производительности. Реализация занимает всего 5-10 строк кода, но значительно улучшает UX и снижает нагрузку на систему.⁡
}