//! REDIS — ШПАРГАЛКА ДЛЯ СОБЕСЕДОВАНИЯ
// 🔳 ⁡⁢⁣⁣ОСНОВНОЕ ОПРЕДЕЛЕНИЕ⁡
{
	// ⭐ ⁡⁢⁣⁣Redis (Remote Dictionary Server)⁡ ⁡⁣⁣⁢— это высокопроизводительная база данных типа «ключ-значение» с открытым исходным кодом, работающая в оперативной памяти (in-memory). Её часто называют «структурированным хранилищем данных в памяти», поскольку она поддерживает сложные структуры данных, а не только простые строки⁡.
	// ⭐ ⁡⁢⁣⁣Ключевая концепция⁡: ⁡⁣⁣⁢Все данные хранятся в оперативной памяти (RAM), что обеспечивает чрезвычайно высокую скорость операций чтения и записи. Для обеспечения сохранности данных на диск используется механизм персистентности (persistence)⁡.
}
// 🔳 ⁡⁢⁣⁣НЕБОЛЬШАЯ ИСТОРИЯ⁡
{
	// ⭐ ⁡⁣⁣⁢Разработчик: Сальваторе Санфилиппо (antirez)⁡.
	// ⭐ ⁡⁣⁣⁢Год создания: 2009⁡.
	// ⭐ ⁡⁣⁣⁢Изначальная задача: Улучшение масштабирумости стартапа по работе с реальным временем⁡.
	// ⭐ ⁡⁣⁣⁡⁣⁣⁢Название: Расшифровывается как Remote Dictionary Server(Удаленный сервер словарей)⁡.
	// ⭐ ⁡⁣⁣⁢Текущий статус: Один из самых популярных баз данных в мире, «швейцарский нож» для разработчиков⁡.
}
// 🔳 ⁡⁢⁣⁣ОСНОВНЫЕ КОНЦЕПЦИИ И АРХИТЕКТУРА⁡
{
	// ⭐ ⁡⁢⁣⁣In-Memory Data Store⁡: ⁡⁣⁣⁢Все операции происходят в RAM, что является главным источником скорости (микросекунды на операцию)⁡.
	// ⭐ ⁡⁢⁣⁣Структуры данных (Data Structures)⁡: ⁡⁣⁣⁢Redis не просто key-string, а key-structure data. Это его главное преимущество⁡.
	{
		// ☑️ ⁡⁣⁢⁢String: Текст, числа, бинарные данные (до 512 МБ).⁡
		// ☑️ ⁡⁣⁢⁢List: Списки на основе связных списков.⁡
		// ☑️ ⁡⁣⁢⁢Set: Неупорядоченная коллекция уникальных элементов.⁡
		// ☑️ ⁡⁣⁢⁢Sorted Set (ZSet): Set, где у каждого элемента есть числовой балл (score) для сортировки.⁡
		// ☑️ ⁡⁣⁢⁢Hash: Карта для хранения пар field-value, аналог объекта.⁡
		// ☑️ ⁡⁣⁢⁢Stream: Лог событий для реализации потоков данных (похоже на Kafka).⁡
		// ☑️ ⁡⁣⁢⁢Bitmaps, HyperLogLogs, Geospatial indexes⁡.
		{
			// ✅ ⁡⁢⁢⁢Bitmaps — это особый тип строк, который позволяет выполнять операции на уровне битов. Такие структуры эффективны для хранения двоичных данных и часто используются, например, для отслеживания активности пользователей или управления флагами⁡.
			// ✅ ⁡⁢⁢⁢HyperLogLogs — это вероятностная структура данных, которая используется для подсчёта уникальных элементов. Такие структуры эффективны в использовании памяти и могут обрабатывать миллиарды уникальных элементов всего несколькими килобайтами памяти. HyperLogLogs подходят для задач аналитики, например, для подсчёта уникальных посетителей сайта⁡.
			// ✅ ⁡⁢⁢⁢Geospatial indexes — это структуры для хранения и запроса географических данных. Можно добавлять местоположения с координатами широты и долготы, а затем выполнять такие запросы, как поиск по радиусу или нахождение расстояния между двумя точками. Это полезно для сервисов, основанных на местоположении⁡.
		}
	}
	// ⭐ ⁡⁢⁣⁣Персистентность (Persistence)⁡: ⁡⁣⁣⁢Механизмы сохранения данных на диск⁡.
	{
		// ☑️ ⁡⁣⁢⁢RDB (Snapshotting): Периодическое создание снимков (snapshots) всей базы. Мало места, быстрое восстановление, но можно потерять данные между снимками.⁡
		// ☑️ ⁡⁣⁢⁢AOF (Append Only File): Логирует каждую команду на запись. Выше надежность, но больше размер файла и медленнее восстановление.⁡
		// ☑️ ⁡⁣⁢⁢На практике: Часто используют обе опции вместе.⁡
	}
	// ⭐ ⁡⁢⁣⁣Репликация (Replication)⁡: ⁡⁣⁣⁢Master-Replica архитектура. Данные с мастера асинхронно реплицируются на одну или несколько реплик. Реплики используются для чтения и повышения отказоустойчивости⁡.
	// ⭐ ⁡⁢⁣⁣Кластеризация (Clustering)⁡: ⁡⁣⁣⁢Автоматическое шардирование данных между несколькими узлами (нодами) для горизонтального масштабирования и высокой доступности⁡.
}
// 🔳 ⁡⁢⁣⁣ДЛЯ ЧЕГО ИСПОЛЬЗУЕТСЯ? (USE CASES)⁡
{
	// ⭐ ⁡⁢⁣⁣Кеширование (Cache)⁡: ⁡⁣⁣⁢Самый популярный кейс. Хранение результатов тяжелых запросов к БД, HTML-страниц, сессий. Уменьшает нагрузку на основную БД и ускоряет приложение⁡.
	// ⭐ ⁡⁢⁣⁣Счетчики и Аналитика⁡: ⁡⁣⁣⁢Благодаря атомарным командам INCR, INCRBY. Например, подсчет просмотров, лайков, онлайн-пользователей⁡.
	// ⭐ ⁡⁢⁣⁣Очереди сообщений (Message Queue)⁡: ⁡⁣⁣⁢С помощью структур List (команды LPUSH/BRPOP) или Streams. Например, для фоновых задач⁡.
	// ⭐ ⁡⁢⁣⁣Списки лидеров (Leaderboards)⁡: ⁡⁣⁣⁢Идеально для Sorted Sets. Сразу получаем отсортированный список по какому-либо критерию⁡.
	// ⭐ ⁡⁢⁣⁣Сервер сессий (Session Store)⁡: ⁡⁣⁣⁢Хранение сессий пользователей с TTL (временем жизни)⁡.
	// ⭐ ⁡⁢⁣⁣Геопространственные данные (Geospatial)⁡: ⁡⁣⁣⁢Поиск объектов в радиусе (команды GEOADD, GEORADIUS)⁡.
	// ⭐ ⁡⁢⁣⁣Блокировки (Distributed Locks)⁡: ⁡⁣⁣⁢Реализация механизма взаимоисключения (mutex) для распределенных систем с помощью команды SET с опциями NX PX⁡.
}
// 🔳 ⁡⁢⁣⁣КАК ИСПОЛЬЗОВАТЬ: ПРИМЕРЫ КОДА И КОМАНД⁡
{
	// ⭐ ⁡⁣⁣⁢Подключение⁡
	{
		`import { createClient } from 'redis';

        const client = createClient();
        client.on('error', (err) => console.log('Redis Client Error', err));
        await client.connect();`;
	}
	// ⭐ ⁡⁣⁡⁣⁣⁢SET / GET — запись и чтение значения⁡
	{
		await client.set("username", "alice");
		const username = await client.get("username");
		console.log(username); // 'alice';
	}
	// ⭐ ⁡⁣⁣⁢Установка с TTL (временем жизни)⁡
	{
		await client.set("temp_token", "abc123", { EX: 60 }); // живёт 60 секунд;
		// Или через EXPIRE:
		await client.set("session_id", "xyz789");
		await client.expire("session_id", 300); // 5 минут;
	}
	// ⭐ ⁡⁣⁣⁢Работа со списками (LPUSH / LRANGE)⁡
	{
		await client.lPush("tasks", "task1");
		await client.lPush("tasks", "task2");

		const tasks = await client.lRange("tasks", 0, -1);
		console.log(tasks); // ['task2', 'task1']
	}
	// ⭐ ⁡⁣⁣⁢Хэши (HSET / HGET / HGETALL)⁡
	{
		await client.hSet("user:1001", {
			name: "Bob",
			email: "bob@example.com",
		});

		const name = await client.hGet("user:1001", "name");
		const user = await client.hGetAll("user:1001");
		console.log(user); // { name: 'Bob', email: 'bob@example.com' }
	}
	// ⭐ ⁡⁣⁣⁢Множества (SADD / SMEMBERS)⁡
	{
		await client.sAdd("tags:post:1", ["javascript", "nodejs", "redis"]);
		const tags = await client.sMembers("tags:post:1");
		console.log(tags); // ['javascript', 'nodejs', 'redis']
	}
	// ⭐ ⁡⁣⁣⁢Упорядоченные множества (ZADD / ZRANGE)⁡
	{
		await client.zAdd("leaderboard", [
			{ score: 1500, value: "player1" },
			{ score: 1200, value: "player2" },
		]);

		const topPlayers = await client.zRange("leaderboard", 0, -1, { REV: true });
		console.log(topPlayers); // ['player1', 'player2']
	}
	// ⭐ ⁡⁣⁣⁢Публикация / Подписка (Pub/Sub)⁡
	{
		await client.publish("новости", "Привет подписчики!");
	}
    // ⭐ ⁡⁣⁣⁢Завершение работы⁡
    {
		await client.quit(); // корректное завершение
	}
}
// 🔳 ⁡⁢⁣⁣КОММЕНТАРИИ И ВАЖНЫЕ ДЕТАЛИ ДЛЯ СОБЕСЕДОВАНИЯ⁡
{
	// ⭐ ⁡⁢⁣⁣Скорость⁡: ⁡⁣⁣⁢Redis быстрый не только из-за памяти. Он также однопоточный (обрабатывает одну команду за раз), что исключает блокировки, и использует эффективную модель ввода-вывода (I/O Multiplexing)⁡.
	// ⭐ ⁡⁢⁣⁣Атомарность⁡: ⁡⁣⁣⁢Все команды Redis атомарны. Это значит, что при одновременном доступе нескольких клиентов команда выполнится целиком, без «перемешивания». Сложные транзакции можно собирать с помощью MULTI/EXEC⁡.
	// ⭐ ⁡⁢⁣⁣Когда НЕ использовать Redis?⁡
	{
		// ☑️ ⁡⁣⁢⁢Когда данные не помещаются в RAM (стоимость)⁡.
		// ☑️ ⁡⁣⁢⁢Для сложных запросов с джойнами (JOIN) — это реляционная БД⁡.
		// ☑️ ⁡⁣⁢⁢Когда надежность хранения критична, а настроенная персистентность AOF+RDB кажется недостаточной⁡.
	}
	// ⭐ ⁡⁢⁣⁣Процесс запуска⁡: ⁡⁣⁣⁢Очень простой. Скачиваем, собираем и запускаем redis-server. Клиентом подключается redis-cli⁡.
	// ⭐ ⁡⁢⁣⁣Безопасность⁡: ⁡⁣⁣⁢По умолчанию Redis не имеет пароля и слушает на всех интерфейсах. В продакшне обязательно нужно настраивать requirepass и брандмауэр⁡.
}
// 🔳 ⁡⁢⁣⁣ИТОГ ДЛЯ ФОРМУЛИРОВКИ НА СОБЕСЕДОВАНИИ⁡
{
	// ⭐ ⁡⁣⁣⁢«Redis — это высокопроизводительное in-memory хранилище структур данных, которое чаще всего используется как кеш, брокер сообщений или база для реального времени. Его сила — в поддержке сложных структур (хэши, списки, множества) и атомарных операций над ними, что позволяет легко реализовывать такие вещи, как очереди, рейтинги и блокировки. Данные могут сохраняться на диск с помощью RDB-снимков или AOF-лога. Это однопоточное приложение, что делает его невероятно быстрым и предсказуемым»⁡.
}