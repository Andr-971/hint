Составить тесты для сеньор разработчика HTML на 90 вопросов, пример: 
// ⚠️ ⁡⁣⁣⁢1. Какой HTML-тег используется для хранения неотображаемого шаблонного контента, который может быть клонирован и вставлен через JavaScript?⁡
{
	// 📌 <template> ✔️ ⁡⁢⁢⁢Тег <template> содержит контент, который не отображается сразу, но может быть клонирован и вставлен в DOM через JavaScript⁡
	// 📌 <script>
	// 📌 <slot>
	// 📌 <source>
	// 📌 <shadow>
}
// ⚠️ ⁡⁣⁣⁢2. Вы создаёте компонент Web Components и хотите, чтобы пользователь мог вставить свой контент внутрь определённого места внутри компонента.
Какой тег вы используете?⁡
{
	// 📌 <slot> ✔️ ⁡⁢⁢⁢это механизм Web Components для распределения пользовательского контента в определённые места компонента.⁡
	// 📌 <placeholder>
	// 📌 <template>
	// 📌 <yield>
	// 📌 <shadow>
}
// ⚠️ ⁡⁣⁣⁢3. Рассмотрите фрагмент кода:
⁡⁣⁢⁢<template>
	<h2>Заголовок</h2>
	<p>Тест</p>
</template>⁡
⁡⁣⁣⁢Какой эффект от использования этого кода при загрузке страницы?⁡
{
	// 📌 Элемент <h2> и <p> будут доступны скринридерами
	// 📌 Контент загрузится в скрытом iframe
	// 📌 Контент загрузится, но не будет доступен в DOM
	// 📌 Контент будет загружен, но не будет отрисован до вызова скрипта ✔️ ⁡⁢⁢⁢Содержимое <template> не отображается и не становится частью активного DOM-дерева, но доступно для клонирования⁡.
	// 📌 Контент отобразится как обычно
}
// ⚠️ ⁡⁣⁣⁢4. На странице отображается ход загрузки файла в процентах. Какой HTML-тег лучше всего использовать для отображения прогресса?⁡
{
	// 📌 <range>
	// 📌 <input type="range"/>
	// 📌 <output>
	// 📌 <progress> ✔️ ⁡⁢⁢⁢<progress> предназначен именно для отображения хода выполнения задачи (например, загрузки). <meter> — для измеряемых величин (например, температура, объём).⁡
	// 📌 <meter>
}
// ⚠️ ⁡⁣⁣⁢5. Какой из следующих фрагментов содержит ошибку в логической структуре формы?⁡
{
	// 📌 <fieldset>
				<legend>Настройки</legend>
				<label for="x">Включить</label>
				<input id="x" type="checkbox"/>
			</fieldset>

	// 📌 <form>
				<fieldset>
					<legend>Форма</legend>
				</fieldset>	
			</form>

	// 📌 <fieldset>
				<legend>Регистрация</legend>
				<label for="с">Параоль</label>
				<input id="с" type="password"/>
			</fieldset>

	// 📌 <fieldset>
				<label for="b">Имя</label>
				<input id="b" type="text"/>
				<legend>Данные</legend>
			</fieldset> ✔️ ⁡⁢⁢⁢<legend> должен быть первым дочерним элементом <fieldset>. В остальных случаях структура корректна⁡.

	// 📌 <fieldset>
				<legend>Выбор</legend>
				<label for="a">Имя</label>
				<input id="a" type="radio" name="opt"/>
			</fieldset>
}
// ⚠️ ⁡⁣⁣⁡⁣⁣⁢6. Что нарушенно в структуре этой формы?
⁡⁣⁢⁢<form>
	<input type="text" name="login">
	<input type="password"/>
	<button>Войти</button>
</form>⁡
{
	// 📌 Поле password не имеет фтрибута name - значение не будет отправлено ✔️ ⁡⁢⁢⁢Без атрибута name данные поля не попадут в данные формы при отправке⁡.
	// 📌 Все поля должны быть в нутри <fieldset>
	// 📌 Отсутствует метод формы (method)
	// 📌 Кнопка должна быть <input type="submit"/>
	// 📌 Нужно добавить action="/"
}
// ⚠️ ⁡⁣⁣⁢7. Какой атрибут следует добавить к интерактивному элементу, чтобы исключить его из порядка табуляции, но при этом оставить доступным для чтения скринридером?⁡
{
	// 📌 role="presention"
	// 📌 focusable="false"
	// 📌 tabindex="-1" ✔️ ⁡⁢⁢⁢Убирает элемент из стандартного порядка навигации по Tab, но он остаётся доступен программно и для скринридеров.⁡
	// 📌 tabindex="0"
	// 📌 aria-nidden="true"
}
// ⚠️ ⁡⁣⁣⁢8. Расмотрие следующий код:
⁡⁣⁢⁢<div style="display: none;">Подсказка</div>⁡
⁡⁣⁣⁢Как ведёт себя такой элемент с точки зрения доступности?⁡
{
	// 📌 Элемент будет доступен только для клавиатуры
	// 📌 Элемент не будет отображён визуально, но озвучится
	// 📌 Элемент останется видимым для скринридера
	// 📌 Элемент озвучится при наведении мыши
	// 📌 Элемент будет полностью скрыт ✔️ ⁡⁢⁢⁢display: none полностью удаляет элемент из визуального и доступного (assistive) потока.
⁡}
// ⚠️ ⁡⁣⁣⁢9. Как использование tabindex="10" на случайных элементах страницы влияет на пользователей, использующих клавиатуру?⁡
{
	// 📌 Может нарушить логический порядок фокусировки ✔️ ⁡⁢⁢⁢Явные положительные значения tabindex создают кастомный порядок, который часто не совпадает с визуальным/логическим и путает пользователей клавиатуры.⁡
	// 📌 Позволяет ускорить навигацию
	// 📌 Не влияет на доступность
	// 📌 Обеспечивает поддержку автозаполнения
	// 📌 Упрощает порядок перехода по форме
}
// ⚠️ ⁡⁣⁣⁢10. Вы описываете товар в карточке с заголовком, ценой и кнопкой. Какой вариант предпочтительнее с точки зрения оптимального DOM?⁡
{
	// 📌 <section>
		<div>
			<h3>Товары</h3>
			<p>Цена</p>
			<button>Купить</button>
		<div>
		</section>
	// 📌 <article>
				<h3>Товары</h3>
				<p>Цена</p>
				<button>Купить</button>
			</article> ✔️ ⁡⁢⁢⁢<article> семантически правильный тег для автономного контента (например, карточки товара), улучшает доступность и SEO.⁡
	// 📌 <div>
				<h3>Товары</h3>
				<div>
					<p>Цена</p>
					<div>
						<button>Купить</button>
					</div>
				</div>
			<div>
	// 📌 <div>
			<h3>Товары</h3>
			<div>
				<p>Цена</p>
				<div>
					<button>Купить</button>
				</div>
			<div>
			</div>
	// 📌 <div>
			<div>
				<h3>Товары</h3>
				<div>
					<p>Цена</p>
					<button>Купить</button>
				</div>
			<div>
			</div>
}
// ⚠️ ⁡⁣⁣⁢11. Рассмотрите следующий фрагмент HTML:
⁡⁣⁢⁢<div>
	<div>
		<div>
			<span>
				<b>Заголовок</b>
			</span>
		</div>
	</div>
</div>⁡
⁡⁣⁣⁢Какую проблему с точки зрения DOM и рендеринга он содержит?⁡
{
	// 📌 Следует заменить span на h1
	// 📌 Этот код вызовет ошибку браузера
	// 📌 Отсутствует семантическая структура
	// 📌 Избыточная вложенность div-ов увеличивает глубину DOM ✔️ ⁡⁢⁢⁢Это не ошибка, но антипаттерн: замедляет рендеринг, усложняет поддержку, и нарушает семантику (лучше использовать заголовок <hX> вместо <b>).⁡
	// 📌 Некорректное использование и внутри span
}
// ⚠️ ⁡⁣⁣⁢12. Как изменение структуры с 10 одинаковых вложенных блоков <div> на один блок со списком <ul><li> повлияет на рендеринг?⁡
{
	// 📌 Увеличит колличество DOM-узлов и замедлит рендеринг
	// 📌 Сократит глубину и улучшит производительность за счёт оптимизации браузерного рендера ✔️ ⁡⁢⁢⁢Но также важно: <ul><li> даёт семантическое значение списка, улучшает доступность и упрощает стилизацию.⁡
	// 📌 Повысит семантическую ясность и упростит стилизацию без влияния на производительность
	// 📌 Не повлияет, так как все элементы одинаковы по визуальному весу
	// 📌 Ухудшит доступность для скринридеров
}
// ⚠️ ⁡⁣⁣⁢13. Почему нельзя зарегистрировать элемент с именем <panel> как Web Component?⁡
{
	// 📌 Потому что имя не содержит дефиса ✔️ ⁡⁢⁢⁢Согласно спецификации, имя кастомного элемента обязательно должно содержать дефис (например, my-panel)⁡
	// 📌 Потому что требуется префикс х-
	// 📌 Потому что оно не объявленно в <head>
	// 📌 Потому что оно не начинается с custom-
	// 📌 Потому что оно содержит заглавные буквы
}
// ⚠️ ⁡⁣⁣⁢14. В JSX вы пишите следующий код:
⁡⁣⁢⁢<label for="email"></label>⁡
⁡⁣⁢⁢<input id="email"/>⁡
⁡⁣⁣⁢Как это повлияет на поведение и доступность?⁡
{
	// 📌 Всё будет работать корректно
	// 📌 Скринридеры проигнорируют label
	// 📌 Элемент <label> не будет связан с input ✔️ ⁡⁢⁢⁢правильно в JSX (htmlFor)⁡
	// 📌 Необходимо использовать aria-for
	// 📌 JSX автоматически преобразует for в htmlFor
}
// ⚠️ ⁡⁣⁣⁢15. В шаблоне Web Component используется именованный слот:
⁡⁣⁢⁢<slot name="header"></slot>⁡
⁡⁣⁣⁢Какая разметка правильно вставит контент в этот слот?⁡
{
	// 📌 <my-card slot="header">
		<h1>Заголовок</h1>
	</my-card>
	// 📌 <my-card>
			<h1>Заголовок</h1>
		</my-card>
	// 📌 <my-card>
			<header>Заголовок</header>
		</my-card>
	// 📌 <my-card>
			<h1 slot="header">Заголовок</h1>
		</my-card> ✔️ ⁡⁢⁢⁢В Web Components для распределения контента в именованные слоты используется атрибут slot на дочернем элементе, который должен быть вставлен в соответствующий <slot name="...">.
⁡}
// 📌 