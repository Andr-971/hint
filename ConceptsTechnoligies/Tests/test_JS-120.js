//! ТЕМА 1: ВСПОМОГАТЕЛЬНЫЕ ИНСТРУМЕНТЫ ДЛЯ JAVASCRIPT
// ⚠️ ⁡⁣⁣⁢1. Какая основная цель использования Babel в проекте?⁡
{
	// ✅ ⁡⁢⁢⁢Транспиляция современного JavaScript в обратно совместимую версию для поддержки старых сред выполнения. (Babel преобразует ES6+ в ES5, позволяя использовать новейшие возможности без потери совместимости.)⁡
	// Транспиляция современного JavaScript в версию, совместимую со старыми браузерами.
	// Минификация кода для уменьшения размера.
	// Линтинг кода для поиска ошибок.
}
// ⚠️ ⁡⁣⁣⁢2. Какой инструмент обычно используется для статической типизации в JavaScript?⁡
{
	// ✅ ⁡⁢⁢⁢TypeScript (TypeScript добавляет статическую типизацию и компилируется в JavaScript.)⁡
	// ESLint
	// Webpack
	// Babel
}
// ⚠️ ⁡⁣⁣⁢3. Что такое "дерево тряски" (tree shaking) в контексте сборщиков модулей?⁡
{
	// ✅ ⁡⁢⁢⁢Процесс удаления неиспользуемого кода (мёртвого кода) из финального бандла. (Tree shaking анализирует импорты и экспорты и исключает функции/модули, которые не вызываются.)⁡
	// Процесс удаления неиспользуемых стилей из CSS.
	// Удаление комментариев из кода.
	// Оптимизация изображений.
}
// ⚠️ ⁡⁣⁣⁢4. В каком файле конфигурации Webpack определяются точки входа и выхода?⁡
{
	// ✅ ⁡⁢⁢⁢webpack.config.js (Webpack использует JavaScript-файл конфигурации, обычно webpack.config.js, экспортирующий объект настроек.)⁡
	// package.json
	// webpack.config.json
	// .babelrc
}
// ⚠️ ⁡⁣⁣⁢5. Какую команду npm нужно выполнить, чтобы установить пакет и сохранить его как зависимость для разработки (devDependency)?⁡
{
	// ✅ ⁡⁢⁢⁢npm install package --save-dev (Флаг --save-dev добавляет пакет в devDependencies, которые нужны только для разработки.)⁡
	// npm install package --save
	// npm install package -g
	// npm install package --production
}
// ⚠️ ⁡⁣⁣⁢6. Для чего используется ESLint в проекте?⁡
{
	// ✅ ⁡⁢⁢⁢Для статического анализа кода и выявления проблем, несоответствий стилю и потенциальных ошибок. (ESLint проверяет код на основе правил и может автоматически исправлять некоторые проблемы.)⁡
	// Для автоматического форматирования кода.
	// Для сборки проекта.
	// Для запуска тестов.
}
// ⚠️ ⁡⁣⁣⁢7. Что такое Prettier?⁡
{
	// ✅ ⁡⁢⁢⁢Инструмент для автоматического форматирования кода с целью единообразного стиля. (Prettier переписывает код в соответствии с заданными правилами форматирования.)⁡
	// Линтер для поиска ошибок.
	// Сборщик модулей.
	// Тестовый фреймворк.
}
// ⚠️ ⁡⁣⁣⁢8. Какую задачу выполняет менеджер пакетов (npm или yarn)?⁡
{
	// ✅ ⁡⁢⁢⁢Управление зависимостями проекта: установку, обновление и удаление пакетов. (Менеджеры пакетов также запускают скрипты и управляют версиями.)⁡
	// Компиляцию TypeScript в JavaScript.
	// Отладку кода.
	// Сборку проекта.
}
// ⚠️ ⁡⁣⁣⁢9. Что такое "полифилл" (polyfill)?⁡
{
	// ✅ ⁡⁢⁢⁢Кусок кода, который реализует недостающую функциональность в старых браузерах. (Полифиллы добавляют поддержку современных методов, таких как Array.prototype.includes, в окружениях, где их нет.)⁡
	// Инструмент для сборки.
	// Шаблон проектирования.
	// Тип данных.
}
// ⚠️ ⁡⁣⁣⁢10. Для чего используется файл .babelrc?⁡
{
	// ✅ ⁡⁢⁢⁢Для конфигурации пресетов и плагинов Babel. (В .babelrc указывают, какие трансформации применять к коду.)⁡
	// Для настройки линтера.
	// Для указания точек входа Webpack.
	// Для определения зависимостей проекта.
}
// ⚠️ ⁡⁣⁣⁢11. Какая команда создаёт package.json по умолчанию?⁡
{
	// ✅ ⁡⁢⁢⁢npm init -y (Флаг -y или --yes генерирует package.json с значениями по умолчанию без интерактивных вопросов.)⁡
	// npm create
	// npm new
	// npm start
}
// ⚠️ ⁡⁣⁣⁢12. Что такое "модули" в контексте JavaScript и инструментов сборки?⁡
{
	// ✅ ⁡⁢⁢⁢Способ организации кода в отдельные файлы с изолированной областью видимости. (Модули позволяют импортировать и экспортировать функции/переменные между файлами.)⁡
	// Библиотека для работы с DOM.
	// Фреймворк для построения интерфейсов.
	// Транспилятор.
}
// ⚠️ ⁡⁣⁣⁢13. Какой лоадер в Webpack используется для обработки CSS-файлов?⁡
{
	// ✅ ⁡⁢⁢⁢css-loader (css-loader интерпретирует @import и url() в CSS, а style-loader вставляет стили в DOM.)⁡
	// babel-loader
	// file-loader
	// url-loader
}
// ⚠️ ⁡⁣⁣⁢14. Какая задача у плагина в Webpack?⁡
{
	// ✅ ⁡⁢⁢⁢Выполнение дополнительных действий на разных этапах сборки: оптимизация, генерация HTML, очистка папок и т.д. (Плагины имеют доступ ко всему процессу сборки.)⁡
	// Трансформация модулей на этапе загрузки.
	// Замена содержимого файлов.
	// Управление зависимостями.
}
// ⚠️ ⁡⁣⁣⁢15. Что такое "горячая замена модулей" (Hot Module Replacement) в Webpack?⁡
{
	// ✅ ⁡⁢⁢⁢Обновление модулей в рантайме без полной перезагрузки страницы, сохраняя состояние приложения. (HMR позволяет видеть изменения мгновенно.)⁡
	// Полная перезагрузка страницы при изменении кода.
	// Кэширование модулей.
	// Удаление неиспользуемых модулей.
}
// ⚠️ ⁡⁣⁣⁢16. Для чего используется инструмент Jest?⁡
{
	// ✅ ⁡⁢⁢⁢Для написания и запуска тестов. (Jest — это фреймворк для тестирования JavaScript, включающий средства для assertions, моков и покрытия кода.)⁡
	// Для сборки проекта.
	// Для линтинга.
	// Для форматирования кода.
}
// ⚠️ ⁡⁣⁣⁢17. Что означает флаг --watch в тестовых фреймворках?⁡
{
	// ✅ ⁡⁢⁢⁢Запуск тестов в режиме наблюдения за изменениями файлов и повторный прогон при изменениях. (Удобно для разработки.)⁡
	// Запуск тестов один раз.
	// Вывод подробного отчёта.
	// Пропуск медленных тестов.
}
// ⚠️ ⁡⁣⁣⁢18. Какой файл является точкой входа по умолчанию для npm-скриптов?⁡
{
	// ✅ ⁡⁢⁢⁢package.json (Скрипты определяются в поле "scripts" файла package.json и запускаются командой npm run <script>.)⁡
	// index.js
	// main.js
	// script.js
}
// ⚠️ ⁡⁣⁣⁢19. Что такое "source map"?⁡
{
	// ✅ ⁡⁢⁢⁢Файл, который связывает скомпилированный/минифицированный код с исходным, облегчая отладку. (Позволяет видеть оригинальный код в инструментах разработчика.)⁡
	// Карта сайта для поисковых систем.
	// Схема зависимостей модулей.
	// Список всех функций в проекте.
}
// ⚠️ ⁡⁣⁣⁢20. Какая команда обновляет все пакеты до последних версий согласно semver?⁡
{
	// ✅ ⁡⁢⁢⁢npm update (npm update обновляет пакеты до последних версий, разрешённых версиями в package.json.)⁡
	// npm upgrade
	// npm refresh
	// npm install --latest
}
// ⚠️ ⁡⁣⁣⁢21. Что такое "semver" (семантическое версионирование)?⁡
{
	// ✅ ⁡⁢⁢⁢Семантическое версионирование — стандарт определения версий с определёнными правилами их изменения, состоящая из трёх чисел: major.minor.patch. (major — несовместимые изменения, minor — новая функциональность с обратной совместимостью, patch — исправления.)⁡
	// Язык программирования.
	// Инструмент для сборки.
	// Протокол обмена данными.
}
// ⚠️ ⁡⁣⁣⁢22. Для чего в package.json используется поле "browserslist"?⁡
{
	// ✅ ⁡⁢⁢⁢Поле "browserslist" задаёт целевые браузеры, что используется инструментами (Babel, Autoprefixer) для определения необходимых трансформаций и полифиллов. (Browserslist позволяет централизованно задать целевые браузеры.)⁡
	// Для совместимости с браузерными API.
	// Для настройки сервера.
	// Для генерации манифеста.
}
// ⚠️ ⁡⁣⁣⁢23. Что такое "транспиляция"?⁡
{
	// ✅ ⁡⁢⁢⁡⁢⁢⁢Транспиляция — преобразование исходного кода в другой исходный код на том же или другом языке. Преобразование кода из одного языка в другой (например, CoffeeScript в JavaScript) или из одной версии в другую (ES6 в ES5). (Транспиляция — это компиляция из исходного кода в исходный код.)⁡
	// Выполнение кода.
	// Сжатие кода.
	// Обфускация.
}
// ⚠️ ⁡⁣⁣⁢24. Какой инструмент чаще всего используется для модульного тестирования React-компонентов?⁡
{
	// ✅ ⁡⁢⁢⁢Jest (Jest разработан специально для React и включает в себя всё необходимое для тестирования компонентов.)⁡
	// Mocha
	// Jasmine
	// Karma
}
// ⚠️ ⁡⁣⁣⁢25. Что такое "локальный хост" в контексте инструментов разработки?⁡
{
	// ✅ ⁡⁢⁢⁢Локальный хост — это локальный сервер на компьютере разработчика. (Обычно доступен по адресу http://localhost:порт.)⁡
	// Удалённый сервер.
	// Облачное хранилище.
	// Виртуальная машина.
}
// ⚠️ ⁡⁣⁣⁢26. Для чего используется флаг --save при установке npm-пакета?⁡
{
	// ✅ ⁡⁢⁢⁢Для добавления пакета в зависимости (dependencies) в package.json. (В современных версиях npm --save используется по умолчанию при npm install package.)⁡
	// Для установки пакета глобально.
	// Для сохранения конфигурации.
	// Для установки только для разработки.
}
// ⚠️ ⁡⁣⁣⁢27. Какое расширение файла используют конфигурации TypeScript?⁡
{
	// ✅ ⁡⁢⁢⁢tsconfig.json (Файл tsconfig.json содержит настройки компилятора TypeScript.)⁡
	// .tsconfig
	// .typescriptrc
	// config.ts
}
// ⚠️ ⁡⁣⁣⁢28. Что такое "мост" (bridge) в контексте нативных модулей React Native?⁡
{
	// ✅ ⁡⁢⁢⁢Мост (Bridge) — это ключевой архитектурный компонент React Native, обеспечивающий двустороннюю коммуникацию между JavaScript‑кодом (выполняется в JS‑потоке), нативными модулями (выполняются в нативном потоке: для iOS — Objective‑C/Swift, для Android — Java/Kotlin).⁡
	// Соединение двух экранов.
	// Библиотека для навигации.
	// Тип компонента.
}
// ⚠️ ⁡⁣⁣⁢29. Какой инструмент используется для управления состоянием в приложениях на React, кроме самого React?⁡
{
	// ✅ ⁡⁢⁢⁢Redux (Redux — популярная библиотека для управления состоянием, часто используемая с React.)⁡
	// Webpack
	// Babel
	// Axios
}
// ⚠️ ⁡⁣⁣⁢30. Что такое "JSX"?⁡
{
	// ✅ ⁡⁢⁢⁢JSX — синтаксическое расширение JavaScript для описания разметки. Расширение синтаксиса JavaScript, позволяющее писать HTML-подобный код внутри JS. (JSX транспилируется в вызовы React.createElement.)⁡
	// Новый язык программирования.
	// Библиотека для работы с DOM.
	// Шаблонизатор.
}
// ⚠️ ⁡⁣⁣⁢31. Для чего используется файл .eslintrc?⁡
{
	// ✅ ⁡⁢⁢⁢Для конфигурации правил ESLint. (В .eslintrc определяются правила, окружения, плагины и т.д.)⁡
	// Для настройки Prettier.
	// Для указания зависимостей.
	// Для настройки сборки.
}
// ⚠️ ⁡⁣⁣⁢32. Какая команда запускает тесты в проекте, если используется Jest и настроен скрипт "test" в package.json?⁡
{
	// ✅ ⁡⁢⁢⁢npm test (или npm run test) (Скрипты npm запускаются через npm run <script>, но test — исключение, можно просто npm test.)⁡
	// jest test
	// node test
	// test
}
// ⚠️ ⁡⁣⁣⁢33. Что такое "ленивая загрузка" (lazy loading) в контексте модулей?⁡
{
	// ✅ ⁡⁢⁢⁢Ленивая загрузка — техника разделения кода, загрузка модуля только тогда, когда он действительно нужен, а не сразу при старте приложения. (Уменьшает время начальной загрузки.)⁡
	// Откладывание выполнения кода.
	// Загрузка данных с сервера.
	// Кэширование модулей.
}
// ⚠️ ⁡⁣⁣⁢34. Какой лоадер в Webpack позволяет обрабатывать изображения и возвращать URL?⁡
{
	// ✅ ⁡⁢⁢⁢file-loader или url-loader (file-loader копирует файлы в выходную папку и возвращает URL, url-loader может инлайнить маленькие файлы как DataURL.)⁡
	// babel-loader
	// css-loader
	// style-loader
}
// ⚠️ ⁡⁣⁣⁢35. Для чего используется плагин HtmlWebpackPlugin?⁡
{
	// ✅ ⁡⁢⁢⁢Для генерации HTML-файла, который автоматически подключает все сгенерированные бандлы. (Упрощает подключение скриптов и стилей.)⁡
	// Для обработки HTML-файлов.
	// Для минификации HTML.
	// Для вставки стилей в head.
}
// ⚠️ ⁡⁣⁣⁢36. Что такое "NVM" (Node Version Manager)?⁡
{
	// ✅ ⁡⁢⁢⁢Инструмент для управления несколькими версиями Node.js на одной машине. (Позволяет легко переключаться между версиями Node.)⁡
	// Менеджер пакетов для Node.js.
	// Виртуальная машина для Node.js.
	// Отладчик.
}
// ⚠️ ⁡⁣⁣⁢37. Какую команду следует выполнить, чтобы установить TypeScript глобально?⁡
{
	// ✅ ⁡⁢⁢⁢npm install -g typescript (Флаг -g означает глобальную установку, доступную из любого места в системе.)⁡
	// npm install typescript
	// npm install --global typescript
	// npm install typescript -g
}
// ⚠️ ⁡⁣⁣⁢38. Что такое "алиасы" (aliases) в Webpack?⁡
{
	// ✅ ⁡⁢⁢⁢Сокращённые имена для путей к модулям, позволяющие избежать длинных относительных путей. (Например, можно настроить '@' как ссылку на src/.)⁡
	// Запасные имена для переменных.
	// Псевдонимы для функций.
	// Дополнительные точки входа.
}
// ⚠️ ⁡⁣⁣⁢39. Для чего используется инструмент "npm ci"?⁡
{
	// ✅ ⁡⁢⁢⁢npm ci — команда для быстрой и надёжной установки зависимостей в continuous integration на основе package-lock.json, игнорируя package.json, и удаления node_modules перед установкой. (Используется в CI-средах для быстрой и воспроизводимой установки.)⁡
	// Для создания нового package.json.
	// Для запуска скриптов.
	// Для обновления зависимостей.
}
// ⚠️ ⁡⁣⁣⁢40. Что такое "bundle" в контексте сборки?⁡
{
	// ✅ ⁡⁢⁢⁢Бандл — один файл (или несколько), содержащий объединённый код всех модулей и зависимостей. (Результат работы сборщика.)⁡
	// Пакет npm.
	// Архив с проектом.
	// Конфигурация.
}
//! ТЕМА 2: РАБОТА С EVENT LOOP И DOM
// ⚠️ ⁡⁣⁣⁢41. Что такое «таск» (macrotask) и «микрозадача» (microtask) в Event Loop?⁡
{
	// ✅ ⁡⁢⁢⁢Таски — это setTimeout, setInterval, setImmediate. Микрозадачи — это Promise.then/catch/finally, queueMicrotask, MutationObserver. (Таски и микрозадачи — очереди событий с разным приоритетом: после каждого таска выполняются все накопившиеся микрозадачи.)⁡
	// Это одно и то же, разница только в приоритете.
	// Таски выполняются в основном потоке, микрозадачи — в Web Worker.
	// Микрозадачи — это только queueMicrotask.
}
// ⚠️ ⁡⁣⁣⁢42. Что будет выведено?⁡
{
	setTimeout(() => console.log("timeout"), 0);
	Promise.resolve().then(() => console.log("promise"));
	console.log("log");
	{
		// ✅ ⁡⁢⁢⁢log, promise, timeout (Сначала синхронный код (log), затем микрозадачи (promise), затем макротаски  (timeout).)⁡
		// log, timeout, promise
		// promise, log, timeout
		// timeout, promise, log
	}
}
// ⚠️ ⁡⁣⁣⁢43. Какой метод позволяет отложить выполнение функции до ближайшего кадра анимации?⁡
{
	// ✅ ⁡⁢⁢⁢requestAnimationFrame(fn) (requestAnimationFrame планирует выполнение перед следующей перерисовкой страницы, что идеально для анимаций.)⁡
	// setTimeout(fn, 0)
	// setImmediate(fn)
	// queueMicrotask(fn)
}
// ⚠️ ⁡⁣⁣⁢44. Что произойдёт при вызове event.stopPropagation()?⁡
{
	// ✅ ⁡⁢⁢⁢Остановит дальнейшее всплытие события. (Событие не будет передано родительским элементам.)⁡
	// Предотвратит действие браузера по умолчанию (например, переход по ссылке).
	// Немедленно завершит обработку всех слушателей на текущем элементе.
	// Удалит обработчик события.
}
// ⚠️ ⁡⁣⁣⁢45. В какой фазе события DOM вызывается обработчик, добавленный через addEventListener без указания третьего параметра или с false?⁡
{
	// ✅ ⁡⁢⁢⁢В фазе всплытия (bubbling). (По умолчанию обработчики срабатывают на фазе всплытия.)⁡
	// В фазе перехвата (capture).
	// В фазе цели (target).
	// Во всех фазах.
}
// ⚠️ ⁡⁣⁣⁢46. Какой метод предотвращает действие браузера по умолчанию (например, отправку формы)?⁡
{
	// ✅ ⁡⁢⁢⁢event.preventDefault() (preventDefault отменяет стандартное поведение браузера для события.)⁡
	// event.stopPropagation()
	// event.stopImmediatePropagation()
	// event.cancelBubble()
}
// ⚠️ ⁡⁣⁣⁢47. Что такое "делегирование событий"?⁡
{
	// ✅ ⁡⁢⁢⁡⁢⁢⁢Делегирование событий — использование всплытия для обработки событий на предке вместо каждого потомка. Назначение одного обработчика родительскому элементу для обработки событий от дочерних элементов благодаря всплытию. (Эффективно для динамически добавляемых элементов.)⁡
	// Передача обработки события от одного элемента другому.
	// Принудительный вызов события.
	// Отмена события.
}
// ⚠️ ⁡⁣⁣⁢48. Какой объект предоставляет информацию о пересечении элемента с областью видимости или другим элементом?⁡
{
	// ✅ ⁡⁢⁢⁢IntersectionObserver (IntersectionObserver асинхронно отслеживает пересечение целевого элемента с предком или viewport.)⁡
	// MutationObserver
	// ResizeObserver
	// PerformanceObserver
}
// ⚠️ ⁡⁣⁣⁢49. Как подписаться на изменения в дочерних элементах DOM (добавление, удаление, изменение атрибутов)?⁡
{
	// ✅ ⁡⁢⁢⁢С помощью MutationObserver. (MutationObserver позволяет отслеживать изменения DOM: добавление/удаление узлов, изменение атрибутов и текста.)⁡
	// С помощью события 'change'.
	// С помощью события 'DOMSubtreeModified'.
	// С помощью IntersectionObserver.
}
// ⚠️ ⁡⁣⁣⁢50. Какое значение имеет this внутри обработчика события, добавленного через addEventListener, если обработчик — обычная функция (не стрелочная)?⁡
{
	// ✅ ⁡⁢⁢⁢Элемент, на который был добавлен обработчик (event.currentTarget). (В обработчике this ссылается на элемент, на котором висит слушатель.)⁡
	// Глобальный объект (window).
	// Сам элемент, на котором сработало событие (после всплытия может отличаться от currentTarget).
	// undefined.
}
// ⚠️ ⁡⁣⁣⁢51. Чем отличается e.target от e.currentTarget?⁡
{
	// ✅ ⁡⁢⁢⁢e.target — это элемент, на котором произошло событие (самый глубокий). e.currentTarget — элемент, на который назначен обработчик. (При всплытии target остаётся исходным, а currentTarget может меняться.)⁡
	// Это одно и то же.
	// e.target — элемент, на который назначен обработчик, e.currentTarget — элемент, вызвавший событие.
	// e.target — это всегда document.
}
// ⚠️ ⁡⁣⁣⁢52. Что выведет код?⁡
{
	for (var i = 0; i < 3; i++) {
		setTimeout(() => console.log(i), 0);
	}
	{
		// ✅ ⁡⁢⁢⁢3,3,3 (var создаёт одну переменную i на всю функцию, и к моменту выполнения колбэков цикл уже завершится со значением 3.)⁡
		// 0,1,2
		// 0,0,0
		// Ошибка
	}
}
// ⚠️ ⁡⁣⁣⁢53. Как исправить предыдущую задачу, чтобы выводилось 0,1,2, используя let?⁡
{
	// ✅ ⁡⁢⁢⁢Заменить var на let, тогда для каждой итерации создаётся своя область видимости. (let имеет блочную область видимости, и каждое замыкание получает своё значение i.)⁡
	// Использовать bind.
	// Использовать дополнительную функцию.
	// Всё правильно, так и есть.
}
// ⚠️ ⁡⁣⁣⁢54. Что такое "асинхронность" в JavaScript и как она достигается?⁡
{
	// ✅ ⁡⁢⁢⁢Способ выполнения операций без блокировки основного потока, достигается с помощью callback, промисов, async/await и Event Loop. (JavaScript однопоточный, но благодаря событийному циклу может обрабатывать отложенные задачи.)⁡
	// Возможность выполнять несколько операций параллельно.
	// Возможность вызывать функции по таймеру.
	// Многопоточность.
}
// ⚠️ ⁡⁣⁣⁢55. В каком порядке выполнятся console.log?⁡
{
	console.log(1);
	setTimeout(() => console.log(2), 0);
	Promise.resolve().then(() => console.log(3));
	console.log(4);
	{
		// ✅ ⁡⁢⁢⁢1,4,3,2 (Синхронный код: 1 и 4, затем микрозадача промиса: 3, затем макротаска: 2.)⁡
		// 1,4,2,3
		// 1,2,3,4
		// 4,1,3,2
	}
}
// ⚠️ ⁡⁣⁣⁢56. Что делает метод element.matches(selector)?⁡
{
	// ✅ ⁡⁢⁢⁢Проверяет, удовлетворяет ли элемент указанному CSS-селектору. (Возвращает true, если элемент подходит под селектор.)⁡
	// Проверяет, есть ли у элемента такой селектор в CSS.
	// Ищет ближайшего родителя по селектору.
	// Применяет селектор к элементу.
}
// ⚠️ ⁡⁣⁣⁢57. Как получить ближайшего родителя элемента, соответствующего заданному селектору?⁡
{
	// ✅ ⁡⁢⁢⁢element.closest(selector) (Метод closest идёт вверх по дереву и возвращает ближайший элемент, подходящий под селектор, включая сам элемент.)⁡
	// element.parentElement
	// element.querySelector(selector)
	// element.matches(selector)
}
// ⚠️ ⁡⁣⁣⁢58. Что такое "теневой DOM" (Shadow DOM)?⁡
{
	// ✅ ⁡⁢⁢⁢Технология для инкапсуляции DOM-дерева и стилей внутри компонента, изолируя его от основного документа. (Используется в веб-компонентах.)⁡
	// Скрытая часть документа.
	// Виртуальное представление DOM.
	// Отражение DOM.
}
// ⚠️ ⁡⁣⁣⁢59. Как создать асинхронную функцию, которая возвращает промис?⁡
{
	// ✅ ⁡⁢⁢⁢Добавить async перед функцией, тогда она автоматически оборачивает возвращаемое значение в промис. (Асинхронная функция всегда возвращает промис.)⁡
	// Использовать ключевое слово async перед функцией.
	// Использовать new Promise() внутри.
	// Любая функция асинхронна.
}
// ⚠️ ⁡⁣⁣⁢60. Что выведет следующий код?⁡
{
	async function test() {
		return 42;
	}
	console.log(test());
	{
		// ✅ ⁡⁢⁢⁢Promise { 42 } (Асинхронная функция возвращает промис, который резолвится со значением 42.)⁡
		// 42
		// undefined
		// Ошибка
	}
}
// ⚠️ ⁡⁣⁣⁢61. Как дождаться выполнения нескольких промисов параллельно и получить все результаты?⁡
{
	// ✅ ⁡⁢⁢⁢Использовать Promise.all([p1, p2, p3]). (Promise.all ждёт выполнения всех промисов и возвращает массив результатов.)⁡
	// Использовать await для каждого по очереди.
	// Использовать Promise.race.
	// Использовать цикл с await.
}
// ⚠️ ⁡⁣⁣⁢62. Чем отличается Promise.all от Promise.allSettled?⁡
{
	// ✅ ⁡⁢⁢⁢Promise.all завершается ошибкой при первом же rejected промисе. Promise.allSettled ждёт завершения всех промисов и возвращает массив объектов с результатами (статус и значение/причину). (allSettled никогда не переходит в rejected.)⁡
	// Они идентичны.
	// Promise.allSettled завершается ошибкой при любом rejected.
	// Promise.all возвращает только первый выполненный промис.
}
// ⚠️ ⁡⁣⁣⁢63. Что выведет этот код?⁡
{
	setTimeout(() => console.log("A"), 0);
	Promise.resolve()
		.then(() => console.log("B"))
		.then(() => console.log("C"));
	console.log("D");
	{
		// ✅ ⁡⁢⁢⁢D, B, C, A (Синхронный D, затем микрозадачи: B и C (цепочка), затем макротаска A.)⁡
		// A, B, C, D
		// D, A, B, C
		// D, B, A, C
	}
}
// ⚠️ ⁡⁣⁣⁢64. Как остановить всплытие события и предотвратить выполнение других обработчиков на этом же элементе?⁡
{
	// ✅ ⁡⁢⁢⁢event.stopImmediatePropagation() (Этот метод не только останавливает всплытие, но и предотвращает вызов последующих обработчиков на текущем элементе.)⁡
	// event.stopPropagation()
	// event.preventDefault()
	// event.cancelBubble()
}
// ⚠️ ⁡⁣⁣⁢65. В какой микротаске выполняется колбэк queueMicrotask?⁡
{
	// ✅ ⁡⁢⁢⁢В очереди микрозадач, после завершения текущей макротаски и перед следующей. (queueMicrotask добавляет задачу в ту же очередь, что и then промисов.)⁡
	// В ближайшей макротаске.
	// Сразу же синхронно.
	// В следующем кадре анимации.
}
// ⚠️ ⁡⁣⁣⁢66. Что такое "событийный цикл" (Event Loop) простыми словами?⁡
{
	// ✅ ⁡⁢⁢⁢Event Loop — это бесконечный цикл, который обрабатывает макро- и микрозадачи, обеспечивая асинхронность.⁡
	// Цикл for.
	// Способ обработки ошибок.
	// Библиотека.
}
// ⚠️ ⁡⁣⁣⁢67. Что будет, если в микрозадаче создать новую микрозадачу?⁡
{
	// ✅ ⁡⁢⁢⁢Она выполнится в том же цикле микрозадач, до перехода к следующей макротаске. (Микрозадачи могут добавлять новые микрозадачи, и все они будут выполнены до окончания текущего цикла.)⁡
	// Она выполнится только после следующей макротаски.
	// Она выполнится синхронно.
	// Это приведёт к бесконечному циклу.
}
// ⚠️ ⁡⁣⁣⁢68. Какое событие происходит, когда элемент теряет фокус?⁡
{
	// ✅ ⁡⁢⁢⁢blur (blur срабатывает при потере фокуса, focus — при получении.)⁡
	// focus
	// change
	// input
}
// ⚠️ ⁡⁣⁣⁢69. Как получить все стили элемента, включая унаследованные, после применения CSS?⁡
{
	// ✅ ⁡⁢⁢⁢window.getComputedStyle(element) (getComputedStyle возвращает объект со всеми вычисленными стилями элемента.)⁡
	// element.style
	// element.currentStyle
	// element.className
}
// ⚠️ ⁡⁣⁣⁢70. Что такое "виртуальный DOM" (Virtual DOM) и где он используется?⁡
{
	// ✅ ⁡⁢⁢⁢Легковесное представление реального DOM в виде JavaScript-объектов, используется в React для оптимизации обновлений. (Сначала изменения вносятся в Virtual DOM, затем вычисляется разница и применяется к реальному DOM.)⁡
	// Настоящий DOM, спрятанный в памяти.
	// DOM в iframe.
	// Кэш DOM-узлов.
}
// ⚠️ ⁡⁣⁣⁢71. В чём разница между display: none и visibility: hidden с точки зрения JavaScript?⁡
{
	// ✅ ⁡⁢⁢⁢display: none удаляет элемент из потока, visibility: hidden скрывает, но элемент остаётся в потоке.⁡
	// Элемент с display: none не занимает места и не виден, с visibility: hidden — занимает место, но не виден. (Оба скрывают элемент, но display удаляет из потока.)
	// Никакой разницы.
	// display: none скрывает, visibility: hidden удаляет.
	// display: none скрывает содержимое, но оставляет блок.
}
// ⚠️ ⁡⁣⁣⁢72. Как добавить класс элементу через JavaScript?⁡
{
	// ✅ ⁡⁢⁢⁢element.classList.add('new') (classList предоставляет удобные методы для работы с классами: add, remove, toggle, contains.)⁡
	// element.class = 'new'
	// element.className = 'new'
	// element.setAttribute('class', 'new')
}
// ⚠️ ⁡⁣⁣⁢73. Какое значение имеет arguments внутри стрелочной функции?⁡
{
	// ✅ ⁡⁢⁢⁢Ошибка или undefined, т.к. стрелочные функции не имеют своего объекта arguments. (Они наследуют arguments из внешней области видимости.)⁡
	// Массив аргументов.
	// Псевдомассив аргументов.
	// Объект с аргументами.
}
// ⚠️ ⁡⁣⁣⁢74. Чем отличается localStorage от sessionStorage?⁡
{
	// ✅ ⁡⁢⁢⁢localStorage хранит данные без срока давности, sessionStorage — до закрытия вкладки/окна. (Оба хранят данные в браузере, но с разным временем жизни.)⁡
	// localStorage хранит только строки, sessionStorage — любые типы.
	// localStorage работает только с HTTPS.
	// sessionStorage — это то же самое, что и cookies.
}
// ⚠️ ⁡⁣⁣⁢75. Какой API позволяет отправлять уведомления пользователю даже когда вкладка не активна?⁡
{
	// ✅ ⁡⁢⁢⁢Notification API (требует разрешения) (Notification показывает системные уведомления.)⁡
	// Alert API
	// Push API
	// Console API
}
// ⚠️ ⁡⁣⁣⁢76. Что такое requestIdleCallback?⁡
{
	// ✅ ⁡⁢⁢⁢requestIdleCallback — метод для планирования задачи, которая будет выполнена, когда браузер находится в простое (нет высокоприоритетных задач). (Позволяет выполнять фоновую работу без ущерба для производительности.)⁡
	// То же, что requestAnimationFrame.
	// Запрос на перерисовку.
	// Обработчик бездействия мыши.
}
// ⚠️ ⁡⁣⁣⁢77. Какой метод используется для создания глубокой копии объекта?⁡
{
	// ✅ ⁡⁢⁢⁢structuredClone(obj) (structuredClone — современный метод для глубокого клонирования, поддерживающий многие типы, включая Date, Map, Set.)⁡
	// Object.assign({}, obj)
	// JSON.parse(JSON.stringify(obj))
	// obj.slice()
}
// ⚠️ ⁡⁣⁣⁢78. Что выведет код?⁡
{
	const promise = new Promise((resolve, reject) => {
		console.log(1);
		resolve(2);
		console.log(3);
	});
	promise.then(console.log);
	console.log(4);
	{
		// ✅ ⁡⁢⁢⁢1,3,4,2 (Функция исполнитель промиса выполняется синхронно: выводит 1 и 3. Затем then планирует микрозадачу. Далее синхронный 4. После синхронного кода выполняется микрозадача с 2.)⁡
		// 1,2,3,4
		// 1,4,2,3
		// 1,3,2,4
	}
}
// ⚠️ ⁡⁣⁣⁢79. Как отловить ошибку в асинхронной функции, если она использует await?⁡
{
	// ✅ ⁡⁢⁢⁢С помощью try/catch внутри функции или .catch() на возвращаемом промисе. (await внутри try/catch ловит ошибки от rejected промиса.)⁡
	// С помощью .catch() на вызове функции.
	// Ошибки нельзя отловить.
	// С помощью window.onerror.
}
// ⚠️ ⁡⁣⁣⁢80. Что такое event.preventDefault() и в каких случаях он применяется?⁡
{
	// ✅ ⁡⁢⁢⁢Отменяет стандартное действие браузера для события (например, отправку формы, переход по ссылке). (Применяется, когда нужно переопределить поведение по умолчанию.)⁡
	// Отменяет дальнейшее всплытие события.
	// Останавливает выполнение всех обработчиков.
	// Удаляет событие.
}
// ⚠️ ⁡⁣⁣⁢81. Какой метод позволяет скопировать текст в буфер обмена?⁡
{
	// ✅ ⁡⁢⁢⁢navigator.clipboard.writeText(text) (Clipboard API предоставляет асинхронные методы для работы с буфером обмена.)⁡
	// copy()
	// document.execCommand('copy')
	// window.copy(text)
}
// ⚠️ ⁡⁣⁣⁢82. Что выведет следующий код?⁡
{
	console.log("start");
	setTimeout(() => console.log("timeout1"), 0);
	setTimeout(() => {
		console.log("timeout2");
		Promise.resolve().then(() => console.log("promise inside timeout2"));
	}, 0);
	setTimeout(() => console.log("timeout3"), 0);
	Promise.resolve().then(() => console.log("promise1"));
	Promise.resolve().then(() => console.log("promise2"));
	console.log("end");
	{
		// ✅ ⁡⁢⁢⁢start, end, promise1, promise2, timeout1, timeout2, promise inside timeout2, timeout3 (Синхронные start и end, затем все микрозадачи (promise1, promise2), потом первая макротаска timeout1, затем вторая timeout2, внутри которой синхронно выводится timeout2, а затем добавляется микрозадача promise inside timeout2, которая выполняется до следующей макротаски timeout3.)⁡
		// start, end, promise1, promise2, timeout1, timeout2, promise inside timeout2, timeout3
		// start, end, promise1, promise2, timeout1, timeout2, timeout3, promise inside timeout2
		// start, end, timeout1, promise1, promise2, timeout2, promise inside timeout2, timeout3
	}
}
// ⚠️ ⁡⁣⁣⁢83. Какое значение вернёт typeof null?⁡
{
	// ✅ ⁡⁢⁢⁢'object' (Это историческая ошибка в JavaScript, null считается объектом.)⁡
	// 'null'
	// 'undefined'
	// 'boolean'
}
// ⚠️ ⁡⁣⁣⁢84. Что такое Symbol в JavaScript?⁡
{
	// ✅ ⁡⁢⁢⁢Примитивный тип данных, создающий уникальное значение. (Symbol гарантирует уникальность и часто используется как ключ для свойств объектов.)⁡
	// Строковое представление числа.
	// Специальный объект.
	// Псевдоним для функции.
}
// ⚠️ ⁡⁣⁣⁢85. Как узнать, поддерживает ли браузер ту или иную функцию?⁡
{
	// ✅ ⁡⁢⁢⁢Проверить наличие свойства в глобальном объекте (например, 'Promise' in window). (Самый надёжный способ — проверка существования нужного объекта или метода.)⁡
	// Использовать Modernizr.
	// Использовать try/catch при вызове.
	// Прочитать User-Agent.
}
// ⚠️ ⁡⁣⁣⁢86. Чем отличается mouseenter от mouseover?⁡
{
	// ✅ ⁡⁢⁢⁢mouseenter не всплывает и не срабатывает при переходе на дочерние элементы, в отличие от mouseover. (mouseenter удобен для попадания в область элемента без учёта детей.)⁡
	// mouseover не всплывает.
	// Это синонимы.
	// mouseenter срабатывает только при нажатии кнопки мыши.
}
// ⚠️ ⁡⁣⁣⁢87. Какой метод позволяет получить координаты элемента относительно viewport?⁡
{
	// ✅ ⁡⁢⁢⁢element.getBoundingClientRect() (Возвращает объект с координатами top, left, bottom, right относительно окна браузера.)⁡
	// element.offsetTop
	// element.clientTop
	// element.scrollTop
}
// ⚠️ ⁡⁣⁣⁢88. Что такое WeakMap и для чего он используется?⁡
{
	// ✅ ⁡⁢⁢⁢WeakMap — это коллекция ключ-значение, где ключи — объекты, которые могут быть собраны сборщиком мусора, если на них нет других ссылок. (Используется для хранения дополнительных данных, привязанных к объекту, без предотвращения сборки мусора.)⁡
	// Обычный объект.
	// Коллекция с любыми ключами.
	// Устаревшая структура.
}
// ⚠️ ⁡⁣⁣⁢89. Что выведет код?⁡
{
	console.log(1);
	setTimeout(() => console.log(2), 0);
	Promise.resolve().then(() => setTimeout(() => console.log(3), 0));
	Promise.resolve().then(() => console.log(4));
	console.log(5);
	{
		// ✅ ⁡⁢⁢⁢1,5,4,2,3 (После синхронного 1 и 5 выполняются микрозадачи: 4, а также микрозадача, которая ставит макротаску с 3. Затем выполняются макротаски: первая 2, вторая 3.)⁡
		// 1,5,2,4,3
		// 1,5,4,3,2
	}
}
// ⚠️ ⁡⁣⁣⁢90. Как работает оператор ?? (nullish coalescing)?⁡
{
	// ✅ ⁡⁢⁢⁢Оператор ?? возвращает правый операнд, если левый равен null или undefined, иначе левый. (Отличается от || тем, что не считает falsy-значения (0, '', false) за отсутствие значения.)⁡
	// Возвращает левый операнд, если он истинный, иначе правый.
	// То же, что &&.
	// Возвращает правый операнд всегда.
}
//! ТЕМА 3: API И БИБЛИОТЕКИ
// ⚠️ ⁡⁣⁣⁢91. Какая основная задача библиотеки Axios?⁡
{
	// ✅ ⁡⁢⁢⁢Выполнение HTTP-запросов из браузера или Node.js. (Axios предоставляет удобный API для работы с промисами, интерцепторы, отмену запросов.)⁡
	// Управление состоянием.
	// Работа с DOM.
	// Анимация.
}
// ⚠️ ⁡⁣⁣⁢92. Что такое WebSocket и для чего используется?⁡
{
	// ✅ ⁡⁢⁢⁢Протокол двусторонней связи между клиентом и сервером по одному TCP-соединению. (Позволяет отправлять данные в реальном времени без постоянных HTTP-запросов.)⁡
	// Протокол для отправки уведомлений.
	// Библиотека для работы с сокетами.
	// API для работы с файлами.
}
// ⚠️ ⁡⁣⁣⁢93. Какой метод Fetch API используется для отправки POST-запроса?⁡
{
	// ✅ ⁡⁢⁢⁢fetch(url, { method: 'POST', body: data }) (Fetch по умолчанию GET, для POST нужно указать метод и тело.)
	// fetch.post(url, data)⁡
	// fetch(url, { method: 'POST' }) без body.
	// axios.post(url, data)
}
// ⚠️ ⁡⁣⁣⁢94. В чём отличие Fetch API от XMLHttpRequest?⁡
{
	// ✅ ⁡⁢⁢⁢Fetch основан на промисах, более современный и гибкий. XMLHttpRequest — старый, событийно-ориентированный. (Fetch проще и чище, но не поддерживает отслеживание прогресса загрузки.)⁡
	// Fetch работает только с JSON.
	// XMLHttpRequest не поддерживает промисы.
}
// ⚠️ ⁡⁣⁣⁢95. Как отменить выполнение Fetch-запроса?⁡
{
	// ✅ ⁡⁢⁢⁢Использовать AbortController и сигнал AbortSignal.  (AbortController позволяет отправить сигнал об отмене запроса.)⁡
	// Нельзя отменить.
	// Вызвать .abort() на запросе.
	// Использовать clearTimeout.
}
// ⚠️ ⁡⁣⁣⁢96.Что такое Service Worker?⁡
{
	// ✅ ⁡⁢⁢⁢Service Worker — сценарий, который браузер запускает в фоновом режиме, отдельно от веб-страницы, позволяя перехватывать сетевые запросы, кэшировать ресурсы и работать офлайн. (Основа PWA.)⁡
	// Веб-воркер для тяжёлых вычислений.
	// Библиотека для работы с сервисами.
	// Обработчик событий.
}
// ⚠️ ⁡⁣⁣⁢97. Какой API позволяет хранить большие объёмы структурированных данных в браузере?⁡
{
	// ✅ ⁡⁢⁢⁢IndexedDB (IndexedDB — это низкоуровневая NoSQL-база данных в браузере для хранения больших объёмов данных, включая файлы.)⁡
	// localStorage
	// sessionStorage
	// Cookies
}
// ⚠️ ⁡⁣⁣⁢98. Для чего используется библиотека Lodash?⁡
{
	// ✅ ⁡⁢⁢⁢Предоставляет множество вспомогательных функций для работы с массивами, объектами, строками и т.д. (Утилиты, такие как debounce, throttle, cloneDeep, и др.)⁡
	// Для работы с DOM.
	// Для построения интерфейсов.
	// Для тестирования.
}
// ⚠️ ⁡⁣⁣⁢99. Что такое History API в браузере?⁡
{
	// ✅ ⁡⁢⁢⁢History API для работы с историей посещений, позволяющее изменять URL без перезагрузки страницы (для SPA). (Методы pushState, replaceState, событие popstate.)⁡
	// API для получения истории браузера.
	// Библиотека для навигации.
	// Способ сохранения состояния.
}
// ⚠️ ⁡⁣⁣⁢100. Какой метод React используется для оптимизации производительности функциональных компонентов, предотвращая лишние рендеры?⁡
{
	// ✅ ⁡⁢⁢⁢React.memo (React.memo — это HOC, который мемоизирует функциональный компонент, предотвращая его повторный рендер, если пропсы не изменились.)⁡
	// shouldComponentUpdate
	// PureComponent
	// useCallback
}
// ⚠️ ⁡⁣⁣⁢101. Что такое "поток" (stream) в Node.js?⁡
{
	// ✅ ⁡⁢⁢⁢Поток — это способ асинхронной обработки данных по частям, не загружая всё в память. (Потоки используются для чтения/записи файлов, сетевых запросов и т.д.)⁡
	// Последовательность операций.
	// Тип данных.
	// Поток выполнения.
}
// ⚠️ ⁡⁣⁣⁢102. Какая библиотека чаще всего используется для управления состоянием в React?⁡
{
	// ✅ ⁡⁢⁢⁢Redux или Zustand (Redux — классическое решение, Zustand — более лёгкое и современное.)⁡
	// Axios
	// Lodash
	// Moment
}
// ⚠️ ⁡⁣⁣⁢103. Что такое Web Worker?⁡
{
	// ✅ ⁡⁢⁢⁢Сценарий, выполняющийся в фоновом потоке, отдельном от основного потока браузера. (Позволяет выполнять тяжёлые вычисления, не блокируя интерфейс.)⁡
	// То же, что Service Worker.(Service Worker — это скрипт, который работает в фоновом режиме и действует как посредник между веб-приложением и сетью. Он перехватывает сетевые запросы, управляет кэшированием и обеспечивает работу приложения в офлайн-режиме.)
	// API для работы с воркерами.
	// Событие.
}
// ⚠️ ⁡⁣⁣⁢104. Какие ограничения у Web Worker?⁡
{
	// ✅ ⁡⁢⁢⁢Нет доступа к DOM, объекту window, document. Есть доступ к navigator, location (только чтение), XMLHttpRequest, fetch, IndexedDB, таймерам. (Изоляция от основного потока.)⁡
	// Нет доступа к сети.
	// Не могут использовать таймеры.
	// Не могут обмениваться данными с основным потоком.
}
// ⚠️ ⁡⁣⁣⁢105. Для чего используется метод debounce из Lodash?⁡
{
	// ✅ ⁡⁢⁢⁢Для ограничения частоты вызова функции, гарантируя, что она выполнится только после того, как прошло определённое время с момента последнего вызова. (Полезно для обработки ввода в поисковой строке.)⁡
	// Для кэширования результатов.
	// Для асинхронного вызова.
	// Для мемоизации.
}
// ⚠️ ⁡⁣⁣⁢106. Чем debounce отличается от throttle?⁡
{
	// ✅ ⁡⁢⁢⁢debounce ждёт паузы, throttle гарантирует выполнение не чаще одного раза в указанный интервал. (Debounce — "только после тишины", throttle — "регулярно, но не слишком часто".)⁡
	// Это одно и то же.
	// throttle — то же, что debounce, но с максимальной задержкой.
	// debounce используется для анимаций.
}
// ⚠️ ⁡⁣⁣⁢107. Какой API позволяет работать с геолокацией пользователя?⁡
{
	// ✅ ⁡⁢⁢⁢Geolocation API (navigator.geolocation) (Позволяет получать текущее местоположение и отслеживать его изменения с разрешения пользователя.)⁡
	// Location API
	// Maps API
	// GPS API
}
// ⚠️ ⁡⁣⁣⁢108. Что такое canvas и для чего используется?⁡
{
	// ✅ ⁡⁢⁢⁢Элемент HTML5 для рисования 2D- и 3D-графики с помощью JavaScript. (Используется для игр, визуализации данных, обработки изображений.)⁡
	// Библиотека для рисования.
	// Видеоплеер.
	// Тип изображения.
}
// ⚠️ ⁡⁣⁣⁢109. Какой метод у canvas используется для получения контекста рисования?⁡
{
	// ✅ ⁡⁢⁢⁢canvas.getContext('2d') (Для 2D-графики передают '2d', для WebGL — 'webgl'.)⁡
	// canvas.context
	// canvas.beginPath()
	// canvas.draw()
}
// ⚠️ ⁡⁣⁣⁢110. Что такое "полифилл" в контексте библиотек?⁡
{
	// ✅ ⁡⁢⁢⁢Полифилл — это кусок кода, реализующий современную функциональность в старых браузерах. (Библиотеки вроде core-js добавляют полифиллы для новых возможностей.)⁡
	// Библиотека для тестирования.
	// Инструмент сборки.
	// Расширение браузера.
}
// ⚠️ ⁡⁣⁣⁢111. Какая библиотека используется для работы с датами в JavaScript?⁡
{
	// ✅ ⁡⁢⁢⁢Date-fns или Moment.js (Обе предоставляют удобные функции для парсинга, форматирования и манипуляции датами.)⁡
	// Lodash
	// Axios
	// jQuery
}
// ⚠️ ⁡⁣⁣⁢112. Что такое REST API?⁡
{
	// ✅ ⁡⁢⁢⁢REST — это стиль построения API над HTTP, архитектурный стиль взаимодействия компонентов распределённого приложения. (Обычно подразумевает HTTP-запросы к ресурсам с использованием методов GET, POST, PUT, DELETE.)⁡
	// Библиотека для запросов.
	// Протокол передачи данных.
	// Способ хранения данных.
}
// ⚠️ ⁡⁣⁣⁢113. Как обработать ошибку в Fetch, когда сервер вернул HTTP-статус 404?⁡
{
	// ✅ ⁡⁢⁢⁢Fetch не считает 404 ошибкой, промис резолвится, но свойство ok ответа будет false. Нужно проверять response.ok или response.status. (Ошибка сети или CORS вызывает reject.)⁡
	// Автоматически перейдёт в catch.
	// Нужно использовать try/catch.
	// Вызовется обработчик ошибки.
}
// ⚠️ ⁡⁣⁣⁢114. Для чего используется библиотека Immer?⁡
{
	// ✅ ⁡⁢⁢⁢Для работы с неизменяемыми данными, позволяя писать "мутирующий" код, который на самом деле создаёт новое неизменяемое состояние. (Упрощает работу с состоянием в Redux и React.)⁡
	// Для создания анимаций.
	// Для HTTP-запросов.
	// Для форматирования кода.
}
// ⚠️ ⁡⁣⁣⁢115. Что такое JWT (JSON Web Token)?⁡
{
	// ✅ ⁡⁢⁢⁢Стандарт для создания токенов аутентификации, обычно используется в HTTP-заголовке Authorization для подтверждения личности. (Токен содержит подпись и может быть проверен сервером.)⁡
	// Формат данных.
	// Библиотека для шифрования.
	// Протокол авторизации.
}
// ⚠️ ⁡⁣⁣⁢116. Какой метод console позволяет вывести предупреждение?⁡
{
	// ✅ ⁡⁢⁢⁢console.warn (Выводит жёлтое предупреждение в консоль.)⁡ // ⚠️
	// console.error // ⛔
	// console.log
	// console.info
}
// ⚠️ ⁡⁣⁣⁢117. Что такое CORS?⁡
{
	// ✅ ⁡⁢⁢⁢CORS — политика общего доступа к ресурсам между разными источниками, механизм безопасности браузера, который ограничивает запросы с других доменов, если сервер явно не разрешил это. (Cross-Origin Resource Sharing.)⁡
	// Способ обмена данными.
	// Библиотека для работы с запросами.
	// Тип ошибки.
}
// ⚠️ ⁡⁣⁣⁢118. Как получить данные из локального хранилища?⁡
{
	// ✅ ⁡⁢⁢⁢localStorage.getItem(key) (Возвращает значение по ключу или null.)⁡
	// localStorage.get(key)
	// localStorage.retrieve(key)
	// localStorage.getItem(key, defaultValue)
}
// ⚠️ ⁡⁣⁣⁢119. Что такое "SPA" (Single Page Application)?⁡
{
	// ✅ ⁡⁢⁢⁢SPA — это одностраничное приложение с динамическим обновлением контента, приложение, которое загружает одну HTML-страницу и динамически обновляет её при взаимодействии, без полной перезагрузки. (Использует History API и JavaScript для рендеринга.)⁡
	// Одностраничный сайт.
	// Приложение с одной страницей.
	// Технология для мобильных приложений.
}
// ⚠️ ⁡⁣⁣⁢120. Какая библиотека обычно используется для создания пользовательских интерфейсов в связке с React?⁡
{
	// ✅ ⁡⁢⁢⁢React — Material UI (также известная как MUI). Это открытая библиотека компонентов, которая реализует дизайн-систему Material Design от Google. Она позволяет быстро создавать приложения за счёт готовых компонентов и предоставляет гибкие возможности кастомизации.⁡
	// React сама по себе и есть библиотека для UI.
	// ReactDOM для рендеринга в браузере.
	// React Router.
}
