//! 🧪 JAVASCRIPT TEST: MIDDLE — SENIOR (130 ВОПРОСОВ)
// ⁡⁢⁣⁣Темы:⁡
// ⁡⁣⁣⁢• Вспомогательные инструменты⁡
// ⁡⁣⁣⁢• Event loop и DOM⁡
// ⁡⁣⁣⁢• API и библиотеки⁡
// Формат: вопрос → варианты → ✅ правильный ответ + комментарий
// 🔧 ⁡⁢⁣⁣РАЗДЕЛ 1: ВСПОМОГАТЕЛЬНЫЕ ИНСТРУМЕНТЫ ДЛЯ JAVASCRIPT (ВОПРОСЫ 1–43)⁡
{
	// ⚠️ ⁡⁣⁣⁢1. Какой метод console позволяет группировать вывод с отступами?⁡
	{
		// ✅ ⁡⁢⁢⁢console.group() / console.groupEnd()⁡ (Создают вложенные группы в консоли, удобно для структурирования логов)
		// console.indent()
		// console.nest()
		// console.block()
	}
	// ⚠️ ⁡⁣⁣⁢2. Что делает console.table()?⁡
	{
		// ✅ ⁡⁢⁢⁢Выводит данные массива или объекта в виде табличного представления⁡ (Удобно для отладки коллекций)
		// Экспортирует данные в CSV
		// Создаёт HTML-таблицу в DOM
		// Логирует только числовые значения
	}
	// ⚠️ ⁡⁣⁣⁢3. Для чего используется console.time() / console.timeEnd()?⁡
	{
		// ✅ ⁡⁢⁢⁢Замер времени выполнения участка кода в миллисекундах⁡ (Автоматически выводит разницу между вызовами)
		// Установка таймаута для асинхронных операций
		// Профилирование памяти
		// Отслеживание частоты вызовов функции
	}
	// ⚠️ ⁡⁣⁣⁢4. Какой метод позволяет сделать «снимок» стека вызовов без ошибки?⁡
	{
		// ✅ ⁡⁢⁢⁢console.trace() ⁡(Выводит стек вызовов в консоль, полезно для отладки потока выполнения)
		// console.stack()
		// console.callstack()
		// console.debug()
	}
	// ⚠️ ⁡⁣⁣⁢5. Что возвращает Object.entries(obj)?⁡
	{
		// ✅ ⁡⁢⁢⁢Массив пар [ключ, значение] собственных перечисляемых свойств объекта⁡ (Удобно для итерации по объектам)
		// Массив только ключей
		// Массив только значений
		// Итератор свойств прототипа
	}
	// ⚠️ ⁡⁣⁣⁢6. Какой метод создаёт «поверхностную» копию объекта?⁡
	{
		// ✅ ⁡⁢⁢⁢{ ...obj } или Object.assign({}, obj)⁡ (Копирует только собственные свойства, вложенные объекты копируются по ссылке)
		// JSON.parse(JSON.stringify(obj))
		// Object.clone(obj)
		// obj.duplicate()
	}
	// ⚠️ ⁡⁣⁣⁢7. В чём разница между == и ===?⁡
	{
		// ✅ ⁡⁢⁢⁢=== сравнивает значения и типы без приведения типов⁡ (== выполняет coercion, что может привести к неожиданным результатам)
		// Разницы нет, это алиасы
		// == быстрее работает
		// === работает только с объектами
	}
	// ⚠️ ⁡⁣⁣⁢8. Что делает оператор ?. (optional chaining)?⁡
	{
		// ✅ ⁡⁢⁢⁢Позволяет безопасно обращаться к вложенным свойствам, возвращая undefined при null/undefined⁡ (Избегает ошибок «Cannot read property of undefined»)
		// Автоматически создаёт недостающие свойства
		// Приводит значение к пустому объекту
		// Выбрасывает ошибку при отсутствии свойства
	}
	// ⚠️ ⁡⁣⁣⁢9. Для чего нужен ?? (nullish coalescing)?⁡
	{
		// ✅ ⁡⁢⁢⁢Возвращает правый операнд, если левый === null или === undefined⁡ (В отличие от ||, не реагирует на 0, '', false)
		// То же, что оператор ||
		// Проверяет на пустую строку
		// Всегда возвращает левый операнд
	}
	// ⚠️ ⁡⁣⁣⁢10. Что делает деструктуризация с остаточными параметрами: { a, ...rest } = obj?⁡
	{
		// ✅ ⁡⁢⁢⁢Выделяет свойство a, остальные собирает в объект rest⁡ (Удобно для разделения «известных» и «прочих» свойств)
		// Удаляет свойство a из оригинала
		// Создаёт ссылку на оригинальный объект
		// Работает только с массивами
	}
	// ⚠️ ⁡⁣⁣⁢11. Какой метод массива не мутирует исходный массив?⁡
	{
		// ✅ ⁡⁢⁢⁢map()⁡ (Возвращает новый массив, оригинал остаётся неизменным — принцип иммутабельности)
		// push()
		// splice()
		// sort()
	}
	// ⚠️ ⁡⁣⁣⁢12. Что вернёт Array.from('abc')?⁡
	{
		// ✅ ⁡⁢⁢⁢['a', 'b', 'c']⁡ (Создаёт массив из итерируемого объекта, строка — итерируемая)
		// ['abc']
		// {0: 'a', 1: 'b', 2: 'c'}
		// Ошибку типа
	}
	// ⚠️ ⁡⁣⁣⁢13. Для чего используется Symbol?⁡
	{
		// ✅ ⁡⁢⁢⁢Создание уникальных идентификаторов, исключающих коллизии ключей⁡ (Полезно для «приватных» свойств и метаданных)
		// Шифрование строк
		// Ускорение доступа к свойствам
		// Замена string в качестве ключей для производительности
	}
	// ⚠️ ⁡⁣⁣⁢14. Что делает метод Object.freeze()?⁡
	{
		// ✅ ⁡⁢⁢⁢Делает объект неизменяемым: нельзя добавить, удалить или изменить свойства⁡ (Поверхностное «замораживание», вложенные объекты не защищены)
		// Удаляет все свойства объекта
		// Делает свойства объекта неперечисляемыми
		// Блокирует прототип объекта
	}
	// ⚠️ ⁡⁣⁣⁢15. Как проверить, что значение — массив?⁡
	{
		// ✅ ⁡⁢⁢⁢Array.isArray(value)⁡ (Надёжный способ, работает даже для массивов из других контекстов/фреймов)
		// typeof value === 'array'
		// value instanceof Array
		// value.constructor === Array
	}
	// ⚠️ ⁡⁣⁣⁢16. Что вернёт typeof null?⁡
	{
		// ✅ ⁡⁢⁢⁢'object'⁡ (Историческая ошибка в специации JS, исправлять нельзя из-за backward compatibility)
		// 'null'
		// 'undefined'
		// 'void'
	}
	// ⚠️ ⁡⁣⁣⁢17. Какой метод позволяет создать объект с указанным прототипом?⁡
	{
		// ✅ ⁡⁢⁢⁢Object.create(proto)⁡ (Позволяет явно задать прототип, основа для классического наследования)
		// new Object(proto)
		// Object.assign(proto, {})
		// Object.inherit(proto)
	}
	// ⚠️ ⁡⁣⁣⁢18. Что делает метод .bind()?⁡
	{
		// ✅ ⁡⁢⁢⁢Создаёт новую функцию с привязанным контекстом this и/или аргументами⁡ (Полезно для передачи методов как колбэков без потери контекста)
		// Немедленно вызывает функцию с новым контекстом
		// Меняет прототип функции
		// Кэширует результат выполнения
	}
	// ⚠️ ⁡⁣⁣⁢19. В чём разница между debounce и throttle?⁡
	{
		// ✅ ⁡⁢⁢⁢debounce ждёт паузы между вызовами, throttle ограничивает частоту вызовов⁡ (debounce — для поиска, throttle — для скролла)
		// Это синонимы
		// throttle ждёт паузы, debounce ограничивает частоту
		// Оба немедленно выполняют функцию при первом вызове
	}
	// ⚠️ ⁡⁣⁣⁢20. Как создать «ленивую» итерацию?⁡
	{
		// ✅ ⁡⁢⁢⁢Использовать генераторы (function*) или итераторы ⁡(Значения вычисляются по требованию, экономия памяти)
		// Массив с 10000 элементами
		// setTimeout внутри map
		// Promise.all с массивом
	}
	// ⚠️ ⁡⁣⁣⁢21. Что делает метод .flat()?⁡
	{
		// ✅ ⁡⁢⁢⁢«Расплющивает» вложенные массивы на указанную глубину⁡ (По умолчанию глубина 1, можно передать Infinity)
		// Удаляет дубликаты из массива
		// Преобразует массив в строку
		// Сортирует вложенные массивы
	}
	// ⚠️ ⁡⁣⁣⁢22. Для чего нужен метод .reduce()?⁡
	{
		// ✅ ⁡⁢⁢⁢Агрегация массива в одно значение (сумма, объект-аккумулятор, группировка)⁡ (Требует начального значения для надёжности)
		// Фильтрация элементов
		// Создание копии массива
		// Сортировка по критерию
	}
	// ⚠️ ⁡⁣⁣⁢23. Что вернёт Promise.allSettled()?⁡
	{
		// ✅ ⁡⁢⁢⁢Массив объектов {status, value/reason} для всех промисов, независимо от исхода⁡ (Удобно, когда нужно обработать все результаты)
		// Первый выполнившийся промис
		// Ошибку при первом отклонённом промисе
		// Только успешные значения
	}
	// ⚠️ ⁡⁣⁣⁢24. Как отменить fetch-запрос?⁡
	{
		// ✅ ⁡⁢⁢⁢С помощью AbortController и сигнала abort⁡ (Современный стандартный способ отмены асинхронных операций)
		// fetch.abort()
		// Promise.reject()
		// setTimeout с очисткой
	}
	// ⚠️ ⁡⁣⁣⁢25. Что делает метод .finally() у промиса?⁡
	{
		// ✅ ⁡⁢⁢⁢Выполняется после resolve/reject, не получает аргументов⁡ (Удобен для очистки: скрытия лоадеров, закрытия соединений)
		// Обрабатывает только ошибки
		// Заменяет .then() и .catch()
		// Возвращает новое значение для цепочки
	}
	// ⚠️ ⁡⁣⁣⁢26. Какой тип данных не является примитивом в JS?⁡
	{
		// ✅ ⁡⁢⁢⁢Object⁡ (Все остальные: string, number, boolean, null, undefined, symbol, bigint — примитивы)
		// string
		// bigint
		// symbol
	}
	// ⚠️ ⁡⁣⁣⁢27. Что такое «подъём» (hoisting)?⁡
	{
		// ✅ ⁡⁢⁢⁢Объявления var и function «поднимаются» в начало области видимости⁡ (Но инициализация var — нет, let/const не поднимаются)
		// Автоматическая оптимизация кода движком
		// Перемещение кода в Web Worker
		// Кэширование функций в памяти
	}
	// ⚠️ ⁡⁣⁣⁢28. Что вернёт выражение: [] + {}?⁡
	{
		// ✅ ⁡⁢⁢⁢'[object Object]'⁡ (Массив приводится к строке '', объект — к '[object Object]', конкатенация строк)
		// {}
		// []
		// Ошибку приведения типов
	}
	// ⚠️ ⁡⁣⁣⁢29. Как проверить, что объект — пустой?⁡
	{
		// ✅ ⁡⁢⁢⁢Object.keys(obj).length === 0⁡ (Проверяет только собственные перечисляемые свойства, для полной проверки нужны дополнительные методы)
		// obj === {}
		// !obj
		// obj.size === 0
	}
	// ⚠️ ⁡⁣⁣⁢30. Что делает метод .toLocaleString()?⁡
	{
		// ✅ ⁡⁢⁢⁢Форматирует число/дату с учётом локали⁡ (Например, разделители тысяч, формат даты)
		// Переводит строку на другой язык
		// Кодирует строку в UTF-8
		// Возвращает временную зону пользователя
	}
	// ⚠️ ⁡⁣⁣⁢31. Для чего нужен метод .padStart() / .padEnd()?⁡
	{
		// ✅ ⁡⁢⁢⁢Дополняет строку до нужной длины указанным символом⁡ (Удобно для форматирования чисел, дат, кодов)
		// Обрезает строку
		// Меняет регистр символов
		// Удаляет пробелы по краям
	}
	// ⚠️ ⁡⁣⁣⁢32. Что делает оператор ??= ?⁡
	{
		// ✅ ⁡⁢⁢⁢Присваивает правое значение, если левое === null/undefined⁡ (Логическое продолжение ?? для присваивания)
		// То же, что ||=
		// Присваивает только если левое — ложное
		// Всегда перезаписывает левое значение
	}
	// ⚠️ ⁡⁣⁣⁢33. Как создать «приватное» поле в классе (стандартный способ)?⁡
	{
		// ✅ ⁡⁢⁢⁢Использовать #name⁡ (Синтаксис приватных полей, доступен только внутри класса)
		// _name (конвенция)
		// private name (как в TypeScript)
		// Object.defineProperty с writable: false
	}
	// ⚠️ ⁡⁣⁣⁢34. Что вернёт выражение: 0.1 + 0.2 === 0.3?⁡
	{
		// ✅ ⁡⁢⁢⁢false⁡ (Ошибка представления чисел с плавающей точкой в формате IEEE 754, использовать toFixed() или epsilon-сравнение)
		// true
		// null
		// Ошибку вычислений
	}
	// ⚠️ ⁡⁣⁣⁢35. Для чего нужен метод .at()?⁡
	{
		// ✅ ⁡⁢⁢⁢Позволяет обращаться к элементам массива/строки по отрицательным индексам⁡ (arr.at(-1) — последний элемент)
		// Возвращает индекс элемента
		// Проверяет наличие элемента
		// Сортирует массив
	}
	// ⚠️ ⁡⁣⁣⁢36. Что делает метод .hasOwn()?⁡
	{
		// ✅ ⁡⁢⁢⁢Проверяет наличие собственного (не унаследованного) свойства ⁡(Современная замена Object.prototype.hasOwnProperty)
		// Проверяет наличие свойства в прототипе
		// Возвращает значение свойства
		// Удаляет свойство
	}
	// ⚠️ ⁡⁣⁣⁢37. Какой метод позволяет «глубоко» сравнить объекты?⁡
	{
		// ✅ ⁡⁢⁢⁢JSON.stringify(a) === JSON.stringify(b) (с оговорками) или lodash.isEqual⁡ (Стандартного глубокого сравнения в языке нет)
		// a === b
		// Object.is(a, b)
		// a.equals(b)
	}
	// ⚠️ ⁡⁣⁣⁢38. Что делает метод .structuredClone()?⁡
	{
		// ✅ ⁡⁢⁢⁢Создаёт глубокую копию объекта с поддержкой циклических ссылок и сложных типов⁡ (Современный стандартный способ клонирования)
		// Поверхностную копию
		// Копию только примитивных свойств
		// Сериализует в JSON
	}
	// ⚠️ ⁡⁣⁣⁢39. Для чего нужен метод .replaceAll()?⁡
	{
		// ✅ ⁡⁢⁢⁢Заменяет все вхождения подстроки/регулярки⁡ (В отличие от replace() с строкой, который меняет только первое)
		// Заменяет только первое вхождение
		// Удаляет все пробелы
		// Экранирует спецсимволы
	}
	// ⚠️ ⁡⁣⁣⁢40. Что вернёт выражение: Boolean('0')?⁡
	{
		// ✅ ⁡⁢⁢⁢true⁡ (Любая непустая строка, включая '0', приводится к true; только '' — false)
		// false
		// null
		// Ошибку приведения
	}
	// ⚠️ ⁡⁣⁣⁢41. Как проверить, что значение — конечное число?⁡
	{
		// ✅ ⁡⁢⁢⁢Number.isFinite(value)⁡ (Не приводит аргумент, в отличие от глобального isFinite)
		// typeof value === 'number'
		// !isNaN(value)
		// value < Infinity
	}
	// ⚠️ ⁡⁣⁣⁢42. Что делает метод .findLast()?⁡
	{
		// ✅ ⁡⁢⁢⁢Возвращает последний элемент, удовлетворяющий условию⁡ (Поиск с конца массива, аналог find, но в обратном порядке)
		// Удаляет последний подходящий элемент
		// Возвращает индекс последнего элемента
		// Сортирует массив в обратном порядке
	}
	// ⚠️ ⁡⁣⁣⁢43. Для чего нужен метод .toSorted()?⁡
	{
		// ✅ ⁡⁢⁢⁢Возвращает новый отсортированный массив, не мутируя исходный⁡ (Иммутабельная альтернатива .sort())
		// Сортирует на месте
		// Обратная сортировка
		// Группирует элементы
	}
}
// 🔄 ⁡⁢⁣⁣РАЗДЕЛ 2: EVENT LOOP И DOM (ВОПРОСЫ 44–87)⁡
{
	// ⚠️ ⁡⁣⁣⁢44. Что выведет код?⁡
	{
		console.log("start");
		setTimeout(() => console.log("timeout"), 0);
		Promise.resolve().then(() => console.log("promise"));
        console.log("end");
        {
            // ✅ ⁡⁢⁢⁢start, end, promise, timeout⁡ (Синхронный код → микрозадачи (промисы) → макрозадачи (setTimeout))
            // start, end, timeout, promise
            // start, promise, end, timeout
            // timeout, promise, start, end
        }
	}
	// ⚠️ ⁡⁣⁣⁢45. В каком порядке выполняются микрозадачи?⁡
	{
		// ✅ ⁡⁢⁢⁢В порядке очереди (FIFO), каждая новая добавляется в конец⁡ (Promise.then, queueMicrotask, MutationObserver)
		// В случайном порядке
		// Сначала новые, потом старые
		// Параллельно, без гарантий порядка
	}
	// ⚠️ ⁡⁣⁣⁢46. Что такое «рендеринг» в контексте event loop?⁡
	{
		// ✅ ⁡⁢⁢⁢Шаг между микрозадачами и макрозадачами, когда браузер может обновить UI⁡ (Не гарантируется после каждой задачи)
		// Выполнение всех скриптов на странице
		// Загрузка ресурсов с сервера
		// Обработка событий мыши
	}
	// ⚠️ ⁡⁣⁣⁢47. Когда выполнится requestAnimationFrame?⁡
	{
		// ✅ ⁡⁢⁢⁢Перед следующей перерисовкой экрана (~60fps), в специальной фазе event loop⁡ (Идеально для анимаций)
		// Немедленно, как setTimeout(0)
		// После всех setTimeout
		// Только при изменении DOM
	}
	// ⚠️ ⁡⁣⁣⁢48. Что выведет код?⁡
	{
		setTimeout(() => console.log(1), 0);
		new Promise((res) => {
			console.log(2);
			res();
			console.log(3);
		}).then(() => console.log(4));
        console.log(5);
        {
            // ✅ ⁡⁢⁢⁢2,3,5,4,1⁡ (Синхронно: 2,3,5; микрозадача: 4; макрозадача: 1)
            // 2,3,4,5,1
            // 1,2,3,4,5
            // 5,2,3,1,4
        }
	}
	// ⚠️ ⁡⁣⁣⁢49. Что делает event.preventDefault()?⁡
	{
		// ✅ ⁡⁢⁢⁢Отменяет стандартное поведение события (например, отправка формы, переход по ссылке)⁡ (Не останавливает всплытие!)
		// Останавливает всплытие события
		// Удаляет обработчик события
		// Блокирует все события на элементе
	}
	// ⚠️ ⁡⁣⁣⁢50. В чём разница между event.target и event.currentTarget?⁡
	{
		// ✅ ⁡⁢⁢⁢target — элемент, на котором произошло событие; currentTarget — элемент, на котором висит обработчик (Важно при делегировании)⁡
		// Это одно и то же
		// target всегда равен document
		// currentTarget — вложенный элемент
	}
	// ⚠️ ⁡⁣⁣⁢51. Что такое «делегирование событий»?⁡
	{
		// ✅ ⁡⁢⁢⁢Навешивание обработчика на родителя для обработки событий потомков⁡ (Экономит память, работает для динамических элементов)
		// Копирование обработчиков на дочерние элементы
		// Автоматическое всплытие всех событий
		// Блокировка событий на детях
	}
	// ⚠️ ⁡⁣⁣⁢52. Какой порядок фаз события?⁡
	{
		// ✅ ⁡⁢⁢⁢Capturing → Target → Bubbling⁡ (По умолчанию обработчики вешаются на фазу bubbling, можно указать capturing: true)
		// Bubbling → Target → Capturing
		// Target → Capturing → Bubbling
		// Только Target
	}
	// ⚠️ ⁡⁣⁣⁢53. Что делает event.stopPropagation()?⁡
	{
		// ✅ ⁡⁢⁢⁢Останавливает дальнейшее всплытие/погружение события⁡ (Но не отменяет стандартное поведение — для этого preventDefault)
		// Отменяет действие по умолчанию
		// Удаляет все обработчики
		// Блокирует событие навсегда
	}
	// ⚠️ ⁡⁣⁣⁢54. Как добавить обработчик на фазе capturing?⁡
	{
		// ✅ ⁡⁢⁢⁢element.addEventListener('click', handler, { capture: true })⁡ (Третий аргумент — опции или boolean useCapture)
		// element.oncapture = handler
		// element.addEventListener('click', handler, 'capture')
		// element.bind('click', handler, true)
	}
	// ⚠️ ⁡⁣⁣⁢55. Что выведет код?⁡
	{
		const el = document.getElementById("test");
		el.addEventListener("click", () => console.log(1));
		el.addEventListener("click", () => console.log(2), { once: true });
		el.click();
        el.click();
        {
            // ✅ ⁡⁢⁢⁢1,2,1⁡ (once: true удаляет обработчик после первого вызова)
            // 1,2,1,2
            // 1,1,2,2
            // 2,1,2,1
        }
	}
	// ⚠️ ⁡⁣⁣⁢56. Что такое «passive» обработчик?⁡
	{
		// ✅ ⁡⁢⁢⁢Подсказка браузеру, что preventDefault не будет вызван⁡ (Улучшает производительность скролла, особенно на мобильных)
		// Обработчик, который не получает event
		// Асинхронный обработчик без блокировки UI
		// Обработчик только для фазы capturing
	}
	// ⚠️ ⁡⁣⁣⁢57. Когда сработает MutationObserver?⁡
	{
		// ✅ ⁡⁢⁢⁢Асинхронно, после завершения текущего скрипта, в микрозадаче⁡ (Наблюдает за изменениями DOM: атрибуты, дети, текст)
		// Синхронно при изменении DOM
		// Только после перерисовки
		// По таймеру каждые 100мс
	}
	// ⚠️ ⁡⁣⁣⁢58. Что делает IntersectionObserver?⁡
	{
		// ✅ ⁡⁢⁢⁢Отслеживает видимость элемента в области просмотра⁡ (Идеально для ленивой загрузки, аналитики видимости)
		// Наблюдает за изменением размеров
		// Отслеживает позицию мыши
		// Мониторит загрузку ресурсов
	}
	// ⚠️ ⁡⁣⁣⁢59. Как отложить тяжёлую задачу без блокировки UI?⁡
	{
		// ✅ ⁡⁢⁢⁢Разбить на части и выполнять через setTimeout / requestIdleCallback⁡ (Даёт браузеру время на рендеринг и обработку ввода)
		// Использовать async/await
		// Вынести в Web Worker
		// Оба последних варианта тоже верны, но вопрос про main thread
	}
	// ⚠️ ⁡⁣⁣⁢60. Что делает requestIdleCallback?⁡
	{
		// ✅ ⁡⁢⁢⁢Выполняет колбэк, когда браузер свободен⁡ (Полезно для фоновых задач, но не поддерживается в Safari без флага)
		// То же, что setTimeout(0)
		// Выполняет код до следующей анимации
		// Блокирует UI до завершения
	}
	// ⚠️ ⁡⁣⁣⁢61. Что выведет код?⁡
	{
		console.log(1);
		setTimeout(() => console.log(2), 0);
		queueMicrotask(() => console.log(3));
		Promise.resolve().then(() => console.log(4));
        console.log(5);
        {
            // ✅ ⁡⁢⁢⁢1,5,3,4,2⁡ (Синхронные → микрозадачи в порядке очереди → макрозадачи)
            // 1,5,2,3,4
            // 1,3,4,5,2
            // 3,4,1,5,2
        }
	}
	// ⚠️ ⁡⁣⁣⁢62. Что такое «long task» в Performance API?⁡
	{
		// ✅ ⁡⁢⁢⁢Задача, выполняющаяся >50мс, блокирующая основной поток⁡ (Можно отслеживать через PerformanceObserver)
		// Любая асинхронная операция
		// Запрос к серверу дольше 1с
		// Анимация с низким FPS
	}
	// ⚠️ ⁡⁣⁣⁢63. Как измерить время перерисовки элемента?⁡
	{
		// ✅ ⁡⁢⁢⁢Performance.mark/measure + requestAnimationFrame⁡ (Или использовать PerformanceObserver с entryType 'paint')
		// console.time вокруг DOM-изменений
		// Только через DevTools
		// Невозможно измерить программно
	}
	// ⚠️ ⁡⁣⁣⁢64. Что делает метод .closest()?⁡
	{
		// ✅ ⁡⁢⁢⁢Ищет ближайшего предка (включая себя), соответствующего селектору⁡ (Удобно при делегировании для определения цели)
		// Возвращает всех потомков
		// Проверяет только прямых родителей
		// Работает только с классами
	}
	// ⚠️ ⁡⁣⁣⁢65. В чём разница между innerHTML и textContent?⁡
	{
		// ✅ ⁡⁢⁢⁢innerHTML парсит HTML, textContent вставляет как текст⁡ (textContent безопаснее от XSS, быстрее)
		// Разницы нет
		// innerHTML быстрее
		// textContent поддерживает HTML-теги
	}
	// ⚠️ ⁡⁣⁣⁢66. Что такое «reflow» (layout)?⁡
	{
		// ✅ ⁡⁢⁢⁢Пересчёт геометрии элементов при изменении размеров/позиций⁡ (Дорогая операция, лучше батчить изменения)
		// Изменение стилей без перерисовки
		// Загрузка изображений
		// Обработка событий
	}
	// ⚠️ ⁡⁣⁣⁢67. Что такое «repaint»?⁡
	{
		// ✅ ⁡⁢⁢⁢Перерисовка пикселей без изменения геометрии⁡ (Дешевле reflow, но тоже влияет на производительность)
		// Полная перезагрузка страницы
		// Изменение структуры DOM
		// Загрузка CSS
	}
	// ⚠️ ⁡⁣⁣⁢68. Как минимизировать reflow?⁡
	{
		// ✅ ⁡⁢⁢⁢Батчить чтения/записи стилей, использовать classList, documentFragment⁡ (Чередование read/write вызывает синхронный layout)
		// Использовать !important в CSS
		// Всегда применять inline-стили
		// Отключать анимации
	}
	// ⚠️ ⁡⁣⁣⁢69. Что делает document.createDocumentFragment()?⁡
	{
		// ✅ ⁡⁢⁢⁢Создаёт «лёгкий» контейнер для пакетной вставки в DOM⁡ (Не является частью дерева, не вызывает reflow при манипуляциях)
		// Клонирование узла
		// Временное хранилище для событий
		// Асинхронная загрузка скриптов
	}
	// ⚠️ ⁡⁣⁣⁢70. Что такое Shadow DOM?⁡
	{
		// ✅ ⁡⁢⁢⁢Инкапсулированное поддерево DOM со своими стилями и скриптами⁡ (Основа веб-компонентов, изоляция от глобальных стилей)
		// Скрытый iframe
		// Приватная область памяти
		// Альтернатива Virtual DOM
	}
	// ⚠️ ⁡⁣⁣⁢71. Как получить доступ к элементам внутри shadow root?⁡
	{
		// ✅ ⁡⁢⁢⁢element.shadowRoot.querySelector(...)⁡ (Если режим mode: 'open'; 'closed' скрывает доступ)
		// document.querySelector внутри компонента
		// Через globalScope
		// Невозможно, только через события
	}
	// ⚠️ ⁡⁣⁣⁢72. Что делает метод .matches()?⁡
	{
		// ✅ ⁡⁢⁢⁢Проверяет, соответствует ли элемент селектору⁡ (Аналог :is() в CSS, но в JS)
		// Находит все подходящие элементы
		// Применяет стили
		// Возвращает псевдоэлемент
	}
	// ⚠️ ⁡⁣⁣⁢73. Что выведет код?⁡
	{
		const el = document.createElement("div");
		document.body.appendChild(el);
		const obs = new MutationObserver(() => console.log("mutated"));
		obs.observe(el, { childList: true });
		el.innerHTML = "<p>test</p>";
        console.log("done");
        {
            // ✅ ⁡⁢⁢⁢done, mutated⁡ (MutationObserver срабатывает асинхронно, после выполнения скрипта)
            // mutated, done
            // Только done
            // Только mutated
        }
	}
	// ⚠️ ⁡⁣⁣⁢74. Когда сработает обработчик события, добавленный внутри setTimeout?⁡
	{
		// ✅ ⁡⁢⁢⁢После выполнения текущих микрозадач и рендеринга (если он запланирован) (setTimeout — макрозадача, выполняется после микрозадач)⁡
		// Немедленно
		// До микрозадач
		// Только после клика пользователя
	}
	// ⚠️ ⁡⁣⁣⁢75. Что такое «microtask checkpoint»?⁡
	{
		// ✅ ⁡⁢⁢⁢Момент после выполнения задачи, когда выполняются все накопленные микрозадачи⁡ (Гарантирует порядок: синхронный код → микро → макро)
		// Точка сохранения состояния приложения
		// Момент инициализации модулей
		// Синхронизация между воркерами
	}
	// ⚠️ ⁡⁣⁣⁢76. Что выведет код?⁡
	{
		Promise.resolve().then(() => {
			console.log(1);
			queueMicrotask(() => console.log(2));
		});
		queueMicrotask(() => console.log(3));
        console.log(4);
        {
            // ✅ ⁡⁢⁢⁢4,1,3,2⁡ 
            // 4,1,2,3
            // 1,2,3,4
            // 3,4,1,2
        }
	}
	// ⚠️ ⁡⁣⁣⁢77. Как отменить requestAnimationFrame?⁡
	{
		// ✅ ⁡⁢⁢⁢cancelAnimationFrame(id)⁡ (Возвращает ID при вызове rAF, передаётся в cancel)
		// rAF.abort()
		// clearTimeout
		// Невозможно отменить
	}
	// ⚠️ ⁡⁣⁣⁢78. Что делает метод .getBoundingClientRect()?⁡
	{
		// ✅ ⁡⁢⁢⁢Возвращает размеры и позицию элемента относительно вьюпорта⁡ (Учитывает трансформации, прокрутку)
		// Возвращает только ширину/высоту
		// Возвращает позицию относительно document
		// Возвращает стили computed
	}
	// ⚠️ ⁡⁣⁣⁢79. Почему чтение offsetTop после записи style.width вызывает reflow?⁡
	{
		// ✅ ⁡⁢⁢⁢Чтение геометрии после записи стилей форсирует синхронный пересчёт layout⁡ (Форсированный reflow — частая причина лагов)
		// Это баг браузера
		// Только в режиме strict mode
		// Не вызывает, это миф
	}
	// ⚠️ ⁡⁣⁣⁢80. Что такое «composite-only» анимация?⁡
	{
		// ✅ ⁡⁢⁢⁢Анимация свойств transform/opacity, не требующая reflow/repaint⁡ (Выполняется на уровне композитинга, максимально быстро)
		// Анимация через CSS-классы
		// Анимация только в Safari
		// Анимация с использованием Web Animations API
	}
	// ⚠️ ⁡⁣⁣⁢81. Как выполнить код после полной загрузки страницы?⁡
	{
		// ✅ ⁡⁢⁢⁢window.addEventListener('load', ...)⁡ (Ждёт загрузки всех ресурсов: изображений, стилей; DOMContentLoaded — только дерево)
		// document.ready()
		// setTimeout(() => {}, 1000)
		// Promise.resolve().then(...)
	}
	// ⚠️ ⁡⁣⁣⁢82. Что делает метод .scrollIntoView()?⁡
	{
		// ✅ ⁡⁢⁢⁢Прокручивает элемент в видимую область⁡ (Поддерживает опции: behavior: 'smooth', block: 'center')
		// Возвращает позицию прокрутки
		// Отслеживает скролл
		// Блокирует прокрутку страницы
	}
	// ⚠️ ⁡⁣⁣⁢83. Как отследить изменение размера окна?⁡
	{
		// ✅ ⁡⁢⁢⁢window.addEventListener('resize', handler)⁡ (Лучше debounce-ить обработчик для производительности)
		// Только через CSS media queries
		// MutationObserver за window
		// requestAnimationFrame в цикле
	}
	// ⚠️ ⁡⁣⁣⁢84. Что такое «event loop starvation»?⁡
	{
		// ✅ ⁡⁢⁢⁢Блокировка выполнения других задач из-за долгой синхронной операции или рекурсивных микрозадач⁡ (Приложение «зависает», UI не отвечает)
		// Переполнение стека вызовов
		// Ошибка в Promise
		// Конфликт событий
	}
	// ⚠️ ⁡⁣⁣⁢85. Что выведет код?⁡
	{
		async function f() {
			console.log(1);
			await console.log(2);
			console.log(3);
		}
		f();
        console.log(4);
        {
            // ✅ ⁡⁢⁢⁢1,2,4,3⁡ (await перед небольшим выражением: 1,2 синхронно, затем микрозадача с 3, после синхронного 4)
            // 1,2,3,4
            // 4,1,2,3
            // 1,4,2,3
        }
	}
	// ⚠️ ⁡⁣⁣⁢86. Как работает async/await внутри event loop?⁡
	{
		// ✅ ⁡⁢⁢⁢await «разрывает» функцию на части: до await — синхронно, после — в микрозадаче (Как цепочка .then())⁡
		// Весь async-код выполняется синхронно
		// await переводит код в макрозадачу
		// async функции игнорируют event loop
	}
	// ⚠️ ⁡⁣⁣⁢87. Что делает PerformanceObserver?⁡
	{
		// ✅ ⁡⁢⁢⁢Позволяет асинхронно получать метрики производительности (longtask, paint, resource и др.) (Современная замена устаревшим событиям)⁡
		// Логирует все события DOM
		// Отслеживает только ошибки
		// Замеряет только время загрузки
	}
}
// ⁡⁢⁣⁣🌐 РАЗДЕЛ 3: API И БИБЛИОТЕКИ (ВОПРОСЫ 88–130)⁡
{
	// ⚠️ ⁡⁣⁣⁢88. Как отменить fetch-запрос?⁡
	{
		// ✅ ⁡⁢⁢⁢С помощью AbortController: controller.abort()⁡ (Передаётся signal в fetch, вызывает отмену с ошибкой типа AbortError)
		// fetch().cancel()
		// Promise.reject()
		// setTimeout с флагом
	}
	// ⚠️ ⁡⁣⁣⁢89. Что выведет код?⁡
	{
		console.log(1);
		setTimeout(() => console.log(2), 0);
		Promise.resolve().then(() => setTimeout(() => console.log(3), 0));
		Promise.resolve().then(() => console.log(4));
        console.log(5);
        {
            // ✅ ⁡⁢⁢⁢1,5,4,2,3⁡ (После синхронного 1 и 5 выполняются микрозадачи: 4, а также микрозадача, которая ставит макротаску с 3. Затем выполняются макротаски: первая 2, вторая 3.)
            // 1,5,2,4,3
            // 1,5,4,3,2
        }
	}
	// ⚠️ ⁡⁣⁣⁢90. Как работает оператор ?? (nullish coalescing)?⁡
	{
		// ✅ ⁡⁢⁢⁢Оператор ?? возвращает правый операнд, если левый равен null или undefined, иначе левый.⁡ (Отличается от || тем, что не считает falsy-значения (0, '', false) за отсутствие значения.)
		// Возвращает левый операнд, если он истинный, иначе правый.
		// То же, что &&.
		// Возвращает правый операнд всегда.
	}
	// ⚠️ ⁡⁣⁣⁢91. Что возвращает fetch() по умолчанию?⁡
	{
		// ✅ ⁡⁢⁢⁢Promise, который resolve-ится с Response (даже при 404/500)⁡ (Нужно проверять response.ok или status)
		// Автоматически выбрасывает ошибку при 4xx/5xx
		// Возвращает сразу данные в JSON
		// Возвращает XMLHttpRequest
	}
	// ⚠️ ⁡⁣⁣⁢92. Как прочитать JSON из ответа fetch?⁡
	{
		// ✅ ⁡⁢⁢⁢response.json()⁡ (Возвращает промис с распарсенными данными; тело можно прочитать только один раз)
		// JSON.parse(response)
		// response.data
		// response.body.json
	}
	// ⚠️ ⁡⁣⁣⁢93. Что такое CORS?⁡
	{
		// ✅ ⁡⁢⁢⁢Механизм безопасности браузера, ограничивающий кросс-доменные запросы⁡ (Сервер должен отправить заголовки Access-Control-Allow-*)
		// Протокол шифрования
		// Метод сжатия данных
		// Система кэширования
	}
	// ⚠️ ⁡⁣⁣⁢94. Как отправить POST-запрос с телом через fetch?⁡
	{
		// ✅ ⁡⁢⁢⁢fetch(url, { method: 'POST', headers: {...}, body: JSON.stringify(data) }) (Не забыть заголовок Content-Type: application/json)⁡
		// fetch.post(url, data)
		// fetch(url, { data })
		// new Request(url, data).send()
	}
	// ⚠️ ⁡⁣⁣⁢95. Что делает Headers API?⁡
	{
		// ✅ ⁡⁢⁢⁢Позволяет работать с заголовками запроса/ответа как с итерируемым объектом (Методы: get, set, append, has, delete)⁡
		// Автоматически добавляет заголовки
		// Шифрует заголовки
		// Кэширует ответы
	}
	// ⚠️ ⁡⁣⁣⁢96. Как отправить форму без перезагрузки страницы?⁡
	{
		// ✅ ⁡⁢⁢⁢new FormData(form) + fetch с методом POST (FormData автоматически устанавливает multipart/form-data)⁡
		// form.ajax()
		// XMLHTTPRequest обязательно
		// Только через jQuery
	}
	// ⚠️ ⁡⁣⁣⁢97. Что такое WebSocket?⁡
	{
		// ✅ ⁡⁢⁢⁢Двусторонний канал связи поверх TCP для реального времени⁡ (После handshake по HTTP переключается на ws://)
		// Улучшенная версия long-polling
		// Протокол только для отправки данных
		// Альтернатива HTTP/2
	}
	// ⚠️ ⁡⁣⁣⁢98. Как обработать ошибку в WebSocket?⁡
	{
		// ✅ ⁡⁢⁢⁢ws.onerror + ws.onclose (onerror не всегда содержит детали, проверять код закрытия в onclose)⁡
		// try/catch вокруг new WebSocket
		// Promise.reject автоматически
		// Только через addEventListener('error')
	}
	// ⚠️ ⁡⁣⁣⁢99. Что делает navigator.sendBeacon()?⁡
	{
		// ✅ ⁡⁢⁢⁢Отправляет данные асинхронно даже при выгрузке страницы (Идеально для аналитики, не блокирует unload)⁡
		// То же, что fetch с keepalive
		// Отправляет только в Safari
		// Синхронный запрос перед закрытием
	}
	// ⚠️ ⁡⁣⁣⁢100. Как хранить данные на клиенте с лимитом ~5-10МБ?⁡
	{
		// ✅ ⁡⁢⁢⁢localStorage (Синхронный, только строки, лимит зависит от браузера; для больших данных — IndexedDB)⁡
		// sessionStorage
		// Cookies
		// Все перечисленные, но с разными лимитами
	}
	// ⚠️ ⁡⁣⁣⁢101. В чём разница между localStorage и sessionStorage?⁡
	{
		// ✅ ⁡⁢⁢⁢sessionStorage очищается при закрытии вкладки, localStorage — персистентный (Оба синхронны и хранят только строки)⁡
		// Разницы нет
		// localStorage быстрее
		// sessionStorage поддерживает объекты
	}
	// ⚠️ ⁡⁣⁣⁢102. Как хранить сложные объекты в localStorage?⁡
	{
		// ✅ ⁡⁢⁢⁢JSON.stringify / JSON.parse (localStorage работает только со строками, вложенные структуры нужно сериализовать)⁡
		// Автоматически сериализуется
		// Использовать Object.assign
		// Только примитивы поддерживаются
	}
	// ⚠️ ⁡⁣⁣⁢103. Что такое IndexedDB?⁡
	{
		// ✅ ⁡⁢⁢⁢Асинхронная база данных в браузере для хранения больших структурированных данных (Поддерживает индексы, транзакции) ⁡
		// Синхронная замена localStorage
		// Только для кэширования изображений
		// Работает только в Chrome
	}
	// ⚠️ ⁡⁣⁣⁢104. Как добавить страницу в историю без перезагрузки?⁡
	{
		// ✅ ⁡⁢⁢⁢history.pushState(state, title, url)⁡ (Меняет URL и историю, не вызывает перезагрузку; нужно обрабатывать popstate)
		// window.location = url
		// location.replace(url)
		// history.navigate(url)
	}
	// ⚠️ ⁡⁣⁣⁢105. Как отследить навигацию через pushState?⁡
	{
		// ✅ ⁡⁢⁢⁢window.addEventListener('popstate', handler) (Срабатывает при навигации вперёд/назад; pushState сам по себе не триггерит)⁡
		// Слушать change события на location
		// MutationObserver за URL
		// Автоматически через router
	}
	// ⚠️ ⁡⁣⁣⁢106. Что делает Service Worker?⁡
	{
		// ✅ ⁡⁢⁢⁢Прокси-скрипт между приложением и сетью для офлайн-работы, кэширования, пушей (Работает в отдельном потоке, не имеет доступа к DOM)⁡
		// Ускоряет выполнение основного кода
		// Заменяет localStorage
		// Анимации в фоне
	}
	// ⚠️ ⁡⁣⁣⁢107. Как зарегистрировать Service Worker?⁡
	{
		// ✅ ⁡⁢⁢⁢navigator.serviceWorker.register('/sw.js')⁡ (Возвращает промис; регистрация работает только на HTTPS/localhost)
		// new ServiceWorker('/sw.js')
		// worker.register()
		// Через <script type="worker">
	}
	// ⚠️ ⁡⁣⁣⁢108. Что такое Cache API?⁡
	{
		// ✅ ⁡⁢⁢⁢Программное управление кэшем для ресурсов⁡ (Работает вместе с Service Worker, хранит Request/Response пары)
		// Автоматический кэш браузера
		// Только для изображений
		// Альтернатива localStorage
	}
	// ⚠️ ⁡⁣⁣⁢109. Как получить геолокацию пользователя?⁡
	{
		// ✅ ⁡⁢⁢⁢navigator.geolocation.getCurrentPosition(success, error)⁡ (Асинхронный запрос с разрешением пользователя, возвращает координаты)
		// new Geolocation()
		// fetch('/api/geo')
		// Только через IP
	}
	// ⚠️ ⁡⁣⁣⁢110. Что делает Clipboard API?⁡
	{
		// ✅ ⁡⁢⁢⁢Позволяет читать/записывать в буфер обмена⁡ (navigator.clipboard.writeText / readText; требует HTTPS и прав доступа)
		// Автоматически копирует выделенный текст
		// Работает только с изображениями
		// Заменяет document.execCommand
	}
	// ⚠️ ⁡⁣⁣⁢111. Как отформатировать дату с учётом локали?⁡
	{
		// ✅ ⁡⁢⁢⁢new Intl.DateTimeFormat(locale, options).format(date)⁡ (Часть Internationalization API, гибкое форматирование)
		// date.toLocaleString() (работает, но менее гибко)
		// Только через сторонние библиотеки
		// JSON.stringify(date)
	}
	// ⚠️ ⁡⁣⁣⁢112. Что делает Intl.NumberFormat?⁡
	{
		// ✅ ⁡⁢⁢⁢Форматирует числа с разделителями, валютой, знаками⁡ (Например, 1000 → '1 000,00 ₽' для ru-RU)
		// Конвертирует валюты
		// Округляет числа
		// Переводит в прописью
	}
	// ⚠️ ⁡⁣⁣⁢113. Как проверить поддержку фичи в браузере?⁡
	{
		// ✅ ⁡⁢⁢⁢'feature' in window или проверка существования объекта/метода⁡ (Feature detection надёжнее, чем user-agent sniffing)
		// По версии браузера
		// Через try/catch
		// Только через Modernizr
	}
	// ⚠️ ⁡⁣⁣⁢114. Что такое «polyfill»?⁡
	{
		// ✅ ⁡⁢⁢⁢Код, добавляющий поддержку новой фичи в старых браузерах⁡ (Обычно проверяет наличие и определяет метод, если нет)
		// Плагин для DevTools
		// Инструмент минификации
		// Библиотека для TypeScript
	}
	// ⚠️ ⁡⁣⁣⁢115. Как работает Babel?⁡
	{
		// ✅ ⁡⁢⁢⁢Транспиляция современного JS в совместимый код + полифиллы через core-js⁡ (Не «компилирует», а преобразует синтаксис)
		// Выполняет код в песочнице
		// Сжимает и минифицирует
		// Заменяет TypeScript
	}
	// ⚠️ ⁡⁣⁣⁢116. Что делает tree-shaking в сборщиках?⁡
	{
		// ✅ ⁡⁢⁢⁢Удаляет неиспользуемый код из финального бандла⁡ (Работает только с ES6-импортами/экспортами, требует sideEffects: false)
		// Сжимает изображения
		// Разделяет код на чанки
		// Кэширует модули
	}
	// ⚠️ ⁡⁣⁣⁢117. Как импортировать модуль динамически?⁡
	{
		// ✅ ⁡⁢⁢⁢import('./module.js').then(m => ...) или await import(...)⁡ (Возвращает промис с модулем, позволяет код-сплиттинг)
		// require() в браузере
		// <script src="...">
		// eval('import...')
	}
	// ⚠️ ⁡⁣⁣⁢118. Что такое «code splitting»?⁡
	{
		// ✅ ⁡⁢⁢⁢Разделение кода на чанки, загружаемые по требованию⁡ (Уменьшает начальный размер бандла, улучшает TTI)
		// Удаление мёртвого кода
		// Сжатие ассетов
		// Кэширование в Service Worker
	}
	// ⚠️ ⁡⁣⁣⁢119. Как обработать глобальную ошибку в браузере?⁡
	{
		// ✅ ⁡⁢⁢⁢window.addEventListener('error', handler, true) + window.onunhandledrejection⁡ (Перехват синхронных ошибок и отклонённых промисов)
		// try/catch вокруг всего кода
		// Только через console.error
		// Автоматически отправляется на сервер
	}
	// ⚠️ ⁡⁣⁣⁢120. Что делает Performance API?⁡
	{
		// ✅ ⁡⁢⁢⁢Предоставляет метрики загрузки, навигации, ресурсов⁡ (performance.timing, performance.getEntriesByType)
		// Управляет анимациями
		// Кэширует запросы
		// Отслеживает ошибки
	}
	// ⚠️ ⁡⁣⁣⁢121. Как измерить время загрузки страницы?⁡
	{
		// ✅ ⁡⁢⁢⁢performance.timing.loadEventEnd - performance.timing.navigationStart⁡ (Или более современный PerformanceNavigationTiming)
		// new Date() до/after load
		// Только через DevTools
		// Невозможно точно измерить
	}
	// ⚠️ ⁡⁣⁣⁢122. Что такое «lazy loading» изображений?⁡
	{
		// ✅ ⁡⁢⁢⁢Загрузка изображений только при приближении к вьюпорту (loading="lazy" или IntersectionObserver)⁡
		// Сжатие изображений на лету
		// Предзагрузка всех картинок
		// Кэширование в localStorage
	}
	// ⚠️ ⁡⁣⁣⁢123. Как отправить аналитику при закрытии вкладки?⁡
	{
		// ✅ ⁡⁢⁢⁢navigator.sendBeacon(url, data)⁡ (Гарантирует доставку даже при выгрузке, асинхронно и неблокирующе)
		// fetch с keepalive: true (работает, но менее надёжно)
		// XMLHttpRequest синхронный
		// Только через WebSocket
	}
	// ⚠️ ⁡⁣⁣⁢124. Что делает метод .finally() у Promise?⁡
	{
		// ✅ ⁡⁢⁢⁢Выполняется после resolve/reject, не получает аргументов⁡ (Удобен для скрытия лоадеров, очистки ресурсов)
		// Обрабатывает только ошибки
		// Заменяет then/catch
		// Возвращает новое значение
	}
	// ⚠️ ⁡⁣⁣⁢125. Как создать «реактивный» объект без фреймворков?⁡
	{
		// ✅ ⁡⁢⁢⁢Proxy с обработкой get/set для отслеживания изменений⁡ (Основа реактивности в Vue 3)
		// Object.defineProperty на всех свойствах
		// Только через наблюдателей (Observers)
		// Невозможно без библиотек
	}
	// ⚠️ ⁡⁣⁣⁢126. Что такое «debounce» в контексте API-запросов?⁡
	{
		// ✅ ⁡⁢⁢⁢Отложенный вызов функции после паузы в событиях⁡ (Уменьшает количество запросов при вводе в поиск, скролле)
		// Немедленное выполнение
		// Кэширование ответов
		// Параллельные запросы
	}
	// ⚠️ ⁡⁣⁣⁢127. Как реализовать «retry» для fetch?⁡
	{
		// ✅ ⁡⁢⁢⁢Обернуть в функцию с рекурсией/циклом и задержкой⁡ (Учитывать статус, сетевые ошибки, лимит попыток)
		// fetch имеет встроенный retry
		// Только через библиотеки
		// Невозможно, нужен сервер
	}
	// ⚠️ ⁡⁣⁣⁢128. Что такое «idempotent» HTTP-метод?⁡
	{
		// ✅ ⁡⁢⁢⁢Метод, повторный вызов которого не меняет состояние⁡ (GET, PUT, DELETE — идемпотентны; POST — нет)
		// Метод с авторизацией
		// Метод, который нельзя отменить
		// Метод для кэширования
	}
	// ⚠️ ⁡⁣⁣⁢129. Как защитить токены в клиентском приложении?⁡
	{
		// ✅ ⁡⁢⁢⁢Хранить в httpOnly cookie (недоступно для JS) или в памяти (с риском потери при перезагрузке) (localStorage уязвим для XSS)⁡
		// Только в localStorage
		// В URL-параметрах
		// В заголовках по умолчанию
	}
	// ⚠️ ⁡⁣⁣⁢130. Что делает AbortSignal.timeout()?⁡
	{
		// ✅ ⁡⁢⁢⁢Создаёт сигнал, который автоматически прервётся через указанное время (Удобно для таймаутов fetch без ручного AbortController)⁡
		// Увеличивает таймаут по умолчанию
		// Откладывает выполнение
		// Повторяет запрос при таймауте
	}
}
