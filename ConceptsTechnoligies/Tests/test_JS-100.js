// 🔳 ⁡⁢⁣⁣КАТЕГОРИЯ 1: ЯДРО JAVASCRIPT (ВОПРОСЫ 1-25)⁡
{
	// ⚠️ ⁡⁣⁣⁢Что будет выведено в консоль?⁡
	{
		console.log(0.1 + 0.2 === 0.3);
		// ✅ ⁡⁢⁢⁢false ⁡✔️ Из-за особенностей представления чисел с плавающей точкой в двоичной системе, 0.1 + 0.2 дает 0.30000000000000004.
	}
	// ⚠️ ⁡⁣⁣⁢Какой метод массива возвращает новый массив, изменяя его размер?⁡
	{
		const arr = [1, 2, 3, 4, 5];
		arr.splice(1, 2, "a", "b");
		console.log(arr);
		{
			// slice()
			// ✅ ⁡⁢⁢⁢splice()⁡ ✔️ splice изменяет исходный массив, удаляя/добавляя элементы. Результат: [1, 'a', 'b', 4, 5].
			// map()
			// filter()
		}
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «поднятие» (Hoisting) в JavaScript?⁡
	{
		// ⁡⁢⁢⁢Автоматическое перемещение, только объявлений переменных(undefined) и функций вместе с телом, причём функции имеют приоритет над примитивами, в начало их области видимости во время компиляции.⁡  ✔️
	}
	// ⚠️ ⁡⁣⁣⁢Каким будет результат?⁡
	{
		const obj = { a: 1 }; // Создаётся объект obj со свойством a: 1
		const map = new Map(); // Создаётся пустая коллекция Map
		map.set(obj, "value"); // Метод set() создаёт ключ — ссылка на объект obj; значение — строка 'value'
		obj.a = 999; // Изменяется свойство a объекта obj. Теперь obj выглядит как { a: 999 }
		console.log(map.get(obj)); // Метод get() ищет запись по ссылке на ключ obj, и его значение — строка 'value'.
		{
			// undefined
			// ✅ ⁡⁢⁢⁢'value'⁡ ✔️ Ключом в Map является ссылка на сам объект, а не его содержимое. Изменение свойств объекта не влияет на эту ссылку.
			// { a: 999 }
			// Ошибка.
		}
	}
	// ⚠️ ⁡⁣⁣⁢Что будет выведено?⁡
	{
		async function foo() {
			return Promise.resolve("Done");
		}
		foo().then(console.log);
		{
			// Promise {<pending>}
			// ✅ ⁡⁢⁢⁢'Done'⁡ ✔️ async функция всегда возвращает Promise. Возврат Promise.resolve обернет его, но итоговый then получит разрешенное значение.
			// undefined
			// Ошибка: foo is not a function.
		}
	}
	// ⚠️ ⁡⁣⁣⁢Какой оператор позволяет безопасно обращаться к свойствам вложенных объектов?⁡
	{
		// ?? (Nullish Coalescing) // const age = undefined/null ?? 28; console.log(age); // 28 — оператор возвращает 28, потому что первое значение — undefined/null.
		// ⁡⁢⁢⁢?. (Optional Chaining)⁡ ✔️
		// ! (Non-null assertion) логическое НЕ (инвертирует значение).
		// ~ (Bitwise NOT) побитовое НЕ
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «таск» (macrotask) и «микрозадача» (microtask) в Event Loop?⁡
	{
		// ⁡⁢⁢⁢Таски — это setTimeout, setInterval, setImmediate. Микрозадачи — это Promise.then/catch/finally, queueMicrotask, MutationObserver.⁡ ✔️
		// Это одно и то же, разница только в приоритете.
		// Таски выполняются в основном потоке, микрозадачи — в Web Worker.
		// Микрозадачи — это только queueMicrotask.
	}
	// ⚠️ ⁡⁣⁣⁢Что будет выведено?⁡
	{
		function foo(a = 10, b = a * 2) {
			console.log(a + b);
		}
		foo(undefined, 5);
		{
			// 30
			// ✅ ⁡⁢⁢⁢15⁡ ✔️ Параметр a получает undefined, поэтому используется значение по умолчанию 10. Параметр b явно передан как 5. Результат: 10 + 5 = 15.
			// NaN
			// 20
		}
	}
	// ⚠️ ⁡⁣⁣⁢Какой метод создает «глубокую» (deep) копию объекта?⁡
	{
		// Object.assign({}, obj)
		// {...obj}
		// ✅ ⁡⁢⁢⁢JSON.parse(JSON.stringify(obj))⁡ ✔️ Этот способ создает глубокую копию, но имеет ограничения (не копирует функции, undefined, Symbol, циклические ссылки).
		// Array.from(obj)
	}
	// ⚠️ ⁡⁣⁣⁢Что такое Symbol и для чего он используется?⁡
	{
		const id = Symbol("id");
		const user = {
			name: "Алексей",
			[id]: 123, // скрытое свойство
		};
		// Перебор не увидит Symbol-свойство
		for (let key in user) {
			console.log(key); // name
		}
		console.log(user[id]); // 123 (доступ есть)
		console.log(user.id); // undefined (доступа нет)
		// ⁡⁢⁢⁢Это примитивный тип для создания уникальных идентификаторов, часто используемых как ключи свойств объектов.⁡ ✔️ Symbol — примитивный тип данных в JavaScript, представляющий собой уникальный неизменяемый идентификатор. Создаётся с помощью функции Symbol().
		// Это синоним строкового типа для оптимизации.
		// Это специальная функция для создания итераторов.
		// Это устаревший тип данных.
	}
	// ⚠️ ⁡⁣⁣⁢Что будет выведено?⁡
	{
		const promise1 = Promise.reject("Error 1");
		const promise2 = Promise.resolve("Success 2");
		const promise3 = Promise.reject("Error 3");
		Promise.all([promise1.catch((e) => e), promise2, promise3.catch((e) => e)]).then(
			console.log,
		);
		{
			// ⁡⁢⁢⁢['Error 1', 'Success 2', 'Error 3']⁡ ✔️ Promise.all ожидает все промисы. catch обрабатывает ошибки и возвращает значение, поэтому все промисы разрешаются успешно.
			// 'Error 1' (первая ошибка)
			// Ошибка в консоли, выполнение прервется.
			// ['Success 2']
		}
	}
	// ⚠️ ⁡⁣⁣⁢Какой из этих методов модифицирует исходный массив?⁡
	{
		// slice()
		// concat()
		// ✅ ⁡⁢⁢⁢sort()⁡ ✔️ sort сортирует массив на месте (in-place).
		// map()
	}
	// ⚠️ ⁡⁣⁣⁢Что будет результатом?⁡
	{
		const num = 42;
		console.log(typeof `${num}`);
		{
			// 'number'
			// ✅ ⁡⁢⁢⁢'string'⁡ ✔️ Шаблонные литералы (обратные кавычки) всегда возвращают строку.
			// 'template'
			// 'undefined'
		}
	}
	// ⚠️ ⁡⁣⁣⁢Для чего используется ключевое слово yield в функции?⁡
	{
		// ⁡⁢⁢⁢Для остановки выполнения функции-генератора и возврата значения.⁡ ✔️
		// Для возврата значения из асинхронной функции.
		// Для передачи данных в callback-функцию.
		// Это устаревший синоним return.
	}
	// ⚠️ ⁡⁣⁣⁢Какой метод Promise выполнится, когда любой из переданных промисов завершится (успешно или с ошибкой)?⁡
	{
		Promise.race(); // ✔️
		Promise.any();
		Promise.allSettled();
		Promise.all();
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «чистая функция» (Pure Function)?⁡
	{
		// ⁡⁢⁢⁢Функция, которая не имеет побочных эффектов и всегда возвращает одинаковый результат для одинаковых аргументов.⁡ ✔️
		// Функция, которая не использует внешние переменные.
		// Функция, написанная в функциональном стиле.
		// Функция, которая не вызывает других функций.
	}
	// ⚠️ ⁡⁣⁣⁢Что выведет этот код?⁡
	{
		const arr = [10, 12, 15, 21];
		for (var i = 0; i < arr.length; i++) {
			setTimeout(function () {
				console.log("Index: " + i + ", element: " + arr[i]);
			}, 1000);
		}
		{
			// Index: 0... 1... 2... 3...
			// ✅ ⁡⁢⁢⁢Index: 4, element: undefined (4 раза)⁡ ✔️ var имеет функциональную область видимости. К моменту выполнения setTimeout цикл завершен, i = 4, arr[4] — undefined.
			// Будет ошибка ReferenceError.
			// Index: 3, element: 21 (4 раза).
		}
	}
	// ⚠️ ⁡⁣⁣⁢Какой оператор используется для проверки прототипной цепочки объекта?⁡
	{
		// typeof
		// ✅ ⁡⁢⁢⁢instanceof⁡ ✔️
		// in
		// hasOwnProperty
	}
	// ⚠️ ⁡⁣⁣⁢Что такое IIFE (Immediately Invoked Function Expression, «немедленно вызываемое функциональное выражение»)?⁡
	{
		// ⁡⁢⁢⁢Немедленно вызываемое функциональное выражение, используется для создания изолированной области видимости.⁡ ✔️
		(function () {
			// код функции
		})();
		// Функция, которая вызывается только один раз за время жизни приложения.
		// Асинхронная функция, которая запускается сразу после объявления.
		// Специальный паттерн для оптимизации циклов.
	}
	// ⚠️ ⁡⁣⁣⁢Что будет выведено?⁡
	{
		console.log(1);
		setTimeout(() => console.log(2), 0);
		Promise.resolve().then(() => console.log(3));
		console.log(4);
		{
			// 1, 2, 3, 4
			// ✅ ⁡⁢⁢⁢1, 4, 3, 2⁡ ✔️ Синхронный код (1,4), затем микрозадачи (3), затем макрозадачи (2).
			// 1, 4, 2, 3
			// 4, 1, 3, 2
		}
	}
	// ⚠️ ⁡⁣⁣⁢Как создать объект с прототипом null?⁡
	{
		// ⁡⁢⁢⁢Object.create(null)⁡ ✔️
		// new Object(null)
		// {} с последующим удалением прототипа.
		// const obj = null;
	}
	// ⚠️ ⁡⁣⁣⁢Какой метод добавляет элемент в начало массива?⁡
	{
		// push()
		// ✅ ⁡⁢⁢⁢unshift()⁡ ✔️
		// shift()
		// pop()
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «деструктуризация» (Destructuring)?⁡
	{
		// ⁡⁢⁢⁢Синтаксис, позволяющий извлекать значения из массивов или свойств из объектов в отдельные переменные.⁡ ✔️
		// Процесс удаления неиспользуемого кода.
		// Механизм разбивки больших файлов на модули.
		// Оптимизация работы сборщика мусора.
	}
	// ⚠️ ⁡⁣⁣⁢Что будет результатом?⁡
	{
		const a = {};
		const b = { key: "b" };
		const c = { key: "c" };
		a[b] = 123;
		a[c] = 456;
		console.log(a[b]);
		{
			// 123
			// ✅ ⁡⁢⁢⁢456⁡ ✔️ Ключами объекта могут быть только строки. При попытке использовать объект b как ключ, он преобразуется в строку '[object Object]'. То же происходит с c. Второе присваивание перезаписывает значение.
			// undefined
			// { key: 'b' }
		}
	}
	// ⚠️ ⁡⁣⁣⁢Что такое WeakMap и чем отличается от Map?⁡
	{
		// ⁡⁢⁢⁢WeakMap принимает в качестве ключей только объекты, не препятствует сборке мусора, не итерируем.⁡ ✔️
		// WeakMap медленнее, но поддерживает больше типов ключей.
		// WeakMap автоматически сортирует ключи.
		// Это синоним, разницы нет.
	}
}
// 🔳 ⁡⁢⁣⁣КАТЕГОРИЯ 2: АСИНХРОННОСТЬ И EVENT LOOP (ВОПРОСЫ 26-35)⁡
{
	// ⚠️ ⁡⁣⁣⁢Какой порядок вывода в консоль?⁡
	{
		console.log("A");
		setTimeout(() => console.log("B"), 100);
		Promise.resolve().then(() => console.log("C"));
		queueMicrotask(() => console.log("D"));
		console.log("E");
		{
			// A, E, B, C, D
			// ✅ ⁡⁢⁢⁢A, E, C, D, B⁡ ✔️ A, E — синхронно. C, D — микрозадачи (очередь промисов и queueMicrotask). B — макрозадача.
			// A, B, C, D, E
			// E, A, C, D, B
		}
	}
	// ⚠️ ⁡⁣⁣⁢Какой метод Promise выполнится, когда ВСЕ промисы завершатся (независимо от успеха)?⁡
	{
		// Promise.all()
		// Promise.any()
		// ✅ ⁡⁢⁢⁢Promise.allSettled()⁡ ✔️
		// Promise.race()
	}
	// ⚠️ ⁡⁣⁣⁢Для чего используется AbortController?⁡
	{
		// ⁡⁢⁢⁢Для отмены вызовов fetch или других асинхронных операций.⁡ ✔️
		// Для прерывания выполнения синхронного цикла.
		// Для остановки работы Web Worker.
		// Для управления состоянием приложения (state management).
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «состояние гонки» (Race Condition) в асинхронном коде?⁡
	{
		// ⁡⁢⁢⁢Непредсказуемый результат, возникающий, когда поведение системы зависит от последовательности или времени выполнения асинхронных операций.⁡ ✔️
		// Ошибка, возникающая при использовании Promise.race().
		// Конфликт имен переменных в разных областях видимости.
		// Проблема с блокировкой основного потока.
	}
	// ⚠️ ⁡⁣⁣⁢Какой из этих способов НЕ является асинхронным в JavaScript?⁡
	{
		// setTimeout(callback, delay)
		// fs.readFile(path, callback) (в Node.js)
		// ✅ ⁡⁢⁢⁢array.forEach(callback)⁡ ✔️ forEach выполняет callback синхронно для каждого элемента.
		// fetch(url).then(callback)
	}
	// ⚠️ ⁡⁣⁣⁢Как создать «задержку» (sleep) на 2 секунды в асинхронной функции?⁡
	{
		// setTimeout(2000);
		// ✅ ⁡⁢⁢⁢await new Promise(res => setTimeout(res, 2000));⁡ ✔️
		// delay(2000);
		// wait(2000);
	}
	// ⚠️ ⁡⁣⁣⁢Что выведет код?⁡
	{
		async function getData() {
			return await Promise.reject("Неуспешный!");
		}
		getData().catch((err) => console.log("Пойманный:", err));
		{
			// ⁡⁢⁢⁢Пойманный: Неуспешный!⁡ ✔️ await «разворачивает» промис, но если промис отклонен, он выбросит исключение, которое будет перехвачено цепочкой .catch.
			// Uncaught (in promise) Failed!
			// Promise {<rejected>: 'Failed!'}
			// Ничего, программа зависнет.
		}
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «обратный вызов с двумя аргументами» (error-first callback) в Node.js?⁡
	{
		// ⁡⁢⁢⁢Паттерн, где первый аргумент колбэка — объект ошибки (или null), а второй — результат успешного выполнения.⁡ ✔️
		// Колбэк, который вызывается дважды: при успехе и при ошибке.
		// Колбэк, который принимает ошибку и контекст выполнения.
		// Устаревший паттерн, замененный на Promises.
	}
	// ⚠️ ⁡⁣⁣⁢Как выполнить несколько независимых асинхронных запросов ПАРАЛЛЕЛЬНО и дождаться всех результатов?⁡
	{
		// Использовать их в цепочке .then().then().
		// Вызвать их в цикле for с await.
		// ✅ ⁡⁢⁢⁢Использовать Promise.all([request1, request2])⁡ ✔️
		// Использовать async.parallel (сторонняя библиотека).
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «потоковая передача» (Streaming) данных и какие API в браузере её поддерживают?⁡
	{
		// ⁡⁢⁢⁢Постепенная обработка данных по мере их поступления. Примеры: fetch + ReadableStream, WebSocket.⁡ ✔️
		// Отправка данных большими блоками. API: FileReader.
		// Кэширование данных в памяти. API: localStorage.
		// Синхронное чтение файлов. API: XMLHttpRequest.
	}
}
// 🔳 ⁡⁢⁣⁣КАТЕГОРИЯ 3: БРАУЗЕРНЫЕ API И DOM (ВОПРОСЫ 36-50)⁡
{
	// ⚠️ ⁡⁣⁣⁢В чем разница между attribute и property DOM-элемента?⁡
	{
		// ⁡⁢⁢⁢Attribute — это значение из HTML-разметки. Property — это текущее значение в объекте DOM, которое может меняться.⁡ ✔️
		// Это синонимы.
		// Attribute — для стилей, Property — для содержимого.
		// Attribute доступен только для чтения.
	}
	// ⚠️ ⁡⁣⁣⁢Какой объект содержит информацию о событии (клик, нажатие клавиши и т.д.)?⁡
	{
		// EventTarget
		// ✅ ⁡⁢⁢⁢Event⁡ ✔️ Событие
		// EventListener
		// Window
	}
	// ⚠️ ⁡⁣⁣⁢Как остановить всплытие (bubbling) события?⁡
	{
		// ⁡⁢⁢⁢event.stopPropagation()⁡ ✔️
		// event.preventDefault()
		// event.stopImmediatePropagation()
		// event.cancelBubble = true (устаревший способ)
	}
	// ⚠️ ⁡⁣⁣⁢Какой метод добавляет обработчик события, который сработает только один раз?⁡
	{
		// ⁡⁢⁢⁢element.addEventListener('click', handler, {once: true}) ⁡✔️
		// element.once('click', handler)
		// element.addEventHandler('click', handler, true)
		// Такого метода нет, нужно удалять обработчик вручную.
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «пассивный» обработчик события (passive: true)?⁡
	{
		// ⁡⁢⁢⁢Обработчик, который никогда не вызовет preventDefault(), что позволяет браузеру сразу выполнить скролл, не дожидаясь выполнения JS.⁡ ✔️
		// Обработчик, который срабатывает только при пассивном скролле (колесиком мыши).
		// Обработчик с низким приоритетом.
		// Устаревшая опция для совместимости.
	}
	// ⚠️ ⁡⁣⁣⁢В чем разница между window.load и DOMContentLoaded?⁡
	{
		// ⁡⁢⁢⁢DOMContentLoaded — когда DOM построен. load — когда загружены ВСЕ ресурсы (стили, изображения).⁡ ✔️
		// DOMContentLoaded — для динамического контента, load — для статического.
		// Это одно и то же событие.
		// DOMContentLoaded срабатывает позже.
	}
	// ⚠️ ⁡⁣⁣⁢Как получить все элементы с классом item внутри #container?⁡
	{
		// ⁡⁢⁢⁢document.querySelectorAll('#container .item')⁡ ✔️
		// document.getElementsByClassName('item container')
		// document.getElementById('container').getClasses('item')
		// #container > .item (это CSS, не JS).
	}
	// ⚠️ ⁡⁣⁣⁢Какой API позволяет наблюдать за изменениями в DOM (появление/исчезновение элементов)?⁡
	{
		// ⁡⁢⁢⁢MutationObserver⁡ ✔️
		// DOMWatcher;
		// EventDelegate;
		// ResizeObserver;
	}
	// ⚠️ ⁡⁣⁣⁢Для чего используется requestAnimationFrame(callback)?⁡
	{
		// ⁡⁢⁢⁢Для выполнения анимаций, синхронизируя вызов callback с частотой обновления экрана.⁡ ✔️
		// Для создания асинхронной задержки, аналогично setTimeout.
		// Для выполнения тяжелых вычислений в фоне.
		// Для отправки аналтитических событий.
	}
	// ⚠️ ⁡⁣⁣⁢Как отложить выполнение кода до следующего цикла обновления («тик») Event Loop?⁡
	{
		// ✅ ⁡⁢⁢⁢setTimeout(fn, 0) или queueMicrotask(fn) в зависимости от приоритета.⁡ ✔️
		// process.nextTick() (только Node.js).
		// window.nextFrame().
		// Promise.immediate().
	}
	// ⚠️ ⁡⁣⁣⁢Как проверить, виден ли элемент в области просмотра (viewport)?⁡
	{
		// ⁡⁢⁢⁢Использовать IntersectionObserver.⁡ ✔️
		// Сравнить координаты элемента (getBoundingClientRect()) с размерами окна.
		// Проверить CSS-свойство visibility.
		// ⁡⁢⁢Оба первых варианта верны.⁡ ✔️
	}
	// ⚠️ ⁡⁣⁣⁢Какой API используется для копирования текста в буфер обмена?⁡
	{
		// ⁡⁢⁢⁢Navigator.clipboard.writeText()⁡ ✔️ read Text() прочитать текст, write Text(text) записать текст
		// Document.copy();
		// window.clipboard;
		// execCommand("copy")(устаревший);
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «Web Workers» и для чего они используются?⁡
	{
		// ⁡⁢⁢⁢Механизм для запуска скриптов в фоновом потоке, отдельно от основного потока UI.⁡ ✔️ Вынести тяжёлые вычисления из основного потока. Сохранить отзывчивость интерфейса при работе с большими данными. Использовать многопоточность в JavaScript (в рамках ограничений браузера).
		// Сервисы для кэширования ресурсов.
		// Скрипты для обработки событий мыши.
		// API для создания многопоточных анимаций.
	}
	// ⚠️ ⁡⁣⁣⁢Какой объект представляет историю навигации браузера?⁡
	{
		// ⁡⁢⁢⁢window.history⁡ ✔️
		// window.navigation;
		// location.history;
		// document.history;
	}
	// ⚠️ ⁡⁣⁣⁢Как программно инициировать полную перезагрузку страницы?⁡
	{
		// location.reload(true) (устаревший force)
		// ✅ ⁡⁢⁢⁢location.reload()⁡ ✔️
		// window.refresh()
		// document.reload()
	}
}
// 🔳 ⁡⁢⁣⁣КАТЕГОРИЯ 4: ХРАНЕНИЕ ДАННЫХ И РАБОТА С СЕТЬЮ (ВОПРОСЫ 51-65)⁡
{
	// ⚠️ ⁡⁣⁣⁢В чем ключевое отличие localStorage от sessionStorage?⁡
	{
		// ⁡⁢⁢⁢localStorage не имеет срока действия, sessionStorage очищается при закрытии вкладки.⁡ ✔️
		// localStorage доступен только по HTTPS.
		// sessionStorage имеет больший лимит объема.
		// localStorage работает только в основном потоке.
	}
	// ⚠️ ⁡⁣⁣⁢Какой заголовок HTTP используется для управления кэшированием на стороне клиента?⁡
	{
		// ⁡⁢⁢⁢Cache-Control ⁡✔️
		// Content - Type;
		// Authorization;
		// ETag;
	}
	// ⚠️ ⁡⁣⁣⁢Что такое CORS (Cross-Origin Resource Sharing)?⁡
	{
		// ⁡⁢⁢⁢Механизм, который использует дополнительные HTTP-заголовки, чтобы дать браузеру разрешение на доступ к ресурсам с другого домена.⁡ ✔️
		// Протокол для шифрования данных между клиентом и сервером.
		// Способ кэширования запросов между разными доменами.
		// Политика безопасности, полностью запрещающая межсайтовые запросы.
	}
	// ⚠️ ⁡⁣⁣⁢Какой метод fetch используется для отправки данных формы (FormData) на сервер?⁡
	{
		// ⁡⁢⁢⁢fetch(url, { method: 'POST', body: formData })⁡ ✔️ Заголовок Content-Type установится автоматически как multipart/form-data.
		// fetch(url, { method: 'POST', body: JSON.stringify(formData) })
		// Только с помощью XMLHttpRequest.
		// fetch(url, { method: 'POST', headers: {'Content-Type': 'application/json'}, body: formData })
	}
	// ⚠️ ⁡⁣⁣⁢Что такое IndexedDB и когда его используют?⁡
	{
		// ⁡⁢⁢⁢Клиентская NoSQL-база данных для хранения больших объемов структурированных данных с поддержкой транзакций и индексов.⁡ ✔️
		// API для хранения пар «ключ-значение» (аналог localStorage).
		// Встроенная реляционная база данных в браузере.
		// Устаревшая технология, замененная на WebSQL.
	}
	// ⚠️ ⁡⁣⁣⁢Какой статусный код HTTP означает «Создано» (успешное создание ресурса)?⁡
	{
		// ✅ ⁡⁢⁢⁢201 Created⁡ ✔️
		// 200 OK
		// 204 No Content
		// 301 Moved Permanently
	}
	// ⚠️ ⁡⁣⁣⁢Как отправить cookie вместе с кросс-доменным fetch запросом?⁡
	{
		// ⁡⁢⁢⁢fetch(url, { credentials: 'include' })⁡ ✔️ отправляет куки(по умолчанию браузер их не включает в кросс‑доменные запросы)
		// fetch(url, { mode: 'cors', cookies: true })
		// fetch(url, { useCookies: true })
		// Cookies отправляются автоматически всегда.
	}
	// ⚠️ ⁡⁣⁣⁢Какой тип хранилища лучше для токена аутентификации, который должен быть доступен только во время сессии?⁡
	{
		// ⁡⁢⁢⁢sessionStorage ⁡✔️
		// localStorage
		// Cookie с флагом HttpOnly
		// IndexedDB
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «preflight request» (предварительный запрос) в CORS?⁡
	{
		// ⁡⁢⁢⁢Запрос методом OPTIONS, который браузер автоматически отправляет для сложных запросов, чтобы проверить разрешения сервера.⁡ ✔️ Preflight не отправляется для «простых» запросов (только GET/POST/HEAD с разрешёнными заголовками).
		// Запрос на предзагрузку ресурсов.
		// Первый запрос в цепочке редиректов.
		// Запрос кэша браузера перед обращением к сети.
	}
	// ⚠️ ⁡⁣⁣⁢Какой API позволяет приложению работать в оффлайн-режиме и загружать ресурсы в фоне?⁡
	{
		// ⁡⁢⁢⁢Service Worker + Cache API⁡ ✔️ Service Workers — «фоновые процессы» Отдельный JavaScript‑поток, работающий вне основного потока страницы. Может перехватывать сетевые запросы, управлять кэшем и выполнять задачи в фоне, даже когда страница закрыта. Cache API — хранение ресурсов. API для явного сохранения и извлечения HTTP‑ответов (HTML, CSS, JS, изображений и т. п.) в именованных кэшах.
		// Web Workers
		// localStorage
		// Background Fetch API
	}
	// ⚠️ ⁡⁣⁣⁢В чем разница между PUT и PATCH HTTP-методами?⁡
	{
		// ⁡⁢⁢⁢PUT — полное обновление ресурса. PATCH — частичное обновление.⁡ ✔️
		// PUT — для создания, PATCH — для обновления.
		// PUT — идемпотентный, PATCH — нет.
		// Разницы нет, это синонимы.
	}
	// ⚠️ ⁡⁣⁣⁢Что такое WebSocket и чем отличается от HTTP?⁡
	{
		// ⁡⁢⁢⁢Протокол для двусторонней (full-duplex) связи поверх одного TCP-соединения.⁡ ✔️ HTTP — запрос-ответ.
		// Устаревший протокол для чатов.
		// HTTP-метод для потоковой передачи.
		// Протокол, работающий только поверх HTTP/2.
	}
	// ⚠️ ⁡⁣⁣⁢Как безопасно хранить JWT-токен на клиенте?⁡
	{
		// ⁡⁢⁢⁢В HttpOnly cookie (для защиты от XSS). В localStorage/sessionStorage только если приняты меры против XSS.⁡ ✔️
		// Только в localStorage.
		// В глобальной переменной JavaScript.
		// В IndexedDB с шифрованием.
	}
	// ⚠️ ⁡⁣⁣⁢Какой заголовок отвечает за сжатие данных (gzip, brotli) при HTTP-запросе?⁡
	{
		// ⁡⁢⁢⁢Accept-Encoding⁡ ✔️
		// Content - Encoding;
		// Transfer - Encoding;
		// Content - Length;
	}
	// ⚠️ ⁡⁣⁣⁢Что такое HTTP/2 Server Push?⁡
	{
		// ⁡⁢⁢⁢Возможность сервера отправлять ресурсы клиенту до того, как тот их запросит.⁡ ✔️ Не ждёт загрузки index.html, чтобы обнаружить ссылки на CSS/JS/img.
		// Принудительная перезагрузка страницы с сервера.
		// Механизм отправки уведомлений от сервера.
		// Протокол для push-уведомлений.
	}
}
// 🔳 ⁡⁢⁣⁣КАТЕГОРИЯ 5: ПРОИЗВОДИТЕЛЬНОСТЬ И ОПТИМИЗАЦИЯ (ВОПРОСЫ 66-80)⁡
{
	// ⚠️ ⁡⁣⁣⁢Что такое «debouncing» и «throttling» и зачем они нужны?⁡
	{
		// ⁡⁢⁢⁢Debouncing(«отключение») — откладывает выполнение функции до истечения периода бездействия. Throttling(«регулирование») — ограничивает частоту выполнения функции.⁡ ✔️
		// Debouncing — для анимаций, Throttling — для обработки событий.
		// Это способы кэширования результатов функции.
		// Алгоритмы сжатия данных.
	}
	// ⚠️ ⁡⁣⁣⁢Какой инструмент браузера наиболее важен для первоначального анализа производительности загрузки?⁡
	{
		// ⁡⁢⁢⁢Lighthouse⁡ ✔️ — автоматизированный аудит производительности, доступности, SEO и др.
		// Console;
		// Network;
		// Elements;
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «Critical Rendering Path»(CRP) (Критический путь рендеринга)?⁡
	{
		// ⁡⁢⁢⁢Последовательность шагов, которые браузер проходит для преобразования HTML, CSS и JS в пиксели на экране.⁡ ✔️
		// Путь к критическим CSS-файлам.
		// Алгоритм определения видимых элементов.
		// Процесс загрузки шрифтов.
	}
	// ⚠️ ⁡⁣⁣⁢Какой атрибут у тега <script> позволяет загружать его без блокировки парсинга HTML?⁡
	{
		// ⁡⁢⁢⁢async⁡ ✔️ Скрипт загружается параллельно с HTML. Выполняется сразу после загрузки, даже если HTML ещё не до конца обработан. Не гарантирует порядок выполнения: если несколько скриптов с async, они запускаются в порядке завершения загрузки.
		// defer; Скрипт загружается параллельно с парсингом HTML. Выполняется только после полной загрузки и обработки HTML (после события DOMContentLoaded, но до load). Сохраняет порядок выполнения скриптов: если на странице несколько <script defer>, они выполняются строго в том порядке, в котором указаны в коде.
		// non - blocking;
		// preload;
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «tree shaking»?⁡
	{
		// ⁡⁢⁢⁢Удаление неиспользуемого кода (мертвого кода) из финального бандла на этапе сборки.⁡ ✔️
		// Оптимизация DOM-дерева.
		// Алгоритм сжатия изображений.
		// Метод кэширования HTTP-запросов.
	}
	// ⚠️ ⁡⁣⁣⁢Какой CSS-свойство «вырывает» элемент из нормального потока и может вызвать перерасчет layout?⁡
	{
		// ⁡⁢⁢⁢position: absolute / fixed⁡ ✔️
		// display: none;
		// visibility: hidden;
		// opacity: 0;
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «layout thrashing» (или «forced synchronous layout»)?⁡
	{
		// ⁡⁢⁢⁢Неудачный паттерн, когда JS многократно и синхронно читает и меняет геометрию DOM, вызывая множество перерасчетов.⁡⁡ ✔️ Layout thrashing (или forced synchronous layout) — это ситуация, когда браузер вынужден многократно и несвоевременно пересчитывать макет (layout) страницы
		// Ошибка при неправильном использовании flexbox.
		// Слишком частые вызовы requestAnimationFrame.
		// Блокировка основного потока тяжелыми вычислениями.
	}
	// ⚠️ ⁡⁣⁣⁢Какой Web API помогает отложить тяжелые вычисления, не блокируя основной поток?⁡
	{
		// ⁡⁢⁢⁢Web Workers⁡ ✔️
		// setTimeout
		// requestIdleCallback
		// async/await
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «Lazy Loading» и как его реализовать для изображений?⁡
	{
		// ⁡⁢⁢⁢Отложенная загрузка ресурсов до момента, когда они понадобятся. Атрибут <img loading="lazy"> или IntersectionObserver.⁡ ✔️
		// Предзагрузка изображений до того, как пользователь доскроллит.
		// Кэширование всех изображений в localStorage.
		// Использование CSS background-image вместо <img>.
	}
	// ⚠️ ⁡⁣⁣⁢Какой метод помогает измерить время выполнения участка кода?⁡
	{
		// console.time('label') / console.timeEnd('label') - Простой способ замера в консоли браузера или Node.js.
		// Date.now() до и после. - Получает текущее время в миллисекундах.
		// performance.mark() / performance.measure() - Высокоточное время (в миллисекундах с дробной частью) на основе API (рекомендуется)
		// ⁡⁢⁢⁢Все варианты верны.⁡ ✔️
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «code splitting» (разделение кода) и зачем он нужно?⁡
	{
		// ⁡⁢⁢⁢Разделение одного большого бандла на более мелкие части (чанки), которые можно загружать по необходимости.⁡ ✔️
		// Разделение кода на модули.
		// Удаление комментариев и пробелов.
		// Разделение кода на серверный и клиентский.
	}
	// ⚠️ ⁡⁣⁣⁢Какой из этих факторов НЕ влияет на производительность загрузки сайта?⁡
	{
		// ⁡⁢⁢⁢Цветовая схема интерфейса.⁡ ✔️
		// Размер и количество HTTP-запросов.
		// Оптимизация изображений.
		// Минификация и сжатие ресурсов.
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «Long Task»(«длинная задача») в терминах производительности?⁡
	{
		// ⁡⁢⁢⁢Задача, выполняющаяся в основном потоке более 50 мс и блокирующая взаимодействие.⁡ ✔️
		// Асинхронная операция, занимающая более 1 секунды.
		// Запрос к сети с большим временем отклика.
		// Любая операция в Web Worker.
	}
	// ⚠️ ⁡⁣⁣⁢Какой инструмент используется для анализа размера бандла и входящих в него модулей?⁡
	{
		// ⁡⁢⁢⁢webpack-bundle-analyzer⁡ ✔️
		// Chrome DevTools -> Coverage
		// Lighthouse
		// PageSpeed Insights
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «preconnect», prefetch, preload?⁡
	{
		// ⁡⁢⁢⁢Подсказки (<link rel="...">) для браузера, чтобы заранее установить соединение, предзагрузить или предвыбрать ресурсы.⁡ ✔️ preconnect — для быстрого соединения с внешними доменами. prefetch — для фоновой загрузки ресурсов будущих страниц. preload — для принудительной загрузки критических ресурсов текущей страницы.
		// Методы оптимизации JavaScript.
		// Атрибуты тега <script>.
		// HTTP-заголовки для кэширования.
	}
}
// 🔳 ⁡⁢⁣⁣КАТЕГОРИЯ 6: АРХИТЕКТУРА, БЕЗОПАСНОСТЬ, ИНСТРУМЕНТЫ (ВОПРОСЫ 81-100)⁡
{
	// ⚠️ ⁡⁣⁣⁢Что такое «инверсия зависимостей» (Dependency Inversion) в SOLID?⁡
	{
		// ⁡⁢⁢⁢Модули верхнего уровня не должны зависеть от модулей нижнего уровня. Оба должны зависеть от абстракций.⁡ ✔️
		// Каждый класс должен иметь только одну причину для изменения.
		// Объекты должны быть открыты для расширения, но закрыты для изменения.
		// Принцип разделения интерфейса.
	}
	// ⚠️ ⁡⁣⁣⁢Какой шаблон проектирования использует один глобальный экземпляр для доступа к общим ресурсам?⁡
	{
		// ⁡⁢⁢⁢Singleton (Одиночка)⁡ ✔️
		// Factory(Фабрика);
		// Observer(Наблюдатель);
		// Decorator(Декоратор);
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «XSS» (Cross-Site Scripting) атака?⁡
	{
		// ⁡⁢⁢⁢Внедрение вредоносного JavaScript-кода на страницу, который выполняется в браузере жертвы.⁡⁡ ✔️
		// Подделка межсайтовых запросов (CSRF).
		// Кража сессионных cookie через поддельный сайт.
		// Атака на сервер базы данных.
	}
	// ⚠️ ⁡⁣⁣⁢Как защититься от XSS?⁡
	{
		// ⁡⁢⁢⁢Экранирование (escaping) пользовательского ввода, использование textContent вместо innerHTML, Content Security Policy (CSP).⁡ ✔️
		// Использование только HTTPS.
		// Валидация данных на стороне клиента.
		// Хэширование паролей.
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «CSP» (Content Security Policy)?⁡
	{
		// ⁡⁢⁢⁢Механизм безопасности, который позволяет ограничить источники, из которых могут загружаться скрипты, стили и другие ресурсы.⁡ ✔️
		// Политика кэширования контента.
		// Протокол безопасной передачи данных.
		// Стандарт для HTTP/2.
	}
	// ⚠️ ⁡⁣⁣⁢В чем разница между git merge и git rebase?⁡
	{
		// ⁡⁢⁢⁢merge создает новый commit-слияния, сохраняя историю. rebase перемещает ваши коммиты «перед» коммитами целевой ветки, переписывая историю.⁡ ✔️
		// merge работает быстрее, чем rebase.
		// rebase нельзя использовать в команде.
		// Разницы нет, это синонимы.
	}
	// ⚠️ ⁡⁣⁣⁢Какой команды НЕ существует в git?⁡
	{
		// git clone
		// git commit
		// ✅ ⁡⁢⁢⁢git push --overwrite⁡ ✔️
		// git stash
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «Docker-контейнер»?⁡
	{
		// ⁡⁢⁢⁢Легковесная, переносимая единица программного обеспечения, которое включает в себя код и все его зависимости.⁡ ✔️
		// Виртуальная машина с полной ОС.
		// Сервис облачного хранения.
		// Система управления пакетами для Node.js.
	}
	// ⚠️ ⁡⁣⁣⁢Для чего используется nginx в веб-разработке?⁡
	{
		// ⁡⁢⁢⁢Веб-сервер, обратный прокси, кэширующий прокси, балансировщик нагрузки.⁡ ✔️
		// Система управления базами данных.
		// Фреймворк для JavaScript.
		// Инструмент для тестирования.
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «микросервисная архитектура»?⁡
	{
		// ⁡⁢⁢⁢Архитектурный стиль, при котором приложение состоит из небольших, независимых сервисов, взаимодействующих через API.⁡ ✔️
		// Архитектура с одним монолитным сервером.
		// Паттерн проектирования на стороне клиента.
		// Синоним Serverless.
	}
	// ⚠️ ⁡⁣⁣⁢Какой статусный код HTTP вернется, если ресурс не найден?⁡
	{
		// ✅ ⁡⁢⁢⁢404⁡ ✔️ ресурс не найден.
		// 200 // OK – стандартный успешный ответ.⁡
		// 403 // доступ запрещён (нет прав).⁡
		// 500 // общая ошибка сервера.⁡
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «Continuous Integration» (CI)?⁡
	{
		// ⁡⁢⁢⁢Практика автоматического тестирования и сборки каждого изменения в кодовой базе.⁡ ✔️
		// Непрерывное развертывание на продакшн.
		// Интеграция разных микросервисов.
		// Процесс код-ревью.
	}
	// ⚠️ ⁡⁣⁣⁢Какой метод HTTP является идемпотентным?⁡
	{
		// ⁡⁢⁢⁢GET, PUT, DELETE⁡ ✔️
		// POST, PATCH
		// Только GET
		// Все методы.
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «GraphQL» и чем отличается от REST?⁡
	{
		// ⁡⁢⁢⁢Язык запросов для API, позволяющий клиенту запрашивать именно те данные, которые ему нужны, за один запрос.⁡ ✔️
		// Новая версия протокола HTTP.
		// Библиотека для построения графиков.
		// Синоним WebSocket.
	}
	// ⚠️ ⁡⁣⁣⁢Какой инструмент используется для статической типизации JavaScript?⁡
	{
		// ⁡⁢⁢⁢TypeScript⁡ ✔️
		// Babel;
		// ESLint;
		// Webpack;
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «Server-Side Rendering» (SSR)?⁡
	{
		// ⁡⁢⁢⁢Рендеринг HTML на сервере и отправка готовой страницы клиенту.⁡ ✔️
		// Рендеринг с помощью Web Workers.
		// Использование шаблонизаторов на клиенте.
		// Генерация PDF на сервере.
	}
	// ⚠️ ⁡⁣⁣⁢Какой принцип ООП описывает скрытие внутренней реализации объекта?⁡
	{
		// ⁡⁢⁢⁢Инкапсуляция⁡ ✔️
		// Наследование; // Это механизм, позволяющий создавать классы (подклассы) на основе других классов
		// Полиморфизм; // ⁢Переопределение методов в дочерних классах
		// Абстракция; // Это концепция, которая позволяет скрыть детали реализации объекта и предоставить пользователю только существенные функции.
	}
	// ⚠️ ⁡⁣⁣⁢Что такое «Webpack» и для чего он используется?⁡
	{
		// ⁡⁢⁢⁢Сборщик модулей (module bundler) для JavaScript-приложений.⁡ ✔️
		// Фреймворк для тестирования.
		// Веб-сервер для разработки.
		// Система контроля версий.
	}
	// ⚠️ ⁡⁣⁣⁢Какой из этих методов НЕ является методом жизненного цикла React-компонента (class component)?⁡
	{
		// ✅ ⁡⁢⁢⁢componentWillRecieveProps⁡ ✔️ Устарел и переименован в UNSAFE_componentWillReceiveProps.
		// componentDidMount
		// componentDidUpdate
		// render
	}
}
