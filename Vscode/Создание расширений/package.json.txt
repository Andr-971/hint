{
  // 🔹 Обязательные поля

  "$schema": "https://json.schemastore.org/package.json", // Если предупреждение
  "name": "my-awesome-package", // Уникальное имя пакета. Только строчные буквы, цифры, дефисы и подчеркивания. Не может начинаться с точки или подчеркивания. Обязательно для публикации в npm.
  "version": "1.0.0", // Версия пакета по схеме SemVer (MAJOR.MINOR.PATCH)

  // 🔹 Описание и метаданные

  "description": "A short description of your package", // Краткое описание пакета (отображается в npm)
  "keywords": ["react", "microfrontend", "vite"], // Массив ключевых слов для поиска в npm
  "homepage": "https://github.com/user/repo#readme", // URL домашней страницы проекта
  "bugs": {
    "url": "https://github.com/user/repo/issues", // Ссылка на трекер багов
    "email": "bugs@example.com" // Альтернативно или дополнительно можно указать email
  },
  "license": "MIT", // Лицензия пакета (рекомендуется SPDX-совместимое значение)
  "author": {
    "name": "John Doe",
    "email": "john@example.com",
    "url": "https://johndoe.com"
  },
  // или кратко: "author": "John Doe <john@example.com> (https://johndoe.com)",

  "contributors": [ // Опциональный массив соавторов (формат как у author)
    {
      "name": "Jane Smith",
      "email": "jane@example.com",
      "url": "https://janesmith.dev"
    }
  ],

  // 🔹 Зависимости

  "dependencies": {
    // Пакеты, необходимые для работы приложения в production
    "react": "^18.2.0"
  },
  "devDependencies": {
    // Пакеты, необходимые только для разработки (тесты, сборка и т.д.)
    "vite": "^5.0.0",
    "@types/react": "^18.2.0"
  },
  "peerDependencies": {
    // Пакеты, которые должны быть установлены в проекте-потребителе (часто используются в библиотеках)
    "react": "^18.0.0"
  },
  "optionalDependencies": {
    // Необязательные зависимости — установка не вызовет ошибку при неудаче
    "some-native-module": "*"
  },
  "bundledDependencies": ["package-name"], // (устаревшее) Пакеты, которые будут включены в архив при публикации
  // Альтернатива: "bundleDependencies": ["package-name"],

  // 🔹 Скрипты

  "scripts": {
    // Пользовательские команды, вызываемые через npm run <script-name>
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "lint": "eslint .",
    "prepare": "husky install", // Запускается автоматически при npm install
    "prepublishOnly": "npm test", // Выполняется перед публикацией (после упаковки)
    "postinstall": "echo 'Installed!'" // Выполняется после установки пакета
  },

  // 🔹 Пути и структура

  "main": "dist/index.js", // Точка входа для CommonJS (по умолчанию: index.js)
  "module": "dist/index.mjs", // Точка входа для ES-модулей (используется сборщиками типа Vite, Rollup)
  "exports": {
    // Современный способ указания точек входа (поддерживает условия: import, require, browser и др.)
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./package.json": "./package.json"
  },
  "types": "dist/index.d.ts", // Типы TypeScript (альтернатива "typings")
  "typings": "dist/index.d.ts", // То же самое, что types

  "files": [ // Список файлов/папок, включаемых при публикации в npm
    "dist",
    "README.md",
    "LICENSE"
  ],

  "bin": {
    // Регистрация исполняемых файлов CLI
    "my-cli": "./bin/my-cli.js"
  },
  // или для одного CLI: "bin": "./bin/my-cli.js",

  // 🔹 Конфигурация

  "config": {
    // Конфигурация для скриптов (редко используется)
    "port": "8080"
  },

  "engines": {
    // Требуемые версии сред выполнения
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },

  "os": ["linux", "!win32"], // Поддерживаемые ОС (например, только Linux, кроме Windows)
  "cpu": ["x64", "!arm"] // Поддерживаемые архитектуры CPU

  // 🔹 Публикация

  "publishConfig": {
    // Конфигурация при публикации
    "registry": "https://npm.pkg.github.com/",
    "access": "public", // или "restricted" для scoped-пакетов
    "tag": "latest"
  },

  // 🔹 Прочее

  "repository": {
    // Репозиторий исходного кода
    "type": "git",
    "url": "https://github.com/user/repo.git"
  },
  "private": true, // Если true — пакет нельзя публиковать в npm
  "workspaces": [ // Для монорепозиториев (Yarn / npm >=7)
    "packages/*"
  ],
  "preferGlobal": false, // Устарело. Раньше использовалось для CLI-утилит

  // 🔹 Поля для инструментов

  "eslintConfig": { ... }, // Встроенная конфигурация ESLint (редко используется)
  "prettier": { ... }, // Встроенная конфигурация Prettier
  "jest": { ... }, // Встроенная конфигурация Jest
  "babel": { ... } // Встроенная конфигурация Babel
}