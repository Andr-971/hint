
//! ⁡⁢⁣⁢ОСНОВНЫЕ ПРИНЦИПЫ И ОПРЕДЕЛЕНИЯ REACT С ПРИМЕРАМИ:⁡
{
	// ⁡⁢⁣⁣🔳 КОМПОНЕНТНЫЙ ПОДХОД⁡
	// ⁡⁣⁣⁢React позволяет создавать интерфейсы из переиспользуемых компонентов (функциональных или классовых).⁡
	{
		// ⁡⁣⁢⁢Пример функционального компонента:⁡
		function Greeting({ name }) {
			return <h1>Hello, {name}!</h1>;
		}
		// ⁡⁣⁢⁢Пример классового компонента:⁡
		class Greeting extends React.Component {
			render() {
				return <h1>Hello, {this.props.name}!</h1>;
			}
		}
	}
    // 🔳 ⁡⁢⁣⁣ДЕКЛАРАТИВНОСТЬ И ИМПЕРАТИВНОСТЬ В REACT
    {
		// 🔹 ⁡⁣⁢⁢ДЕКЛАРАТИВНЫЙ ПОДХОД:⁡ ⁡⁣⁣⁢описываем, что должно быть, а не как это сделать.⁡
		// 🔹 ⁡⁣⁢⁢ИМПЕРАТИВНЫЙ ПОДХОД:⁡ ⁡⁣⁣⁢указываем шаги для достижения результата.⁡
		// 🔹 ⁡⁣⁢⁢REACT ИСПОЛЬЗУЕТ ДЕКЛАРАТИВНЫЙ ПОДХОД ДЛЯ СОЗДАНИЯ UI.⁡⁡
	}
	// ⁡⁢⁣⁣🔳 ВИРТУАЛЬНЫЙ DOM⁡
	// ⁡⁣⁣⁢React создает виртуальную копию DOM и эффективно обновляет реальный DOM только при изменениях.⁡
	{
		const App = () => {
			const [count, setCount] = useState(0);
			return <button onClick={() => setCount(count + 1)}>Clicked {count} times</button>;
		};
		// ⁡⁣⁣⁢React вычисляет разницу между виртуальными DOM и обновляет только измененные элементы.⁡
	}
	// ⁡⁢⁣⁣🔳 ОДНОНАПРАВЛЕННЫЙ ПОТОК ДАННЫХ⁡
	// ⁡⁣⁣⁢Данные передаются от родителя к потомку через props, события — от потомка к родителю через колбэки.⁡
	{
		// 🔹 ⁡⁣⁢⁢ВСТРОЕННЫЕ СПОСОБЫ REACT⁡
		{
			// ✅ ⁡⁢⁢⁢Передача через props (родитель → дочерний)⁡
			{
				function ParentComponent() {
					const [data, setData] = useState("Hello from parent");

					return (
						<div>
							<ChildComponent message={data} />
						</div>
					);
				}
				function ChildComponent({ message }) {
					return <p>{message}</p>;
				}
			}
			// ✅ ⁡⁢⁢⁢Передача через callback-функции (дочерний → родительский)⁡
			{
				function ParentComponent() {
					const [data, setData] = useState("");

					const handleDataFromChild = (childData) => {
						setData(childData);
					};
					return (
						<div>
							<p>Data from child: {data}</p>
							<ChildComponent onSendData={handleDataFromChild} />
						</div>
					);
				}
				function ChildComponent({ onSendData }) {
					const handleClick = () => {
						onSendData("Hello from child");
					};

					return <button onClick={handleClick}>Send Data to Parent</button>;
				}
			}
			// ✅ ⁡⁢⁢⁢Контекст (Context API)⁡
			{
				// import { createContext, useContext, useState } from "react";
				const DataContext = createContext();
				function App() {
					const [sharedData, setSharedData] = useState("Shared data");

					return (
						<DataContext.Provider value={{ sharedData, setSharedData }}>
							<ParentComponent />
						</DataContext.Provider>
					);
				}
				function ParentComponent() {
					return (
						<div>
							<ChildComponent />
						</div>
					);
				}
				function ChildComponent() {
					const { sharedData, setSharedData } = useContext(DataContext);

					return (
						<div>
							<p>{sharedData}</p>
							<button onClick={() => setSharedData("Updated data")}>
								Update Shared Data
							</button>
						</div>
					);
				}
			}
		}
		// 🔹 ⁡⁣⁢⁢СТОРОННИЕ БИБЛИОТЕКИ ДЛЯ УПРАВЛЕНИЯ СОСТОЯНИЕМ⁡
		{
			// ✅ ⁡⁢⁢⁡⁢⁢⁢Redux (RTK)⁡
			// ✅ ⁡⁢⁢⁢MobX⁡
			// ✅ ⁡⁢⁢⁢Zustand⁡
			// ✅ ⁡⁢⁢⁢React Query (для управления серверным состоянием)⁡
		}
		// 🔹 ⁡⁣⁢⁢ВЫБОР ПОДХОДА⁡
		{
			// ✅ ⁡⁢⁢⁢Простые случаи: Используйте props и callback-функции⁡
			// ✅ ⁡⁢⁢⁢Средние приложения: Context API или Zustand⁡
			// ✅ ⁡⁢⁢⁢Крупные приложения: Redux или MobX⁡
			// ✅ ⁡⁢⁢⁢Серверное состояние: React Query или Apollo Client (для GraphQL)⁡
		}
	}
	// ⁡⁢⁣⁣🔳 СОСТОЯНИЕ (STATE)⁡
    // ⁡⁣⁢⁢State⁡ ⁡⁣⁣⁢— внутренние данные компонента, которые могут изменяться.⁡
    {
        function Counter() {
            const [count, setCount] = useState(0);
            return <button onClick={() => setCount(count + 1)}>Count: {count}</button>;
        }
    }
	// ⁡⁢⁣⁣🔳 ХУКИ (HOOKS, Основные)⁡
	{
		// ⁡⁣⁣⁢Хуки позволяют использовать состояние и другие возможности React в функциональных компонентах.⁡
		// ⁡⁣⁣⁢Основные хуки:⁡
		// ⁡⁣⁢⁢useState:⁡ ⁡⁣⁣⁢Управление состоянием.⁡
		// ⁡⁣⁢⁢useEffect:⁡ ⁡⁣⁣⁢Побочные эффекты (запросы к API, подписки)⁡.
		// ⁡⁣⁢⁢useContext:⁡ ⁡⁣⁣⁢Доступ к контексту.⁡
		// ⁡⁣⁢⁢useRef:⁡ ⁡⁣⁣⁢Ссылки на DOM-элементы⁡⁣⁣⁢, или любые другие значения, которые нужно сохранить между рендерами. В отличие от useState, изменение значения, хранимого в useRef, не вызывает повторного рендера компонента.⁡
		// ⁡⁣⁢⁢useReducer:⁡ ⁡⁣⁣⁢Помогает управлять локальным состоянием сложных компонентов. ⁡

		// ⁡⁣⁢⁢useState: Принцип работы:⁡
		{
			// ⁡⁣⁣⁢useState — это один из базовых хуков React, предназначенный для управления состоянием (state) в функциональных компонентах. Он позволяет компоненту "запоминать" данные между рендерами и автоматически обновлять интерфейс при их изменении.
			// ⁡⁣⁣⁢1. Инициализация состояния:⁡
			// ⁡⁢⁢⁢-- При первом рендере useState(initialValue) возвращает массив из двух элементов:⁡
			// ⁡⁢⁣⁣- Текущее значение состояния (state).⁡
			// ⁡⁢⁣⁣- Функция для его обновления (setState).⁡
			// ⁡⁢⁢⁢-- initialValue — начальное значение состояния (может быть функцией для ленивой инициализации).⁡
			// ⁡⁣⁣⁢2. Обновление состояния:⁡
			// ⁡⁢⁢⁢-- При вызове setState(newValue) React:⁡
			// ⁡⁢⁣⁣- Запоминает новое значение.⁡
			// ⁡⁢⁣⁣- Планирует ререндер компонента.⁡
			// ⁡⁢⁣⁣- Сравнивает новое и старое значение (если они одинаковы, ререндера не будет).⁡
			// ⁡⁣⁣⁢3. Асинхронность обновлений:⁡
			// ⁡⁢⁣⁡⁢⁢⁢-- setState не изменяет состояние сразу — React собирает все обновления и применяет их оптимально.⁡
			// ⁡⁣⁣⁢4. Правила работы:⁡
			// ⁡⁢⁢⁢-- Хуки вызываются только на верхнем уровне компонента (не в условиях/циклах).⁡
			// ⁡⁢⁢⁢-- setState можно вызывать в обработчиках событий, useEffect и других хуках.⁡
		}
		// ⁡⁣⁢⁢useEffect: Принцип работы:⁡
		{
			// ⁡⁣⁣⁢useEffect — это хук в React, предназначенный для выполнения побочных эффектов в функциональных компонентах. Побочные эффекты — это действия, которые взаимодействуют с внешним миром: запросы к API, подписки на события, изменение DOM вручную и т.д.⁡
			// ⁡⁢⁣⁣1. ПРИНЦИП РАБОТЫ⁡
			// ⁡⁣⁣⁢useEffect объединяет логику методов жизненного цикла классовых компонентов (componentDidMount, componentDidUpdate, componentWillUnmount) в одном хуке. Его поведение зависит от массива зависимостей (deps):⁡
			// ⁡⁢⁢⁢-- После каждого рендера (если нет массива зависимостей).⁡
			// ⁡⁢⁢⁢-- Только при монтировании (пустой массив []).⁡
			// ⁡⁢⁢⁢-- При изменении зависимостей (например, [count]).⁡
			// ⁡⁣⁣⁢Также useEffect может возвращать функцию очистки, которая выполняется перед размонтированием компонента или перед следующим вызовом эффекта.⁡
			// ⁡⁢⁣⁣2. Синтаксис⁡
			{
				useEffect(() => {
					// Побочный эффект
					return () => {
						// Очистка эффекта (опционально)
					};
				}, [deps]); // Массив зависимостей
			}
		}
		// ⁡⁣⁢⁢useContext: Принцип работы:⁡
		{
			// ⁡⁣⁣⁢useContext — это хук React, который позволяет получать данные из контекста без использования промежуточных компонентов (Consumer). Он упрощает передачу данных через дерево компонентов, избегая "пропс-дриллинга" (передачи пропсов через множество уровней).⁡
			// ⁡⁣⁣⁢Когда использовать useContext?⁡
			// ✅ ⁡⁣⁣⁢Глобальные настройки (тема, язык, авторизация).⁡
			// ✅ ⁡⁣⁣⁢Общие данные (кеш API, состояние модалок).⁡
			// ✅ ⁡⁣⁣⁢Избежание пропс-дриллинга (когда нужно передавать данные через много уровней).⁡
			// ❌ ⁡⁣⁣⁢Не для часто изменяемых данных (может привести к лишним ререндерам).⁡
			// ❌ ⁡⁣⁣⁢Не заменяет Redux/Zustand (если нужны сложные состояния и middleware).⁡
			// ⁡⁣⁢⁢Синтаксис⁡
			{
				// import { createContext, useContext } from "react";
				// 1. Создание контекста
				const MyContext = createContext(defaultValue);
				// 2. Обеспечение контекста
				<MyContext.Provider value={currentValue}>
					{/* Дочерние компоненты */}
				</MyContext.Provider>;
				// 3. Получение значения
				const value = useContext(MyContext);
			}
		}
		// ⁡⁣⁢⁢useRef⁡⁡⁣⁢⁢:⁡ ⁡⁣⁢⁢Принцип работы⁡⁡⁣⁢⁢:⁡
		{
			// ⁡⁣⁣⁢1. Создание ref: const ref = useRef(initialValue);⁡
			// ⁡⁢⁢⁢-- initialValue — начальное значение для ref.current.⁡
			// ⁡⁢⁢⁢-- Возвращает объект { current: initialValue }.⁡
			// ⁡⁣⁣⁢2. Изменение ref: ref.current = newValue;⁡
			// ⁡⁢⁢⁢-- Значение current можно менять напрямую.⁡
			// ⁡⁢⁢⁢-- Не вызывает ререндер компонента!⁡
			// ⁡⁣⁣⁢3. Сохранение значения:⁡
			// ⁡⁢⁢⁢-- Значение ref.current сохраняется между рендерами компонента, даже если состояние (state) или пропсы меняются.⁡
			// ⁡⁢⁣⁡⁣⁢⁢Основные сценарии использования:⁡
			// ⁡⁣⁣⁢1. Доступ к DOM-элементам: Позволяет напрямую взаимодействовать с DOM-узлами (например, фокусировка, получение размеров).⁡
			// ⁡⁣⁣⁢2. Хранение предыдущего значения состояния: Полезно для сравнения текущего и предыдущего значений состояния.⁡
			// ⁡⁣⁣⁢3. Сохранение значений между рендерами (без ререндера): Позволяет хранить значения, которые не требуют рендера при изменении. Например, хранение ID таймера или анимации.⁡
			// ⁡⁣⁣⁢4. Интеграция со сторонними библиотеками: Позволяет использовать рефы для интеграции с библиотеками, которые требуют прямого доступа к DOM (например, библиотеки анимации, графики).⁡⁡
			// ⁡⁣⁢⁢Особенности и предупреждения:⁡
			// ⁡⁣⁢⁡⁣⁣⁢-- Изменение ref.current не вызывает ререндер. Если значение должно влиять на отображение — используйте useState.⁡
			// ⁡⁣⁣⁢-- Не используйте ref в JSX для вычислений. Значение ref.current может быть null во время первого рендера (если ref привязан к DOM-элементу).⁡
			// ⁡⁣⁣⁢-- Обновление ref в useEffect. Изменения ref внутри useEffect не требуют указания зависимостей, так как ref мутабелен.⁡
			// ⁡⁣⁢⁢Итог:⁡
			// ⁡⁣⁣⁢-- useRef — это мутабельный «контейнер» для значений, которые не влияют на рендер.⁡
			// ⁡⁣⁣⁢-- Основные сценарии: доступ к DOM, хранение данных между рендерами, интеграция со сторонними библиотеками.⁡
			// ⁡⁣⁣⁢-- Не злоупотребляйте useRef там, где можно использовать состояние (useState).⁡
		}
	}
	// ⁡⁢⁣⁣🔳 КОНТЕКСТ (CONTEXT API)⁡
	// ⁡⁣⁣⁢Позволяет передавать данные через дерево компонентов без явной передачи props.⁡
	{
		const ThemeContext = React.createContext("light");
		function App() {
			return (
				<ThemeContext.Provider value="dark">
					<Toolbar />
				</ThemeContext.Provider>
			);
		}
		function Toolbar() {
			const theme = useContext(ThemeContext);
			return <div>Theme: {theme}</div>;
		}
	}
}
//! РАЗВЁРНУТЫЕ ОТВЕТЫ ПО КАТЕГОРИЯМ
{
	// 🔳 ⁡⁢⁣⁡⁢⁣⁣ОБОБЩЁННЫЕ В REACT (VIRTUAL DOM, RECONCILIATION, ⁢МЕМОИЗАЦИЯ, ⁢DIFFING, BATCHING, LAZY LOADING, ⁢BUNDLE ANALYZER)⁡
	{
		// 🔹 ⁡⁣⁢⁢ЧЕМ ОТЛИЧАЕТСЯ REAL DOM ОТ VIRTUAL DOM И ЕГО РАБОТА?⁡ ⁡⁣⁣⁢Real DOM — медленный для частых обновлений. Virtual DOM — легковесная копия, которая сравнивается с предыдущим состоянием для минимизации операций с Real DOM (алгоритм СОГЛАСОВАНИЯ (reconciliation)).⁡
		{
			// ✅ ⁡⁢⁢⁢АЛГОРИТМ СОГЛАСОВАНИЯ (RECONCILIATION)⁡
			{
				// ⚡ ⁡⁢⁣⁣Основной механизм, который сравнивает новое и старое состояние Virtual DOM.⁡
				// ⚡ ⁡⁢⁣⁣React сравнивает деревья на уровне корневых элементов. Если тип элемента изменился (например, <div> → <span>), все поддерево пересоздается.⁡
				// ⚡ ⁡⁢⁣⁣Ключи помогают React идентифицировать элементы списка при изменениях порядка, добавлении или удалении.⁡
				// ⚡ ⁡⁢⁣⁣React сохраняет состояние компонента, если структура DOM остается прежней (даже при изменении атрибутов).⁡
			}
			// ✅ ⁡⁢⁢⁢DIFFING-АЛГОРИТМ⁡
			{
				// ⚡ ⁡⁢⁣⁣Сравнение корневых элементов. Если корневой элемент разного типа → полная перерисовка поддерева.⁡
				// ⚡ ⁡⁢⁣⁣Рекурсивное сравнение дочерних элементов. React проходит по дереву, сравнивая элементы на одном уровне иерархии⁡
				// ⚡ ⁡⁢⁣⁣Обработка списков. Для списков без ключей порядок элементов определяет их идентичность. С ключами React сопоставляет элементы по ключам, даже если порядок изменился.⁡
			}
			// ✅ ⁡⁢⁢⁢ПАКЕТНОЕ ОБНОВЛЕНИЕ СОСТОЯНИЯ useState() (BATCHING)⁡
			{
				// ⚡ ⁡⁢⁣⁣React группирует синхронные обновления состояния useState() в один рендер для оптимизации производительности.⁡
				// ⚡ ⁡⁢⁣⁣Исключение: Асинхронные операции (например, в setTimeout) обрабатываются отдельно до React 18.⁡
				// ⚡ ⁡⁢⁣⁣Итог:⁡
				{
					// ✅ ⁡⁢⁢⁢Колбэк в setState гарантирует работу с актуальным состоянием, даже при батчинге.⁡
					// ✅ ⁡⁢⁢⁢В React 18+ батчинг работает везде (включая асинхронный код), если используется createRoot.⁡
					// ✅ ⁡⁢⁢⁢В старых версиях батчинг работает только в синхронных контекстах.⁡
				}
			}
			// ✅ ⁡⁢⁢⁢МЕМОИЗАЦИЯ (MEMOIZATION)⁡
			{
				// ⚡ ⁡⁢⁣⁣React.memo - компонент высшего порядка (Higher-Order Component, HOC) в React⁡
				{
					// ⭐ ⁡⁣⁣⁢React.memo — это компонент высшего порядка (Higher-Order Component, HOC) в React, предназначенный для оптимизации производительности функциональных компонентов. Он предотвращает лишние ререндеры компонента, если его пропсы не изменились.⁡
					{
						// 🔹 ⁡⁣⁢⁢КАК ЭТО РАБОТАЕТ?⁡
						{
							// ✅ ⁡⁢⁢⁢Мемоизирует компонент: сохраняет результат последнего рендера.⁡
							// ✅ ⁡⁢⁢⁢Сравнивает пропсы: при каждом обновлении проверяет, изменились ли пропсы.⁡
							// ✅ ⁡⁢⁢⁢Перерендеривает только при изменениях: если пропсы те же — возвращает закэшированную версию.
							// ⚪ ⁡⁣⁣⁢Синтаксис⁡
							// const MemoizedComponent = React.memo(Component, [arePropsEqual?]);
							// ⚪ ⁡⁣⁣⁢Component: исходный функциональный компонент.⁡
							// ⚪ ⁡⁣⁣⁢arePropsEqual (опционально): функция для кастомного сравнения пропсов.⁡
						}
						// 🔹 ⁡⁣⁢⁢ПРИМЕР 1: БАЗОВОЕ ИСПОЛЬЗОВАНИЕ⁡
						{
							// import React from "react";
							const UserProfile = ({ name, age }) => {
								console.log("Рендер UserProfile");
								return (
									<div>
										<h2>{name}</h2>
										<p>Возраст: {age}</p>
									</div>
								);
							};
							// Мемоизируем компонент
							// export default React.memo(UserProfile);
							// ✅ ⁡⁢⁢⁢Если родительский компонент обновится, но пропсы name и age останутся прежними — UserProfile не будет ререндериться.⁡
							// ✅ ⁡⁢⁢⁢Без React.memo компонент рендерился бы при каждом обновлении родителя.⁡
						}
						// 🔹 ⁡⁣⁢⁢ПРИМЕР 2: КАСТОМНОЕ СРАВНЕНИЕ ПРОПСОВ⁡
						{
							const arePropsEqual = (prevProps, nextProps) => {
								// Ререндерим только при изменении userId
								// Вернуть должна буллеан, если true — ререндер не нужен, если false — ререндер нужен
								return prevProps.userId === nextProps.userId;
							};
							const UserCard = ({ userId, data }) => {
								/* ... */
							};
							// export default React.memo(UserCard, arePropsEqual);
							// ✅ ⁡⁢⁢⁢Компонент будет игнорировать изменения в data, если userId остался прежним.⁡
							// ⛔ ⁡⁢⁣⁢Важно: Используйте кастомное сравнение осторожно — можно пропустить важные изменения.⁡
						}
						// 🔹 ⁡⁣⁢⁢КОГДА ИСПОЛЬЗОВАТЬ REACT.MEMO?⁡
						{
							// ✅ ⁡⁢⁢⁢Компоненты с тяжелым рендером: большие списки, сложная вёрстка.⁡
							// ✅ ⁡⁢⁢⁢Частые ререндеры родителя: когда родитель обновляется часто, но дочерний компонент не зависит от этих изменений.⁡
							// ✅ ⁡⁢⁢⁢Стабильные пропсы: если пропсы редко меняются (например, данные из Redux).⁡
						}
						// 🔹 ⁡⁣⁢⁢КОГДА НЕ ИСПОЛЬЗОВАТЬ REACT.MEMO?⁡
						{
							// ⛔ ⁡⁢⁣⁢Компоненты с часто меняющимися пропсами.⁡
							// ⛔ ⁡⁢⁣⁢Компоненты, которые всегда должны обновляться (например, отображающие реальное время).⁡
							// ⛔ ⁡⁢⁣⁢Для простых компонентов, где оптимизация избыточна.⁡
						}
						// 🔹 ⁡⁣⁢⁢ИТОГ⁡
						{
							// ⚪ ⁡⁣⁣⁡⁣⁣⁢React.memo — мощный инструмент для оптимизации производительности функциональных компонентов. Он помогает избежать лишних ререндеров, что особенно полезно в больших приложениях с множеством компонентов.⁡
							// ⚪ ⁡⁣⁣⁢Правильное использование React.memo может значительно улучшить производительность приложения, особенно при работе с большими объемами данных или сложными компонентами.⁡
							// ⚪ ⁡⁣⁣⁢Оптимизации производительности в "узких" местах приложения.⁡
							// ⚪ ⁡⁣⁣⁢Работы с компонентами, которые получают стабильные пропсы.⁡
						}
					}
				}
				// ⚡ ⁡⁢⁣⁡⁢⁣⁣useMemo() — мемоизация значений в React⁡
				{
					// ⭐ ⁡⁣⁣⁢useMemo — это хук React, предназначенный для мемоизации (запоминания) результатов вычислений. Он помогает избежать дорогостоящих пересчетов при каждом рендере компонента, оптимизируя производительность.⁡
					{
						// 🔹 ⁡⁣⁢⁢КАК РАБОТАЕТ USEMEMO⁡
						{
							// ✅ ⁡⁢⁢⁢Мемоизация — сохранение результата вычислений между рендерами, если зависимости не изменились.⁡
							// ✅ ⁡⁢⁢⁢При изменении зависимостей — пересчет значения⁡
							// ✅ ⁡⁢⁢⁢При неизменных зависимостях — возврат кэшированного значения⁡
							// ⚪ ⁡⁣⁣⁢Синтаксис⁡
							const memoizedValue = useMemo(
								() => computeExpensiveValue(a, b),
								[a, b],
							);
							// ⚡ ⁡⁢⁣⁣Первый аргумент — функция, возвращающая вычисляемое значение⁡
							// ⚡ ⁡⁢⁣⁣Второй аргумент — массив зависимостей, при изменении которых будет пересчитываться значение⁡
						}
						// 🔹 ⁡⁣⁢⁢ОСНОВНЫЕ СЛУЧАИ ИСПОЛЬЗОВАНИЯ⁡
						{
							// ⚪ ⁡⁣⁣⁢Оптимизация тяжелых вычислений⁡
							{
								function ExpensiveComponent({ list }) {
									// Тяжелая сортировка будет выполняться только при изменении list
									const sortedList = useMemo(() => {
										console.log("Сортировка выполняется");
										return [...list].sort((a, b) => a.value - b.value);
									}, [list]);

									return (
										<div>
											{sortedList.map((item) => (
												<div key={item.id}>{item.value}</div>
											))}
										</div>
									);
								}
							}
							// ⚪ ⁡⁣⁣⁢Избежание лишних рендеров дочерних компонентов⁡
							{
								const UserProfile = React.memo(({ user, preferences }) => {
									/* Рендеринг профиля */
								});

								function ParentComponent({ user }) {
									// Мемоизированный объект не будет создаваться заново при каждом рендере
									const preferences = useMemo(
										() => ({
											theme: "dark",
											notifications: user.notificationsEnabled,
										}),
										[user.notificationsEnabled],
									);

									return <UserProfile user={user} preferences={preferences} />;
								}
							}
							// ⚪ ⁡⁣⁣⁢Оптимизация работы с производными данными⁡
							{
								function Dashboard({ transactions }) {
									const stats = useMemo(
										() => ({
											total: transactions.reduce(
												(sum, t) => sum + t.amount,
												0,
											),
											count: transactions.length,
											average: transactions.length
												? transactions.reduce(
														(sum, t) => sum + t.amount,
														0,
												  ) / transactions.length
												: 0,
										}),
										[transactions],
									);

									return (
										<div>
											<p>Total: {stats.total}</p>
											<p>Transactions: {stats.count}</p>
											<p>Average: {stats.average}</p>
										</div>
									);
								}
							}
						}
						// 🔹 ⁡⁣⁢⁢КОГДА СЛЕДУЕТ ИСПОЛЬЗОВАТЬ USEMEMO⁡
						{
							// ✅ ⁡⁢⁢⁢При тяжелых вычислениях (сортировка, фильтрация, сложные математические операции)⁡
							// ✅ ⁡⁢⁢⁢При создании объектов или массивов, которые передаются в дочерние компоненты⁡
							// ✅ ⁡⁢⁢⁢Когда вычисления зависят от часто изменяющихся пропсов или состояния⁡
						}
						// 🔹 ⁡⁣⁢⁢КОГДА НЕ СТОИТ ИСПОЛЬЗОВАТЬ USEMEMO⁡
						{
							// ⛔ ⁡⁢⁣⁢Для простых вычислений (мемоизация может быть дороже самого вычисления)⁡
							// ⛔ ⁡⁢⁣⁢Для примитивных значений (числа, строки, булевы значения)⁡
							// ⛔ ⁡⁢⁣⁢Когда зависимости изменяются при каждом рендере⁡
						}
						// 🔹 ⁡⁣⁢⁢РАЗНИЦА МЕЖДУ USEMEMO И USECALLBACK⁡
						{
							// ⁡⁢⁣⁣USEMEMO									USECALLBACK⁡
							// ⁡⁣⁣⁢Мемоизирует результат вычислений			Мемоизирует саму функцию⁡
							// ⁡⁣⁣⁢Возвращает значение						Возвращает функцию⁡
							// ⁡⁣⁣⁢const value = useMemo(() => compute())	const func = useCallback(() => {})⁡
						}
						// 🔹 ⁡⁣⁢⁢ВАЖНЫЕ НЮАНСЫ⁡
						{
							// ✅ ⁡⁢⁢⁢useMemo не гарантирует, что вычисления не выполнятся — React может сбросить мемоизированное значение для освобождения памяти⁡
							// ✅ ⁡⁢⁢⁢Массив зависимостей должен быть полным — пропуск зависимостей может привести к багам⁡
							// ✅ ⁡⁢⁢⁢Не использовать для побочных эффектов — для эффектов используйте useEffect⁡
						}
						// 🔹 ⁡⁣⁢⁢ИТОГ⁡
						{
							// ⚪ ⁡⁣⁣⁢useMemo — мощный инструмент оптимизации, который следует использовать:⁡
							// ✅ ⁡⁢⁢⁢Для тяжелых вычислений⁡
							// ✅ ⁡⁢⁢⁢При работе с часто обновляемыми производными данными⁡
							// ✅ ⁡⁢⁢⁢Для стабилизации ссылок на объекты и массивы, передаваемые в дочерние компоненты⁡
							// ⚪ ⁡⁣⁣⁢Правильное применение useMemo может значительно улучшить производительность React-приложений, особенно при работе с большими объемами данных или сложными вычислениями.⁡
						}
					}
				}
				// ⚡ ⁡⁢⁣⁣useCallback - хук в React, предназначенный для мемоизации функций⁡
				{
					// ⭐ ⁡⁣⁣⁢useCallback — это хук в React, предназначенный для мемоизации функций. Он помогает избежать ненужного пересоздания функций при каждом рендере компонента, что важно для оптимизации производительности, особенно когда функции передаются в дочерние компоненты или используются в зависимостях эффектов (например, в useEffect).⁡
					{
						// 🔹 ⁡⁣⁢⁢КАК ЭТО РАБОТАЕТ?⁡
						{
							// ✅ ⁡⁢⁢⁢Мемоизация — сохранение ссылки на функцию между рендерами, если её зависимости не изменились.⁡
							// ✅ ⁡⁢⁢⁢Если зависимости изменились — функция пересоздается.⁡
							// ✅ ⁡⁢⁢⁢Если зависимости остались прежними — возвращается та же самая функция (та же ссылка в памяти).⁡
						}
						// 🔹 ⁡⁣⁢⁢СИНТАКСИС⁡
						{
							const memoizedCallback = useCallback(() => {
								// Логика функции
							}, [dependencies]); // Массив зависимостей
						}
						// 🔹 ⁡⁣⁢⁢ЗАЧЕМ ЭТО НУЖНО?⁡
						{
							// ✅ ⁡⁢⁢⁢Предотвращение лишних ререндеров дочерних компонентов Если вы передаете функцию в дочерний компонент, обернутый в React.memo, без useCallback дочерний компонент будет ререндериться при каждом изменении родителя, так как получает новую ссылку на функцию.⁡
							// ✅ ⁡⁢⁢⁢Оптимизация хуков с зависимостями Например, если функция используется в useEffect, её мемоизация предотвратит лишние вызовы эффекта.⁡
						}
						// 🔹 ⁡⁣⁢⁢ПРИМЕР 1: ПЕРЕДАЧА ФУНКЦИИ В ДОЧЕРНИЙ КОМПОНЕНТ⁡
						{
							// import React, { useState, useCallback } from "react";
							// Дочерний компонент с memo
							const ChildComponent = React.memo(({ onClick }) => {
								console.log("ChildComponent rerendered!");
								return <button onClick={onClick}>Click me</button>;
							});
							function ParentComponent() {
								const [count, setCount] = useState(0);
								// Без useCallback при каждом рендере создается новая функция
								const handleClick = useCallback(() => {
									setCount((prev) => prev + 1);
								}, []); // Зависимости пусты — функция создается один раз
								return (
									<div>
										<p>Count: {count}</p>
										<ChildComponent onClick={handleClick} />
									</div>
								);
							}
							// ⚪ ⁡⁣⁣⁢Без useCallback:⁡
							// ⚡ ⁡⁢⁣⁣При каждом изменении count родительский компонент ререндерится.⁡
							// ⚡ ⁡⁢⁣⁣handleClick пересоздается, поэтому ChildComponent тоже ререндерится.⁡
							// ⚪ ⁡⁣⁣⁢С useCallback:⁡
							// ⚡ ⁡⁢⁣⁣handleClick сохраняет ссылку между рендерами.⁡
							// ⚡ ⁡⁢⁣⁣ChildComponent не ререндерится, если пропсы не изменились.⁡
						}
						// 🔹 ⁡⁣⁢⁢ПРИМЕР 2: ИСПОЛЬЗОВАНИЕ В USEEFFECT⁡
						{
							function Component() {
								const [data, setData] = useState([]);
								// Без useCallback эффект срабатывает при каждом рендере
								const fetchData = useCallback(async () => {
									const result = await fetch("https://api.example.com/data");
									setData(await result.json());
								}, []); // Функция не зависит от внешних переменных

								useEffect(() => {
									fetchData();
								}, [fetchData]); // Эффект зависит от мемоизированной функции

								return <div>{/* ... */}</div>;
							}
							// ⚪ ⁡⁣⁣⁢Без useCallback массив зависимостей [fetchData] в useEffect будет каждый раз новым, что вызовет повторный запрос данных.⁡
							// ⚪ ⁡⁣⁣⁢С useCallback эффект сработает только при первом рендере.⁡
						}
						// 🔹 ⁡⁣⁢⁢КОГДА ИСПОЛЬЗОВАТЬ USECALLBACK?⁡
						{
							// ✅ ⁡⁢⁢⁢Когда передаете функции в дочерние компоненты, обернутые в React.memo.⁡
							// ✅ ⁡⁢⁢⁢Когда используете функции в useEffect или других хуках с зависимостями.⁡
							// ✅ ⁡⁢⁢⁢Функция является частью сложной логики, где важна стабильность ссылок.⁡
							// ❌ ⁡⁢⁣⁢Не используйте, если функция не передается в дочерние компоненты или не используется в хуках. Это может привести к избыточной сложности кода.⁡
						}
						// 🔹 ⁡⁣⁢⁢ЧЕМ USECALLBACK ОТЛИЧАЕТСЯ ОТ USEMEMO?⁡
						{
							// ✅ ⁡⁢⁢⁢useMemo мемоизирует значение (результат вычислений)
							const memoizedValue = useMemo(() => computeValue(a, b), [a, b]);
							// ✅ ⁡⁢⁢⁢useCallback мемоизирует саму функцию:⁡
							const memoizedFunc = useCallback(() => {
								doSomething(a, b);
							}, [a, b]);
						}
						// 🔹 ⁡⁣⁢⁢ВАЖНЫЕ НЮАНСЫ⁡
						{
							// ⚪ ⁡⁣⁣⁢Не злоупотребляйте useCallback Мемоизация требует памяти и вычислений. Используйте её только там, где это действительно улучшает производительность.⁡
							// ⚪ ⁡⁣⁣⁢Указывайте зависимости правильно Если функция использует переменные из области видимости, они должны быть в массиве зависимостей⁡
							const [count, setCount] = useState(0);
							const increment = useCallback(() => {
								setCount(count + 1); // Зависимость count обязательна!
							}, [count]);
						}
						// 🔹 ⁡⁣⁢⁢ИТОГ⁡
						{
							// ⚪ ⁡⁣⁣⁢useCallback — это инструмент для оптимизации, который следует применять осознанно. Он помогает:
							// ✅ ⁡⁢⁢⁢Избежать лишних ререндеров.⁡
							// ✅ ⁡⁢⁢⁢Управлять зависимостями в хуках.⁡
							// ✅ ⁡⁢⁢⁢Сохранять стабильность ссылок на функции.⁡
						}
					}
				}
			}
			// ✅ ⁡⁢⁢⁢ЛЕНИВАЯ ЗАГРУЗКА (LAZY LOADING)⁡
			{
				// ⚡ ⁡⁣⁣⁡⁢⁣⁣Динамическая загрузка компонентов только когда они нужны.⁡
				{
					const LazyComponent = React.lazy(() => import("./LazyComponent"));
					<Suspense fallback={<Spinner />}>
						<LazyComponent />
					</Suspense>;
				}
				// ⚡ ⁡⁢⁣⁡⁢⁣⁣LAZY LOADING (ленивая загрузка) — это техника, позволяющая откладывать загрузку не критичных ресурсов (например, компонентов) до момента их фактического использования. Это улучшает производительность приложений за счет:⁡
				{
					// ✅ ⁡⁢⁢⁢Уменьшения начального размера бандла.⁡
					// ✅ ⁡⁢⁢⁢Сокращения времени загрузки приложения.⁡
					// ⁡⁣⁢⁢КОГДА ИСПОЛЬЗОВАТЬ LAZY LOADING?⁡
					{
						// ⁡⁣⁣⁢- Крупные компоненты или страницы: Например, административная панель, доступная только авторизованным пользователям.⁡
						// ⁡⁣⁣⁢- Роутинг: Загрузка страниц только при переходе по определенному маршруту.⁡
						// ⁡⁣⁣⁢- Редко используемые элементы: Модальные окна, сложные виджеты, тяжелые библиотеки (например, графики).⁡
						// ⁡⁣⁣⁢- Оптимизация производительности: Когда нужно улучшить показатели Lighthouse (Time to Interactive, First Contentful Paint).⁡
					}
					// ⁡⁣⁢⁢КАК РАБОТАЕТ LAZY LOADING В REACT?⁡
					{
						// ⁡⁣⁣⁢React.lazy(): Функция для ленивой загрузки компонентов.⁡
						const MyComponent = React.lazy(() => import("./MyComponent"));
						// ⁡⁢⁢⁢- Принимает функцию, возвращающую Promise (обычно через динамический импорт import()).⁡
						// ⁡⁢⁢⁢- Поддерживает только компоненты с default-экспортом.⁡
						// ⁡⁣⁣⁢Suspense: Компонент для отображения запасного контента (fallback) во время загрузки.⁡
						<Suspense fallback={<div>Loading...</div>}>
							<MyComponent />
						</Suspense>;
					}
					// ⁡⁣⁢⁢ПРИМЕР ИСПОЛЬЗОВАНИЯ С REACT ROUTER⁡
					{
						// import { BrowserRouter as Router, Route, Switch } from "react-router-dom";
						// import React, { Suspense } from "react";

						const Home = React.lazy(() => import("./Home"));
						const About = React.lazy(() => import("./About"));
						const Contact = React.lazy(() => import("./Contact"));

						function App() {
							return (
								<Router>
									<Suspense fallback={<div>Loading page...</div>}>
										<Switch>
											<Route exact path="/" component={Home} />
											<Route path="/about" component={About} />
											<Route path="/contact" component={Contact} />
										</Switch>
									</Suspense>
								</Router>
							);
						}
					}
					// ⁡⁣⁢⁢ОБРАБОТКА ОШИБОК: ERROR BOUNDARIES⁡
					{
						// ⁡⁣⁣⁢Если загрузка компонента завершится ошибкой (например, из-за проблем с сетью), React выбросит исключение. Для перехвата таких ошибок используйте Error Boundaries⁡
						class ErrorBoundary extends React.Component {
							state = { hasError: false };

							static getDerivedStateFromError() {
								return { hasError: true };
							}
							componentDidCatch(error, errorInfo) {
								console.error("Error:", error, errorInfo);
							}
							render() {
								if (this.state.hasError) {
									return <h1>Уп! Компонент не удалось загрузить.</h1>;
								}
								return this.props.children;
							}
						}
						// Использование:
						<ErrorBoundary>
							<Suspense fallback={<div>Loading...</div>}>
								<MyComponent />
							</Suspense>
						</ErrorBoundary>;
					}
					// ⁡⁣⁢⁢НЮАНСЫ И ЛУЧШИЕ ПРАКТИКИ⁡
					{
						// ⁡⁣⁣⁢Серверный рендеринг (SSR): React.lazy не работает с SSR. Для Next.js или аналогичных фреймворков используйте библиотеки вроде @loadable/component.⁡
						// ⁡⁣⁣⁢Именованные экспорты: Если компонент экспортируется не через default, оберните его⁡
						const MyComponent = React.lazy(() =>
							import("./MyComponent").then((module) => ({
								default: module.MyComponent,
							})),
						);
						// ⁡⁣⁣⁢Оптимизация fallback: Используйте индикаторы загрузки (спиннеры, скелетоны) вместо простого текста⁡
						<Suspense fallback={<Spinner animation="border" />}>
							<MyComponent />
						</Suspense>;
						// ⁡⁣⁣⁢Разделение кода: Бандлеры (Webpack, Rollup) автоматически разбивают код на чанки при использовании import(). Проверьте настройки, чтобы чанды генерировались корректно.⁡
						// ⁡⁣⁣⁢Избегайте вложенности: Не используйте React.lazy внутри компонентов — только на верхнем уровне⁡
						{
							// Плохо:
							function App() {
								// const Component = React.lazy(...);
								return <Component />;
							}
							// Хорошо:
							// const Component = React.lazy(...);
							function App() {
								return <Component />;
							}
						}
					}
				}
			}
		}
		// 🔹 ⁡⁣⁡⁣⁢⁢КАК РАБОТАЕТ REACT ROUTER?⁡ ⁡⁣⁣⁢Подключение из react-router-dom встроенных компонентов <BrowserRouter/>, <Route/>, <Routes/>⁡
		{
			// import { BrowserRouter, Route, Routes } from "react-router-dom";
			function App() {
				return (
					<BrowserRouter>
						<Routes>
							<Route path="/" element={<Home />} />
							<Route path="/about" element={<About />} />
						</Routes>
					</BrowserRouter>
				);
			}
		}
		// 🔹 ⁡⁣⁢⁢ЧТО ТАКОЕ REACT FIBER?⁡ ⁡⁣⁣⁢Новая архитектура React для поддержки асинхронного рендеринга, приоритезации задач и инкрементальных обновлений⁡
		// 🔹 ⁡⁣⁢⁢PROP DRILLING В REACT?⁡ ⁡⁣⁣⁢Это ситуация, когда данные (пропсы) передаются через несколько уровней вложенных компонентов, даже если промежуточные компоненты их не используют напрямую.⁡
		{
			// ⚡ ⁡⁣⁢⁡⁢⁣⁣Некоторые проблемы, которые возникают из-за Prop Drilling:⁡
			{
				// ⛔ ⁡⁢⁣⁢Загрязнение компонентов — промежуточные компоненты получают ненужные пропсы.⁡
				// ⛔ ⁡⁢⁣⁢Сложность сопровождения — при изменении данных нужно править много компонентов.⁡
				// ⛔ ⁡⁢⁣⁢Уменьшение повторного использования — компоненты привязываются к пропсам, которые им не нужны.⁡
				// ⛔ ⁡⁢⁣⁢Вероятность багов — при изменении структуры сложно отследить, где используются пропсы.⁡
			}
			// ⚡ ⁡⁢⁣⁣Чтобы избежать Prop Drilling, можно использовать, например:⁡
			{
				// ✅ ⁡⁢⁢⁢React Context API. Контекст позволяет передавать данные на любой уровень вложенности, минуя промежуточные компоненты.⁡
				// ✅ ⁡⁢⁢⁢Глобальное состояние (Zustand, Redux, Jotai, Recoil). Подходит для более сложных приложений, где данных много и они нужны в разных частях.⁡
				// ✅ ⁡⁢⁢⁢Композицию (render props, slots, hooks). Иногда пропсы можно передать не напрямую, а с помощью функций или кастомных хуков, которые инкапсулируют логику.⁡
			}
		}
		// 🔹 ⁡⁣⁢⁢КАК ОПТИМИЗИРОВАТЬ ПРОИЗВОДИТЕЛЬНОСТЬ В REACT? (МЕМОИЗАЦИЯ, LAZY LOADING, VIRTUALIZATION, ИНСТРУМЕНТЫ)⁡
		{
			// ⁡⁣⁣⁢Оптимизация производительности в React помогает избежать лишних ререндеров, ускорить загрузку приложения и улучшить пользовательский опыт.⁡
			// ⁡⁣⁢⁢1. МЕМОИЗАЦИЯ КОМПОНЕНТОВ (REACT.MEMO)⁡
			{
				// ⚪ ⁡⁣⁣⁢Используйте для предотвращения ререндеров дочерних компонентов, если их пропсы не изменились.⁡
				{
					// import { memo } from "react";
					const UserItem = memo(({ user }) => {
						return <div>{user.name}</div>;
					});
					// Родительский компонент
					const UserList = ({ users }) => {
						return (
							<div>
								{users.map((user) => (
									<UserItem key={user.id} user={user} />
								))}
							</div>
						);
					};
				}
				// ⚪ ⁡⁣⁣⁢Сравнение пропсов(Второй агумент)⁡
				{
					// ✅ ⁡⁢⁢⁢По умолчанию: поверхностное сравнение (как Object.is для каждого пропа).⁡
					// ✅ ⁡⁢⁢⁢Кастомное сравнение: передайте функцию вторым аргументом.⁡
					{
						const areEqual = (prevProps, nextProps) => {
							// Возвращает true, если ререндер НЕ нужен
							return prevProps.value === nextProps.value;
						};
						const MemoizedComponent = React.memo(MyComponent, areEqual);
					}
				}
			}
			// ⁡⁣⁢⁢2. ХУКИ USECALLBACK И USEMEMO⁡
			{
				// ⁡⁣⁣⁢⚪ useCallback - сохраняет ссылку на функцию между рендерами.⁡
				{
					// ⚡ ⁡⁢⁣⁣Когда действительно нужен useCallback?⁡
					{
						// ⭐ ⁡⁣⁣⁢Когда функция передается в компонент с React.memo⁡
						// ⭐ ⁡⁣⁣⁢Когда функция является зависимостью других хуков (useEffect, useMemo и т.д.)⁡
						// ⭐ ⁡⁣⁣⁢Когда создание функции на каждом рендере является дорогой операцией (редкий случай)⁡
					}
					// ⚡ ⁡⁢⁣⁣Когда использовать useMemo?⁡
					{
						// ⁡⁢⁣⁣СИТУАЦИЯ	                        ПРИМЕР	                            АЛЬТЕРНАТИВА⁡
						// ⁡⁣⁣⁢Тяжёлые вычисления	            Сортировка, фильтрация	            Без оптимизации⁡
						// ⁡⁣⁣⁢Передача производных значений	Передача объектов/массивов детям	React.memo⁡
						// ⁡⁣⁣⁢Стабильность ссылок	            Функции, конфиги	                useCallback (для функций)⁡
					}
				}
				// ⁡⁣⁣⁢⚪ useMemo - кэширует результаты тяжелых вычислений.⁡
				{
					// import { useCallback, useMemo } from "react";
					const App = () => {
						const [count, setCount] = useState(0);
						// Кэшируем функцию
						const increment = useCallback(() => setCount((prev) => prev + 1), []);
						// Кэшируем вычисляемое значение
						const doubledCount = useMemo(() => count * 2, [count]);
						return (
							<div>
								<button onClick={increment}>Count: {count}</button>
								<div>Doubled: {doubledCount}</div>
							</div>
						);
					};
				}
			}
			// ⁡⁣⁢⁢3. ЛЕНИВАЯ ЗАГРУЗКА (LAZY LOADING)⁡
			{
				// ⁡⁣⁣⁢Динамически загружайте компоненты, которые не нужны сразу.⁡
				// import { lazy, Suspense } from "react";
				const HeavyComponent = lazy(() => import("./HeavyComponent"));
				const App = () => (
					<Suspense fallback={<div>Loading...</div>}>
						<HeavyComponent />
					</Suspense>
				);
			}
			// ⁡⁣⁢⁢4. ВИРТУАЛИЗАЦИЯ СПИСКОВ (VIRTUALIZATION)⁡
			{
				// ✅ ⁡⁢⁢⁢Используйте библиотеки вроде react-window для рендеринга больших списков.⁡
				{
					// import { FixedSizeList as List } from "react-window";
					const BigList = ({ items }) => (
						<List height={600} itemCount={items.length} itemSize={50} width={300}>
							{({ index, style }) => <div style={style}>{items[index].name}</div>}
						</List>
					);
				}
				// ✅ ⁡⁢⁢⁢Виртуализация - это техника, которая позволяет отображать только видимые элементы списка, существенно улучшая производительность при работе с большими объемами данных.⁡
				// ✅ ⁡⁣⁣⁡⁢⁢⁢Основные принципы виртуализации⁡
				{
					// ⚡ ⁡⁣⁣⁡⁣⁢⁡⁢⁣⁣Рендеринг только видимых элементов - отображаются только те элементы, которые попадают в область видимости пользователя⁡
					// ⚡ ⁡⁢⁣⁣Динамическая подгрузка - при прокрутке подгружаются новые элементы, а старые удаляются из DOM⁡
					// ⚡ ⁡⁢⁣⁣Фиксированный размер элементов или динамическое вычисление размеров⁡
				}
			}
			// ⁡⁣⁢⁢5. ОПТИМИЗАЦИЯ КОНТЕКСТА (CONTEXT OPTIMIZATION)⁡
			{
				// ⁡⁣⁣⁢Разделяйте контексты, чтобы избежать лишних ререндеров.⁡
				// Плохо: один контекст со всеми данными
				const AppContext = createContext();
				// Хорошо: раздельные контексты
				const UserContext = createContext();
				const ThemeContext = createContext();
				// Использование
				const App = () => (
					<UserContext.Provider value={user}>
						<ThemeContext.Provider value={theme}>
							<ChildComponent />
						</ThemeContext.Provider>
					</UserContext.Provider>
				);
			}
			// ⁡⁣⁢⁢6. ИЗБЕГАЙТЕ ИНЛАЙН-ОБЪЕКТОВ И ФУНКЦИЙ В JSX⁡
			{
				// ⁡⁣⁣⁢Каждый инлайн-объект/функция создает новую ссылку, что может вызвать ререндер.⁡
				// Плохо
				<Button onClick={() => handleClick(id)} style={{ color: "red" }} />;
				// Хорошо
				const buttonStyle = useMemo(() => ({ color: "red" }), []);
				const handleClick = useCallback((id) => {
					/* logic */
				}, []);
				<Button onClick={handleClick} style={buttonStyle} />;
			}
			// ⁡⁣⁢⁢7. ОПТИМИЗАЦИЯ ХУКОВ useEffect⁡
			{
				// ⁡⁣⁣⁢Указывайте точные зависимости и избегайте лишних вызовов.⁡
			}
			// ⁡⁣⁢⁢8. ПРАВИЛЬНЫЕ КЛЮЧИ В СПИСКАХ⁡
			{
				// ⁡⁣⁣⁢Используйте уникальные ключи для каждого элемента списка, чтобы избежать проблем с производительностью и неправильным отображением.⁡
			}
			// ⁡⁣⁢⁢9. ИНСТРУМЕНТЫ АНАЛИЗА ПРОИЗВОДИТЕЛЬНОСТИ⁡
			{
				// ⁡⁣⁣⁢Используйте React DevTools для анализа производительности компонентов и поиска узких мест.⁡
				// ⁡⁣⁣⁢-- React Profiler(Компонент React): позволяет отслеживать время рендеринга компонентов.⁡
				// ⁡⁣⁣⁢-- Lighthouse(Вкладка Chome): анализирует производительность приложения в целом.⁡
				// ⁡⁣⁣⁢-- Web Vitals(Библиотека Google): отслеживает основные метрики производительности.⁡
			}
			// ⁡⁣⁢⁢10. ОПТИМИЗАЦИЯ СОСТОЯНИЯ⁡
			{
				// ⁡⁣⁣⁢Поднимайте состояние только там, где это необходимо, и используйте менеджеры состояния (Redux, Zustand) для глобальных данных.⁡
			}
			// ⁡⁣⁢⁢--⁡ ⁡⁣⁢⁢React.memo:⁡ ⁡⁣⁣⁢Мемоизация функциональных компонентов.⁡
			const MemoizedComponent = React.memo(MyComponent);
			const MyComponent = React.memo(function MyComponent(props) {
				// рендеринг только если пропсы изменились
			});
			// ⁡⁣⁢⁢-- useMemo и useCallback:⁡ ⁡⁣⁣⁢Кэширование значений и функций.⁡
			const memoizedValue = useMemo(() => computeValue(a, b), [a, b]);
			const memoizedCallback = useCallback(() => doSomething(a), [a]);
			const memoizedCallbackFun = useCallback(() => {
				doSomething(a, b);
			}, [a, b]); // Зависимости
		}
		// 🔹 ⁡⁣⁢⁢BUNDLE ANALYZER(инструмент, плагин анализа размера бандлов)⁡
		{
			// ⁡⁣⁣⁢Bundle Analyzer — это инструмент для визуализации структуры и размера JavaScript-бандлов (собранных файлов проекта). Он помогает⁡
			// ⁡⁢⁢⁢- Увидеть, какие модули занимают больше всего места.⁡
			// ⁡⁢⁢⁢- Найти ненужные или дублирующиеся зависимости.⁡
			// ⁡⁢⁢⁢- Оптимизировать производительность приложения.⁡
			// ⁡⁣⁣⁢Как это работает?⁡
			// ⁡⁢⁢⁢- После сборки проекта Bundle Analyzer генерирует интерактивную карту бандла в виде дерева или диаграммы, где размер каждого элемента соответствует его "весу" в бандле.⁡
		}
        // 🔹 ⁡⁣⁢⁢ЧТО ТАКОЕ REACT PORTALS?⁡ ⁡⁣⁣⁢Это механизм в React, который позволяет рендерить дочерние элементы в DOM-узел, находящийся вне иерархии родительского компонента. Это особенно полезно для работы с модальными окнами, всплывающими подсказками, уведомлениями и другими элементами, которые должны быть визуально "отвязаны" от текущего контекста (например, чтобы избежать проблем с z-index, overflow: hidden или семантической вложенностью).⁡
        {
            // ✅ ⁡⁢⁢⁢Когда применять React Portals?⁡
            {
				// ⚡ ⁡⁢⁣⁣Модальные окна и диалоги:⁡
				{
					// ⭐ ⁡⁣⁣⁢Чтобы избежать проблем с z-index и перекрытием другими элементами.⁡
					// ⭐ ⁡⁣⁣⁢Когда родительский компонент имеет overflow: hidden или position: relative.⁡
				}
				// ⚡ ⁡⁢⁣⁣Всплывающие подсказки (Tooltips) и выпадающие меню:⁡
				{
					// ⭐ ⁡⁣⁣⁢Если они должны выходить за границы контейнера.⁡
				}
                // ⚡ ⁡⁢⁣⁣Виджеты поверх всего интерфейса:⁡
                {
                    // ⭐ ⁡⁣⁣⁢Например, загрузчики, уведомления, боковые панели.⁡
                }
                // ⚡ ⁡⁢⁣⁣Оптимизация производительности:⁡
                {
                    // ⭐ ⁡⁣⁣⁢Изоляция тяжелых компонентов в отдельном DOM-узле.⁡
                }
			}
			// ✅ ⁡⁢⁢⁢Пример⁡
			{
				// <div id="root"></div>
				// <div id="modal-root"></div> <!-- Контейнер для порталов -->
				// import React, { useEffect } from "react";
				// import ReactDOM from "react-dom";
				const Modal = ({ children }) => {
					// Создаем элемент для портала (или используем существующий)
					const portalElement = document.getElementById("modal-root");
					// Если портальный элемент не существует, можно создать его динамически
					useEffect(() => {
						if (!portalElement) {
							const el = document.createElement("div");
							el.id = "modal-root";
							document.body.appendChild(el);
						}
					}, [portalElement]);

					if (!portalElement) return null;

					return ReactDOM.createPortal(
						<div className="modal">{children}</div>,
						portalElement,
					);
				};
				// Использование компонента:
				const App = () => (
					<div>
						<Modal>Это модальное окно!</Modal>
					</div>
				);
			}
		}
	}
	// 🔳 ⁡⁢⁣⁣REACT-ПРИЛОЖЕНИЯ В БРАУЗЕРЕ⁡
	{
		// 🔹 ⁡⁣⁢⁢КАК БРАУЗЕР ЧИТАЕТ REACT-ПРИЛОЖЕНИЕ?⁡
		{
			// ✅ ⁡⁢⁢⁢Основные инструменты:⁡
			{
				// ⚡ ⁡⁢⁣⁣Babel (наиболее популярный)⁡
				// ⚡ ⁡⁢⁣⁣TypeScript (если используется TS)⁡
				// ⚡ ⁡⁢⁣⁣SWC (более современная и быстрая альтернатива)⁡
			}
			// ✅ ⁡⁢⁢⁢После транспиляции происходит сборка проекта с помощью:⁡
			{
				// ⚡ ⁡⁢⁣⁣Webpack (наиболее популярный)⁡
				// ⚡ ⁡⁢⁣⁣Vite (более современная альтернатива)⁡
				// ⚡ ⁡⁢⁣⁣Parcel (альтернатива)⁡
				// ⚡ ⁡⁢⁣⁣Rollup (для библиотек)⁡
			}
			// ✅ ⁡⁣⁣⁡⁢⁢⁢Браузеры никогда не видят исходный JSX — они получают уже преобразованный JavaScript. Весь "магический" процесс обработки JSX происходит на этапе разработки и сборки проекта.⁡
		}
		// 🔹 ⁡⁢⁣⁡⁣⁢⁢РАБОТА REACT-ПРИЛОЖЕНИЯ В БРАУЗЕРЕ:⁡
		{
			// ⁡⁣⁣⁢1. Браузер загружает HTML-документ.⁡
			// ⁡⁣⁣⁢2. HTML-документ содержит ссылку на JavaScript-файл (например, bundle.js).⁡
			// ⁡⁣⁣⁢3. Браузер загружает и выполняет JavaScript-файл.⁡
			// ⁡⁣⁣⁢4. JavaScript создает виртуальный DOM с помощью React.⁡
			// ⁡⁣⁣⁢5. React сравнивает виртуальный DOM с реальным DOM и обновляет его при необходимости.⁡
			// ⁡⁣⁣⁢6. Браузер отображает обновленный DOM на экране.⁡
			// ⁡⁢⁢⁢------------------------- СОВСЕМ ПОДРОБНО --------------------------
			// ⁡⁣⁢⁢1. ЗАГРУЗКА СТРАНИЦЫ (INITIAL RENDER)⁡
			{
				// ⁡⁣⁣⁢Когда React-приложение загружается в браузере, происходит первичный рендер:⁡
				// ⁢⁣⁡⁢⁣⁡⁢⁣⁣1. Монтирование компонентов⁡
				//	 ⁡⁣⁣⁢- React начинает с корневого компонента (обычно App).⁡
				//	 ⁡⁣⁣⁢- Постепенно рендерит все дочерние компоненты в дереве⁡
				//   ⁡⁣⁣⁢- Вызывает их функции (для функциональных компонентов) или методы render (для классовых)⁡
				// ⁡⁢⁣⁣2. Создание Virtual DOM⁡
				//   ⁡⁣⁣⁢- React строит виртуальное дерево (Virtual DOM) на основе JSX.⁡
				//   ⁡⁣⁣⁢- Сравнивает его с текущим DOM (при первом рендере DOM пуст).⁡
				// ⁡⁢⁣⁣3. Рендер в реальный DOM⁡
				//   ⁡⁣⁣⁢- React применяет минимальные изменения для синхронизации Virtual DOM с реальным DOM.⁡
				// ⁡⁢⁣⁣4. Вызов useEffect после рендера⁡
				//   ⁡⁣⁣⁢- После отрисовки компонента выполняются эффекты (useEffect), если их зависимости изменились.⁡
				// ⁡⁣⁢⁢Пример:⁡
				{
					// import { useState, useEffect } from "react";
					function App() {
						const [count, setCount] = useState(0);

						useEffect(() => {
							console.log("Компонент смонтирован!");
						}, []); // Пустой массив зависимостей = только при монтировании
						return <button onClick={() => setCount(count + 1)}>Count: {count}</button>;
					}
				}
				// ⁡⁣⁢⁢Что произойдет:⁡
				{
					// ⁡⁣⁣⁢Рендерится App.⁡
					// ⁡⁣⁣⁢Вызывается useEffect (после рендера).⁡
					// ⁡⁣⁣⁢В консоль выводится "Компонент смонтирован!⁡".
				}
			}
			// ⁡⁣⁢⁢2. ИЗМЕНЕНИЕ СОСТОЯНИЯ (STATE UPDATE)⁡
			{
				// ⁡⁣⁣⁢Когда состояние (state) или пропсы (props) меняются, React запускает ререндер:⁡
				// ⁡⁢⁣⁣1. Изменение состояния⁡
				// ⁡⁣⁣⁢- Например, вызов setCount(1) в примере выше.⁡
				// ⁡⁢⁣⁣2. Планирование ререндера⁡
				// ⁡⁣⁣⁢- React помечает компонент как "грязный" (dirty) и ставит его в очередь обновлений.⁡
				// ⁡⁢⁣⁣3. Reconciliation (Сверка Virtual DOM)⁡
				// ⁡⁣⁣⁢- React сравнивает новый Virtual DOM с предыдущим.⁡
				// ⁡⁣⁣⁢- Определяет, какие части DOM нужно обновить (алгоритм Diffing).⁡
				// ⁡⁢⁣⁣4. Фиксация изменений (Commit Phase)⁡
				// ⁡⁣⁣⁢- React вносит минимальные изменения в реальный DOM.⁡
				// ⁡⁢⁣⁣5. Запуск useEffect (если зависимости изменились)⁡
				// ⁡⁣⁣⁢- Если в useEffect указаны зависимости ([count]), он выполнится после рендера.⁡
				// ⁡⁣⁢⁢Пример:⁡
				{
					useEffect(() => {
						console.log("Count изменился:", count);
					}, [count]); // Зависимость от count
				}
				// ⁡⁣⁢⁢Что произойдет при клике:⁡
				{
					// ⁡⁣⁣⁢-- setCount → состояние обновляется.⁡
					// ⁡⁣⁣⁢-- React ререндерит компонент.⁡
					// ⁡⁣⁣⁢-- После рендера проверяет зависимости useEffect:⁡
					//     ⁡⁢⁣⁣- Если count изменился → эффект выполняется.⁡
					//     ⁡⁢⁣⁣- Если нет → пропускается.⁡
				}
			}
			// ⁡⁣⁢⁢3. РАБОТА USEEFFECT()⁡
			{
				// ⁡⁣⁣⁢useEffect выполняется после рендера и зависит от массива зависимостей (deps):⁡
				// ⁡⁢⁢⁢Три варианта поведения:⁡
				//⁡⁢⁣⁣ ЗАВИСИМОСТИ (DEPS)			КОГДА ВЫПОЛНЯЕТСЯ?						ПРИМЕР ИСПОЛЬЗОВАНИЯ⁡
				// ⁡⁣⁣⁢Нет массива					После каждого рендера					Логирование, анимации⁡
				// ⁡⁣⁣⁢[] (пустой)					Только при монтировании					Загрузка данных (fetch), подписки⁡
				// ⁡⁣⁣⁢[dep1, dep2]					При изменении dep1 или dep2				Обновление данных при изменении props⁡
				// ⁡⁣⁢⁢Примеры:⁡
				// ⁡⁣⁣⁢1. Без зависимостей (выполняется при каждом рендере)⁡
				// ⁡⁣⁣⁢2. Только при монтировании (componentDidMount)⁡
				// ⁡⁣⁣⁢3. При изменении зависимостей (componentDidUpdate)⁡
				// ⁡⁣⁣⁢4. Очистка эффекта (componentWillUnmount)⁡
				useEffect(() => {
					const timer = setInterval(() => {}, 1000);
					return () => clearInterval(timer); // Очистка при размонтировании
				}, []);
			}
			// ⁡⁣⁢⁢4. РАЗМОНТИРОВАНИЕ (UNMOUNT)⁡
			{
				// ⁡⁣⁣⁢Когда компонент удаляется из DOM (например, при условном рендеринге):⁡
				// ⁡⁣⁢⁢Этапы:⁡
				// ⁡⁣⁣⁢1. React вызывает функцию очистки useEffect (если она есть).⁡
				// ⁡⁣⁣⁢2. Компонент удаляется из DOM.⁡
				// ⁡⁣⁢⁢Пример:⁡
				{
					const [showTimer, setShowTimer] = useState(true);
					return (
						<div>
							{showTimer && <Timer />}
							<button onClick={() => setShowTimer(false)}>Удалить таймер</button>
						</div>
					);
					// Timer.js
					useEffect(() => {
						const timer = setInterval(() => console.log("Tick!"), 1000);
						return () => clearInterval(timer); // Очистка при размонтировании
					}, []);
				}
				// ⁡⁣⁢⁢Что произойдет:⁡
				// ⁡⁣⁣⁢1. При клике showTimer становится false.⁡
				// ⁡⁣⁣⁢2. React вызывает return () => clearInterval(timer) из useEffect.⁡
				// ⁡⁣⁣⁢3. Компонент Timer удаляется из DOM.⁡
			}
			// ⁡⁣⁢⁢ИТОГ: ПОЛНЫЙ ЦИКЛ РАБОТЫ REACT⁡
			// ⁡⁣⁣⁢1. Монтирование → рендер → useEffect (если []).⁡
			// ⁡⁣⁣⁢2. Обновление → изменение состояния → ререндер → useEffect (если зависимости изменились).⁡
			// ⁡⁣⁣⁢3. Размонтирование → вызов cleanup-функции useEffect.⁡
		}
	}
	// 🔳 ⁡⁢⁣⁣КОМПОНЕНТЫ (CONTROLLED, UNCONTROLLED, HOC, KEYS)⁡
	{
		// 🔹 ⁡⁣⁢⁢ОБЪЯСНИТЕ ЖИЗНЕННЫЙ ЦИКЛ КОМПОНЕНТА:⁡
		{
			// ⁡⁣⁢⁢Для классовых компонентов:⁡
			// ⁡⁣⁣⁢- componentDidMount(): После монтирования.⁡
			// ⁡⁣⁣⁢- componentDidUpdate(): После обновления.⁡
			// ⁡⁣⁣⁢- componentWillUnmount(): Перед удалением.⁡
			// ⁡⁣⁢⁢Для функциональных компонентов:⁡
			// ⁡⁣⁣⁢Используется хук useEffect:⁡
			useEffect(() => {
				// Аналог componentDidMount после монтирования
				return () => {
					// Аналог componentWillUnmount перед удалением
				};
			}, [dependencies]); // Аналог componentDidUpdate при изменении зависимостей
			// ⁡⁣⁢⁢-- useEffect без зависимостей: выполняется при каждом рендере.⁡
			// ⁡⁣⁢⁡⁣⁢⁢-- useEffect с пустым массивом зависимостей: выполняется только при монтировании.⁡
		}
		// 🔹 ⁡⁣⁢⁢В ЧЕМ РАЗНИЦА МЕЖДУ УПРАВЛЯЕМЫМИ (CONTROLLED) И НЕУПРАВЛЯЕМЫМИ (UNCONTROLLED) КОМПОНЕНТАМИ?⁡
		{
			// ⚡ ⁡⁢⁣⁣Управляемые:⁡ ⁡⁣⁣⁢Значение контролируется React через state (например, <input value={value} onChange={...}>).⁡
			// ⚡ ⁡⁢⁣⁣Неуправляемые:⁡ ⁡⁣⁣⁢Значение берется напрямую из DOM (через useRef, например: const inputRef = useRef(); <input ref={inputRef} />).⁡
		}
		// 🔹 ⁡⁣⁢⁢КОМПОНЕНТ ВЫСШЕГО ПОРЯДКА (HOC — Higher-Order Component) в React⁡
		{
			// ⚡ ⁡⁢⁣⁣HOC — это функция, которая принимает компонент и возвращает новый компонент с дополнительной логикой. Это паттерн для повторного использования кода, аналог декораторов в других языках. Сейчас можно обходится хуками⁡
			{
				// ✅ ⁡⁢⁢⁢Добавлять общую функциональность (логика, стили, данные) к разным компонентам.⁡
				// ✅ ⁡⁢⁢⁢Избегать дублирования кода (DRY — Don’t Repeat Yourself).⁡
			}
			// ⚡ ⁡⁣⁣⁡⁢⁣⁣Как работает HOC?⁡
			{
				// ✅ ⁡⁢⁢⁢Принимает компонент: const EnhancedComponent = hoc(BaseComponent).⁡
				// ✅ ⁡⁢⁢⁢Возвращает новый компонент с дополнительными пропсами, состоянием или поведением.⁡
				// ✅ ⁡⁢⁢⁢Пример структуры⁡
				{
					const withHOC = (WrappedComponent) => {
						return function EnhancedComponent(props) {
							// Добавляемая логика
							return <WrappedComponent {...props} extraProp="value" />;
						};
					};
				}
			}
		}
		// 🔹 ⁡⁣⁢⁢ЧТО ТАКОЕ КЛЮЧИ (KEYS) И ЗАЧЕМ ОНИ НУЖНЫ?⁡ ⁡⁣⁣⁢Ключи помогают React идентифицировать элементы списка при изменениях. Они должны быть уникальными среди соседей, чтобы избежать проблем с производительностью и неправильным отображением.⁡
		{
			items.map((item) => <li key={item.id}>{item.name}</li>);
		}
	}
    // 🔳 ⁡⁢⁣⁣ХУКИ (useRef, key, useEffect)⁡
    {
		// 🔹 ⁡⁣⁢⁢ОСНОВНЫЕ ХУКИ (BASIC HOOKS)⁡
		{
			// ⚪ ⁡⁣⁣⁢useState() – управление состоянием компонента.⁡
			// ⚪ ⁡⁣⁣⁢useEffect() – выполнение побочных эффектов (аналог componentDidMount, componentDidUpdate, componentWillUnmount).
			// ⚪ ⁡⁣⁣⁢useContext() – доступ к контексту React.⁡
		}
		// 🔹 ⁡⁣⁢⁡⁣⁢⁢ДОПОЛНИТЕЛЬНЫЕ ХУКИ (ADDITIONAL HOOKS)(useLayoutEffect(), useRef())⁡
		{
			// ⚪ ⁡⁣⁣⁢useReducer() – управление сложным состоянием через редюсер (аналог Redux).⁡
			// ⚪ ⁡⁣⁣⁢useSyncExternalStore() (React 18+) – подписка на внешние хранилища.⁡
			// ⚪ ⁡⁣⁣⁢useMemo() – мемоизация вычислений.⁡
			// ⚪ ⁡⁣⁣⁢useCallback() – мемоизация функций (чтобы избежать лишних ререндеров).⁡
			// ⚪ ⁡⁣⁣⁢useRef() – создание мутируемого объекта, сохраняемого между рендерами.⁡
			// ⚪ ⁡⁣⁣⁡⁣⁣⁢useImperativeHandle() – настройка экземпляра компонента, который передается через ref, какие методы и свойства дочернего компонента будут доступны родительскому при использовании ссылки (ref).⁡
			// ⚪ ⁡⁣⁣⁢useLayoutEffect() – аналог useEffect, но выполняется синхронно после рендера, но до отрисовки в браузере.⁡
			// ⚪ ⁡⁣⁣⁢useDebugValue() – отображение кастомных лейблов в React DevTools.⁡
		}
		// 🔹 ⁡⁣⁢⁢ХУКИ ДЛЯ CONCURRENT MODE (REACT 18+)⁡
		{
			// ⚪ ⁡⁣⁣⁢useTransition() – помечает обновления состояния как неблокирующие.⁡
			// ⚪ ⁡⁣⁣⁢useDeferredValue() – откладывает обновление значения (например, для дебаунсинга).⁡
			// ⚪ ⁡⁣⁣⁢useId() – генерация уникальных ID (полезно для aria-* атрибутов).⁡
		}
		// 🔹 ⁡⁣⁢⁢СТОРОННИЕ ХУКИ⁡
		{
			// ⚪ ⁡⁣⁣⁢useDispatch(), useSelector() (Redux) – работа с хранилищем.⁡
			// ⚪ ⁡⁣⁣⁢useQuery() (React Query) – управление запросами.⁡
			// ⚪ ⁡⁣⁣⁢useParams(), useNavigate() (React Router) – навигация.⁡
			// ⚪ ⁡⁣⁣⁢useForm() (React Hook Form) – управление формами.⁡
		}
		// 🔹 ⁡⁣⁢⁢ПРАВИЛА ХУКОВ В REACT — ЭТО ПРИНЦИПЫ ИХ ИСПОЛЬЗОВАНИЯ, КОТОРЫЕ НЕОБХОДИМО СОБЛЮДАТЬ, ЧТОБЫ REACT КОРРЕКТНО ОТСЛЕЖИВАЛ И ВЫЗЫВАЛ ХУКИ.⁡
		{
			// ✅ ⁡⁢⁢⁢Вызывать хуки только на верхнем уровне. Нельзя вызывать хуки внутри условий, циклов или вложенных функций.⁡
			// ✅ ⁡⁢⁢⁢Вызывать хуки только в функциональных компонентах или внутри кастомных хуков. Нельзя вызывать хуки в обычных функциях, обработчиках, условиях вне компонента.⁡
			// ✅ ⁡⁢⁢⁢Хуки должны вызываться в одинаковом порядке при каждом рендере. Нельзя менять порядок хуков при разных условиях. Это приводит к неправильному сопоставлению значений.⁡
		}
		// ⁡⁣⁢🔹 ⁡⁣⁢⁢ОСНОВНЫЕ СЛУЧАИ ИСПОЛЬЗОВАНИЯ useRef()⁡
		{
			// ⁡⁢⁢⁢1. Доступ к DOM-элементам⁡
			// ⁡⁣⁣⁢- Когда использовать: Когда нужно напрямую взаимодействовать с DOM-элементом (фокусировка, измерение размеров, анимации)⁡.
			{
				function TextInputWithFocus() {
					const inputRef = useRef(null);

					const handleClick = () => {
						inputRef.current.focus(); // Устанавливаем фокус на инпут
					};

					return (
						<>
							<input ref={inputRef} type="text" />
							<button onClick={handleClick}>Фокусировать инпут</button>
						</>
					);
				}
			}
			// ⁡⁢⁢⁢2. Хранение изменяемых значений без перерисовки⁡
			// ⁡⁣⁣⁢- Когда использовать: Для значений, которые должны сохраняться между рендерами, но не должны вызывать обновление компонента.⁡
			{
				function TimerComponent() {
					const [count, setCount] = useState(0);
					const intervalRef = useRef(); // Храним ID интервала

					useEffect(() => {
						intervalRef.current = setInterval(() => {
							setCount((prev) => prev + 1);
						}, 1000);

						return () => clearInterval(intervalRef.current);
					}, []);

					const stopTimer = () => {
						clearInterval(intervalRef.current); // Останавливаем таймер
					};

					return (
						<div>
							<p>Счетчик: {count}</p>
							<button onClick={stopTimer}>Остановить</button>
						</div>
					);
				}
			}
			// ⁡⁢⁢⁢3. Сохранение предыдущих значений⁡
			// ⁡⁣⁣⁢- Когда использовать: Когда нужно сравнить текущее значение с предыдущим⁡
			{
				function CounterWithPrevious() {
					const [count, setCount] = useState(0);
					const prevCountRef = useRef();

					useEffect(() => {
						prevCountRef.current = count; // Сохраняем текущее значение в ref
					}, [count]); // Обновляем при изменении count

					const prevCount = prevCountRef.current;

					return (
						<div>
							<p>
								Текущее: {count}, Предыдущее: {prevCount}
							</p>
							<button onClick={() => setCount((c) => c + 1)}>Увеличить</button>
						</div>
					);
				}
			}
			// ⁡⁢⁢⁢4. Хранение сложных структур данных⁡
			// ⁡⁣⁣⁢- Когда использовать: Для мутабельных структур, которые не должны вызывать ререндер.⁡
			{
				function CanvasDrawing() {
					const canvasRef = useRef(null);
					const pathsRef = useRef([]); // Храним массив путей
					const handleMouseMove = (e) => {
						if (canvasRef.current) {
							const point = { x: e.clientX, y: e.clientY };
							pathsRef.current.push(point); // Добавляем точку без ререндера
							drawPath(); // Рисуем на canvas
						}
					};
					const drawPath = () => {
						const ctx = canvasRef.current.getContext("2d");
						// Рисуем все точки из pathsRef
					};
					return <canvas ref={canvasRef} onMouseMove={handleMouseMove} />;
				}
			}
			// ⁡⁢⁢⁢5. Оптимизация производительности⁡
			// ⁡⁣⁣⁢- Когда использовать: Для сохранения стабильных ссылок на функции или компоненты.⁡
			{
				function HeavyComponent() {
					const heavyCalculationRef = useRef(() => {
						// Сложные вычисления
						return computeExpensiveValue();
					});
					const value = heavyCalculationRef.current(); // Используем сохраненную функцию
					return <div>{value}</div>;
				}
			}
			// ⁡⁢⁢⁢6. Интеграция с внешними библиотеками⁡
			{
				function VideoPlayer() {
					const videoRef = useRef(null);

					useEffect(() => {
						// Инициализация сторонней библиотеки с DOM-элементом
						const player = new VideoLibrary(videoRef.current);
						return () => player.dispose(); // Очистка при размонтировании
					}, []);
					return <video ref={videoRef} />;
				}
			}
			// ⁡⁢⁢⁢7. Измерение размеров элемента⁡
			{
				function MeasureExample() {
					const divRef = useRef(null);
					const [dimensions, setDimensions] = useState({ width: 0, height: 0 });

					useEffect(() => {
						if (divRef.current) {
							setDimensions({
								width: divRef.current.offsetWidth,
								height: divRef.current.offsetHeight,
							});
						}
					}, []);

					return (
						<div ref={divRef}>
							Размеры: {dimensions.width} x {dimensions.height}
						</div>
					);
				}
			}
			// ⁡⁢⁢⁢8. Сохранение предыдущих пропсов⁡
			{
				function ComponentWithProps({ value }) {
					const prevValueRef = useRef();
					useEffect(() => {
						prevValueRef.current = value;
					}, [value]);
					const prevValue = prevValueRef.current;
					return (
						<div>
							Текущее: {value}, Предыдущее: {prevValue}
						</div>
					);
				}
			}
			// ⁡⁢⁢⁢ЗАКЛЮЧЕНИЕ⁡
			{
				// ⁡⁣⁣⁢- Работы с DOM-элементами⁡
				// ⁡⁣⁣⁢- Хранения мутабельных значений между рендерами⁡
				// ⁡⁣⁣⁢- Сохранения предыдущих значений⁡
				// ⁡⁣⁣⁢- Интеграции с императивными API⁡
				// ⁡⁣⁣⁢- Оптимизации производительности⁡
				// ⁡⁣⁣⁢Помните, что useRef не должен заменять useState, когда вам нужно реагировать на изменения данных и обновлять интерфейс. Используйте его осознанно для решения конкретных задач, где требуется мутабельность без ререндеров.⁡
			}
		}
		// 🔹 ⁡⁣⁢⁢ОСНОВНЫЕ СЛУЧАИ ИСПОЛЬЗОВАНИЯ key={}⁡
		{
			// ⁡⁣⁣⁢Атрибут key — специальный проп, который помогает React идентифицировать элементы в коллекциях. Он критически важен для корректного обновления компонентов и оптимизации производительности.⁡
			// ⁡⁢⁢⁢1. Рендеринг списков⁡
			// ⁡⁣⁣⁢- При изменении порядка элементов React переставляет DOM-узлы вместо пересоздания⁡
			// ⁡⁣⁣⁢- Состояние инпутов сохраняется корректно при изменениях списка⁡
			{
				const users = [
					{ id: 1, name: "Alice" },
					{ id: 2, name: "Bob" },
				];
				function UserList() {
					return (
						<ul>
							{users.map((user) => (
								<li key={user.id}>
									{" "}
									{/* Уникальный идентификатор */}
									{user.name}
									<input type="text" placeholder="Комментарий" />
								</li>
							))}
						</ul>
					);
				}
			}
			// ⁡⁢⁢⁢2. Принудительное пересоздание компонента⁡
			// ⁡⁣⁣⁢Когда использовать: Когда нужно сбросить внутреннее состояние компонента при изменении пропсов.⁡
			// ⁡⁣⁣⁢При изменении userId⁡
			// ⁡⁢⁣⁣-- Старая форма полностью уничтожается⁡
			// ⁡⁢⁣⁣-- Создается новая форма с чистым состоянием⁡
			{
				function EditForm({ userId }) {
					const [comment, setComment] = useState("");
					return (
						<div key={userId}>
							{" "}
							{/* При смене userId компонент пересоздается */}
							<input value={comment} onChange={(e) => setComment(e.target.value)} />
						</div>
					);
				}
			}
			// ⁡⁢⁢⁢3. Работа с анимациями⁡
			// ⁡⁣⁣⁢Без key: React не сможет определить новый элемент, и анимация добавления не сработает.⁡
			{
				function TodoList({ items }) {
					return (
						<TransitionGroup>
							{items.map((item) => (
								<CSSTransition
									key={item.id} /* Анимация сработает правильно */
									timeout={500}
									classNames="fade"
								>
									<div className="todo-item">{item.text}</div>
								</CSSTransition>
							))}
						</TransitionGroup>
					);
				}
			}
			// ⁡⁢⁢⁢4. Неконтролируемые компоненты⁡
			// ⁡⁣⁣⁢Пример сброса файлового инпута⁡
			{
				function FileUploader({ resetFlag }) {
					return (
						<input
							type="file"
							key={resetFlag ? "reset" : "normal"} /* Пересоздаем инпут */
						/>
					);
				}
			}
			// ⁡⁣⁣⁢Эффект: При изменении resetFlag файловый инпут полностью сбрасывается.⁡
		}
		// ⁡⁣⁢⁢🔹 ПОДРОБНЫЙ ЦИКЛ РАБОТЫ useEffect()⁡
		{
			// ⁡⁣⁣⁢useEffect — это хук в React, предназначенный для выполнения побочных эффектов в функциональных компонентах. Он объединяет функциональность методов жизненного цикла классовых компонентов (componentDidMount, componentDidUpdate, componentWillUnmount), но работает более гибко.⁡
			// ⁡⁢⁢⁢1. МОНТИРОВАНИЕ КОМПОНЕНТА (MOUNT)⁡
			// ⁡⁣⁣⁡⁣⁣⁢- Когда вызывается: После первого рендера компонента. React выполняет рендер компонента (создает Virtual DOM).⁡
			// ⁡⁣⁣⁢- Аналог в классах: componentDidMount.⁡
			useEffect(() => {
				console.log("Компонент смонтирован");
				// Запрос данных, подписка на события
			}, []); // Пустой массив зависимостей
			// ⁡⁢⁢⁢2. ОБНОВЛЕНИЕ КОМПОНЕНТА (UPDATE)⁡
			// ⁡⁣⁣⁢- Когда вызывается React проверяет, изменились ли зависимости по сравнению с предыдущим рендером.⁡
			// ⁡⁢⁣⁣-- При каждом рендере (если зависимости не указаны).⁡
			// ⁡⁢⁣⁡⁢⁣⁣-- Только при изменении значений в массиве зависимостей. Если зависимости изменились (или отсутствуют), выполняется тело эффекта.⁡
			// ⁡⁣⁣⁢- Аналог в классах: componentDidUpdate.⁡
			const [count, setCount] = useState(0);
			useEffect(() => {
				console.log("Count изменился:", count);
				// Обновление данных при изменении count
			}, [count]); // Зависимость от count
			// ⁡⁢⁢⁢3. РАЗМОНТИРОВАНИЕ КОМПОНЕНТА (UNMOUNT)⁡
			// ⁡⁣⁣⁡⁣⁣⁢- Когда вызывается: Перед удалением компонента из DOM. Перед следующим выполнением эффекта или при размонтировании вызывается функция очистки.⁡
			// ⁡⁣⁣⁢- Аналог в классах: componentWillUnmount.⁡
			useEffect(() => {
				const timer = setInterval(() => {
					console.log("Таймер работает");
				}, 1000);

				return () => {
					clearInterval(timer); // Очистка при размонтировании
					console.log("Компонент размонтирован");
				};
			}, []);
			// ⁡⁢⁢⁢ПУСТОЙ МАССИВ []⁡
			// ⁡⁣⁣⁢- Эффект выполняется только один раз после монтирования.⁡
			// ⁡⁣⁣⁢- Используется для⁡
			// ⁡⁢⁣⁣-- Загрузки начальных данных.⁡
			// ⁡⁢⁣⁣-- Подписки на глобальные события.⁡
			useEffect(() => {
				fetch("/api/data").then((response) => response.json());
			}, []);
			// ⁡⁢⁢⁢МАССИВ С ЗАВИСИМОСТЯМИ [DEP1, DEP2]⁡
			// ⁡⁣⁣⁢- Эффект выполняется при изменении любой из зависимостей.⁡
			// ⁡⁢⁢⁢ОТСУТСТВИЕ МАССИВА ЗАВИСИМОСТЕЙ⁡
			// ⁡⁣⁣⁢- Эффект выполняется после каждого рендера.⁡
			// ⁡⁣⁣⁢- Осторожно: Может привести к бесконечным циклам!⁡
			useEffect(() => {
				console.log("Компонент обновился");
			}); // Выполняется при каждом рендере
		}
	}
}
//! ⁡⁢⁣⁡⁢⁣⁢АРХИТЕКТУРНЫЕ ПОДХОДЫ И ПАТЕРНЫ(ШАБЛОНЫ) К РАЗРАБОТКЕ ПО (монолитная архетектура, микрофронтенды...)⁡
{
	// 🔳 ⁡⁢⁣⁣ОБЩИЕ ПОДХОДЫ⁡
	{
		// ⁡⁣⁣⁢Архитектурные подходы помогают организовать код, сделать его поддерживаемым, масштабируемым и предсказуемым. Вот ключевые паттерны и методологии для JavaScript-приложений:⁡
		// ⁡⁢⁣⁣1. MVC (MODEL-VIEW-CONTROLLER)⁡
		{
			// ⁡⁣⁣⁢Для чего: Разделение логики приложения на три компонента:⁡
			// ⁡⁣⁢⁢-- Model (данные и бизнес-логика)⁡
			// ⁡⁣⁢⁢-- View (отображение данных)⁡
			// ⁡⁣⁢⁢-- Controller (обработка пользовательского ввода)⁡
			// Model
			class TodoModel {
				constructor() {
					this.todos = [];
				}
				addTodo(text) {
					this.todos.push({ text, completed: false });
				}
			}
			// View
			class TodoView {
				render(todos) {
					const list = todos.map((t) => `<li>${t.text}</li>`).join("");
					document.getElementById("app").innerHTML = `<ul>${list}</ul>`;
				}
			}
			// Controller
			class TodoController {
				constructor(model, view) {
					this.model = model;
					this.view = view;
				}
				addTodo(text) {
					this.model.addTodo(text);
					this.view.render(this.model.todos);
				}
			}

			// Использование
			const app = new TodoController(new TodoModel(), new TodoView());
			app.addTodo("Learn MVC");
		}
		// ⁡⁢⁣⁣2. FEATURE-SLICED DESIGN (FSD)⁡
		{
			// ⚪ ⁡⁣⁣⁢Feature-Sliced Design (FSD) — это архитектурная методология для фронтенд-приложений, ориентированная на организацию кода по бизнес-логике и функциональным возможностям (features). Она помогает структурировать проект так, чтобы его было легко масштабировать, поддерживать и тестировать, разделяя ответственность между слоями. FSD особенно популярен в экосистеме React, но применим и к другим фреймворкам.⁡
			// 🔹 ⁡⁣⁢⁢ОСНОВНЫЕ ПРИНЦИПЫ FSD⁡
			{
				// ⚪ ⁡⁣⁣⁢Разделение по бизнес-логике:⁡ ⁡⁣⁣⁢Код группируется вокруг фич (features) — независимых функциональных модулей (например, аутентификация, корзина покупок).⁡
				// ⚪ ⁡⁣⁣⁢Слоистая структура: Каждый слой имеет четкую зону ответственности и ограничения на взаимодействие с другими слоями.⁡
				// ⚪ ⁡⁣⁣⁢Изоляция модулей: Фичи и сущности максимально независимы друг от друга, что уменьшает coupling (связность).⁡
			}
			// 🔹 ⁡⁣⁢⁢СТРУКТУРА ПРОЕКТА (ПРИМЕР)⁡
			{
				// ⁡⁣⁣⁢src/⁡
				// ⁡⁣⁣⁢├── app/  ⁡           # Инициализация приложения (роутинг, провайдеры, глобальные стили)⁡
				// ⁡⁣⁣⁢├── processes/ ⁡      # Сложные бизнес-процессы, охватывающие несколько фич (например, оформление заказа)⁡
				// ⁡⁣⁣⁢├── pages/ ⁡          # Компоненты страниц (только композиция фич и сущностей)
				// ⁡⁣⁣⁢├── features/ ⁡       # Фичи (самостоятельные модули с UI, логикой и API)
				// ⁡⁣⁣⁢│   └── auth/  ⁡      # Пример фичи "Аутентификация"
				// ⁡⁣⁣⁢│       ├── ui/ ⁡     # Компоненты (кнопки, формы)
				// ⁡⁣⁣⁢│       ├── model/⁡   # Логика (стейт-менеджмент, API-запросы)
				// ⁡⁣⁣⁢│       └── lib/ ⁡    # Вспомогательные функции (валидация, утилиты)
				// ⁡⁣⁣⁢├── entities/ ⁡       # Базовые бизнес-сущности (например, User, Product)
				// ⁡⁣⁣⁢├── shared/⁡          # Переиспользуемый код (UI-кит, API-клиент, утилиты)
				// ⁡⁣⁣⁢└── widgets/  ⁡       # Составные компоненты, используемые на страницах (опционально)
			}
			// 🔹 ⁡⁣⁢⁢ПЛЮСЫ FSD⁡
			{
				// ⚪ ⁡⁣⁣⁢Масштабируемость: Новые фичи добавляются без влияния на существующий код.⁡
				// ⚪ ⁡⁣⁣⁢Читаемость: Структура интуитивно понятна даже новым разработчикам.⁡
				// ⚪ ⁡⁣⁣⁢Изоляция ответственности: Легко рефакторить или удалять фичи.⁡
				// ⚪ ⁡⁣⁣⁢Тестируемость: Фичи и сущности можно тестировать изолированно.⁡
				// ⚪ ⁡⁣⁣⁢Гибкость: Подходит для проектов любого размера (от небольших до enterprise). ⁡
			}
			// 🔹 ⁡⁣⁢⁢МИНУСЫ FSD⁡
			{
				// ⚪ ⁡⁣⁣⁢Сложность внедрения: Требует строгого следования правилам и дисциплины в команде.⁡
				// ⚪ ⁡⁣⁣⁢Избыточность для мелких проектов: Для простых приложений структура может показаться слишком громоздкой.⁡
				// ⚪ ⁡⁣⁣⁢Кривая обучения: Нужно время, чтобы освоить взаимодействие между слоями.⁡
				// ⚪ ⁡⁣⁣⁢Риск нарушения границ: При несоблюдении правил слои могут начать "протекать" (например, фичи напрямую обращаются к shared-коду, минуя entities). ⁡
			}
			// 🔹 ⁡⁣⁢⁢СРАВНЕНИЕ С ДРУГИМИ ПОДХОДАМИ⁡
			{
				// ⁡⁢⁣⁣АСПЕКТ				FSD					МОДУЛЬНАЯ АРХИТЕКТУРА		CLEAN ARCHITECTURE⁡
				// ⁡⁣⁣⁢Фокус				Бизнес-фичи			Технические модули			Абстракция и инверсия зависимостей⁡
				// ⁡⁣⁣⁢Структура			Слои + фичи			Компоненты / страницы		Слои (домен, приложение, инфраструктура)⁡
				// ⁡⁣⁣⁢Гибкость				Высокая				Средняя						Высокая⁡
				// ⁡⁣⁣⁢Сложность			Средняя				Низкая						Высокая⁡
				// ⁡⁣⁣⁢Идеальные проекты	Крупные SPA,SaaS	Средние проекты				Enterprise-приложения⁡
			}
			// 🔹 ⁡⁣⁢⁢КОГДА ИСПОЛЬЗОВАТЬ FSD?⁡
			{
				// ⚪ ⁡⁣⁣⁢Крупные проекты с долгосрочной поддержкой.⁡
				// ⚪ ⁡⁣⁣⁢Командная разработка (минимизация конфликтов).⁡
				// ⚪ ⁡⁣⁣⁢Высокие требования к тестированию и изоляции модулей.⁡
				// ⚪ ⁡⁣⁣⁢Планы масштабирования (добавление новых фич).⁡
			}
			// 🔹 ⁡⁣⁢⁢ИНСТРУМЕНТЫ И СТЕК⁡
			{
				// ⚪ ⁡⁣⁣⁢Фреймворки: React, Vue, Angular.⁡
				// ⚪ ⁡⁣⁣⁢Стейт-менеджмент: Redux Toolkit, MobX, Effector.⁡
				// ⚪ ⁡⁣⁣⁢API-клиенты: React Query, RTK Query, Axios.⁡
				// ⚪ ⁡⁣⁣⁢Стилизация: CSS Modules, Styled Components, Tailwind.⁡
			}
			// 🔹 ⁡⁣⁢⁢РЕКОМЕНДАЦИИ⁡
			{
				// ⚪ ⁡⁣⁣⁢Строго соблюдайте границы слоев. Например:⁡
				// ⚡ ⁡⁢⁣⁣features не должны импортировать друг друга напрямую.⁡
				// ⚡ ⁡⁢⁣⁣shared — только общие низкоуровневые компоненты/утилиты.⁡
				// ⚪ ⁡⁣⁣⁢Используйте автономные модули. Каждая фича должна работать независимо.⁡
				// ⚪ ⁡⁣⁣⁢Документируйте структуру. Это поможет новым разработчикам быстрее вникнуть.⁡
				// ⚪ ⁡⁣⁣⁢Комбинируйте с другими подходами. Например, FSD + Micro Frontends для мега-проектов.⁡
			}
		}
		// ⁡⁢⁣⁣⁡⁢⁣⁣3⁡. ⁡⁢⁣⁣КОМПОНЕНТНАЯ АРХИТЕКТУРА (COMPONENT-BASED)⁡
		{
			// ⁡⁣⁣⁢Для чего: Создание переиспользуемых UI-компонентов (используется в React, Vue, Angular).⁡
			// ⁡⁣⁢⁢-- Каждый компонент отвечает за свою часть интерфейса.⁡
			// ⁡⁣⁢⁢-- Состояние компонента инкапсулировано.⁡
			// ⁡⁣⁢⁢-- Компоненты могут быть вложенными.⁡
			function Button({ onClick, children }) {
				return (
					<button className="btn" onClick={onClick}>
						{children}
					</button>
				);
			}
			// Использование
			<Button onClick={() => console.log("Clicked!")}>Press me</Button>;
		}
		// ⁡⁢⁣⁣4. FLUX/REDUX (ОДНОНАПРАВЛЕННЫЙ ПОТОК ДАННЫХ)⁡
		{
			// ⁡⁣⁣⁢Для чего: Управление состоянием приложения в крупных проектах.⁡
			// Store
			// import { createStore } from "redux";
			// Reducer
			function counter(state = 0, action) {
				switch (action.type) {
					case "INCREMENT":
						return state + 1;
					case "DECREMENT":
						return state - 1;
					default:
						return state;
				}
			}

			const store = createStore(counter);

			// Подписка на изменения
			store.subscribe(() => {
				console.log("Current state:", store.getState());
			});

			// Диспатч действий
			store.dispatch({ type: "INCREMENT" });
		}
		// ⁡⁢⁣⁣5. EVENT-DRIVEN ARCHITECTURE (СОБЫТИЙНО-ОРИЕНТИРОВАННАЯ)⁡
		{
			// ⁡⁣⁣⁢Для чего: Организация взаимодействия между частями системы через события.⁡
			const EventEmitter = require("events");
			class Logger extends EventEmitter {
				log(message) {
					console.log(message);
					this.emit("logged", { message, date: Date.now() });
				}
			}
			const logger = new Logger();
			logger.on("logged", (data) => {
				console.log("Event:", data);
			});
			logger.log("Hello World!");
		}
		// ⁡⁢⁣⁣6. МИКРОСЕРВИСНАЯ АРХИТЕКТУРА (MICROSERVICES ARCHITECTURE)⁡
		{
			// ⁡⁣⁣⁢Для чего: Разделение приложения на независимые сервисы (подходит для бэкенда).⁡
			// Auth Service
			const express = require("express");
			const authApp = express();
			authApp.post("/login", (req, res) => {
				/* ... */
			});
			// Product Service
			const productApp = express();
			productApp.get("/products", (req, res) => {
				/* ... */
			});
			// Запуск на разных портах
			authApp.listen(3001);
			productApp.listen(3002);
		}
		// ⁡⁢⁣⁣7. SERVERLESS ARCHITECTURE (СЕРВЕРНО-БЕСПЛАТНАЯ)⁡
		{
			// ⁡⁣⁣⁢Для чего: Запуск кода без управления серверами (AWS Lambda, Vercel).⁡
			exports.handler = async (event) => {
				const name = event.queryStringParameters?.name || "World";
				return {
					statusCode: 200,
					body: JSON.stringify({ message: `Hello ${name}!` }),
				};
			};
		}
		// ⁡⁢⁣⁣8. CLEAN ARCHITECTURE (ЧИСТАЯ АРХИТЕКТУРА)⁡
		{
			// ⁡⁣⁣⁢⁡⁣⁣⁢Для чего: Разделение кода на слои (инфраструктура, бизнес-логика, интерфейс).⁡
			// ⁡⁣⁢⁢-- Entities (бизнес-правила)⁡
			// ⁡⁣⁢⁢-- Use Cases (сценарии применения)⁡
			// ⁡⁣⁢⁢-- Controllers (интерфейсы для внешнего мира)⁡
			// ⁡⁣⁣⁢src/⁡
			// 	⁡⁣⁣⁢entities/⁡
			// 	⁡⁣⁣⁢useCases/⁡
			// 	⁡⁣⁣⁢interfaces/⁡
			// 	⁡⁣⁣⁢infrastructure/⁡
		}
		// ⁡⁢⁣⁣9. MVVM (MODEL-VIEW-VIEWMODEL)⁡
		{
			// ⁡⁣⁣⁢⁡⁣⁣⁢Для чего: Связывание данных между View и Model (популярен в Angular, Vue).⁡
			// ViewModel
			new Vue({
				el: "#app",
				data: { message: "Hello" }, // Model
				computed: {
					reversedMessage() {
						return this.message.split("").reverse().join("");
					},
				}, // ViewModel Logic
			});
			// View
			<div id="app">
				<input v-model="message" />
				<p>{{ reversedMessage }}</p>
			</div>;
		}
		// ⁡⁣⁣⁢СРАВНЕНИЕ ПОДХОДОВ⁡
		{
			// ⁡⁣⁢⁣ПОДХОД	 КОГДА ИСПОЛЬЗОВАТЬ	       		ПЛЮСЫ	         							МИНУСЫ⁡
			// ⁡⁣⁣⁢MVC⁡		⁡⁣⁣⁢Классические веб-приложения		Простота, разделение обязанностей			⁡⁣⁣⁢Может стать "жирным⁡
			// 																						⁡⁣⁣⁢контроллером"⁡
			// ⁡⁣⁣⁢CB⁡		⁡⁣⁣⁢SPA, сложные⁡					⁡⁣⁣⁢Переиспользуемость,⁡						⁡⁣⁣⁢Оверхед для мелких⁡
			//			⁡⁣⁣⁢интерфейсы⁡						⁡⁣⁣⁢изоляция⁡										⁡⁣⁣⁢проектов⁡
			// ⁡⁣⁣⁢Flux/Redux⁡ ⁡⁣⁣⁢Крупные приложения с⁡		⁡⁣⁣⁢Предсказуемость,⁡					⁡⁣⁣⁢Боль⁡⁣⁣⁢ше шаблонного кода⁡
			//				⁡⁣⁣⁢состоянием⁡					⁡⁣⁣⁢отладка⁡
			// ⁡⁣⁣⁢Event-⁡	⁡⁣⁣⁢Реалтайм-системы, чаты⁡			⁡⁣⁣⁢Гибкость,⁡						⁡⁣⁣⁡⁣⁣⁢Сложность отслеживания⁡
			// ⁡⁣⁣⁢Driven									⁡⁣⁣⁢масштабируемость⁡				⁡⁣⁣⁢событий⁡
		}
		// ⁡⁣⁣⁢СОВЕТЫ ПО ВЫБОРУ АРХИТЕКТУРЫ⁡
		{
			// ⁡⁣⁣⁢1. SPA (React/Vue/Angular): Компонентная + Flux/Redux⁡
			// ⁡⁣⁣⁢2. Серверный рендеринг (Next.js/Nuxt): MVC + Server-Side Rendering⁡
			// ⁡⁣⁣⁢3. Микросервисы: Node.js + Express + REST/gRPC⁡
			// ⁡⁣⁣⁢4. Высокая нагрузка: Event-Driven + Message Brokers (Kafka, RabbitMQ)⁡
		}
	}
	// 🔳 ⁡⁢⁣⁣ДЛЯ ФРОНТЕТД РАЗРАБОТКИ⁡
	{
		// 🔹 ⁡⁣⁢⁢МОНОЛИТНАЯ АРХИТЕКТУРА⁡
		// ⚪ ⁡⁣⁣⁢Описание: Все компоненты (UI, бизнес-логика, состояние) tightly coupled (тесно связаны) в одном кодовом проекте
		{
			// ✅ ⁡⁢⁢⁢Простота разработки для маленьких проектов.⁡
			// ✅ ⁡⁢⁢⁢Легко развернуть (один бандл).⁡
			// ✅ ⁡⁢⁢⁢Минимум накладных расходов на взаимодействие между модулями.⁡
			// ⛔ ⁡⁢⁣⁢Сложность масштабирования.⁡
			// ⛔ ⁡⁢⁣⁢Медленная сборка при росте кодовой базы.⁡
			// ⛔ ⁡⁢⁣⁢Сложность внедрения новых технологий.⁡
			// ⚡ ⁡⁢⁣⁣React/Vue/Angular + Redux/MobX (если нужно состояние).⁡
			// ⚡ ⁡⁢⁣⁣Webpack/Vite для сборки.⁡
		}
		// 🔹 ⁡⁣⁢⁢МОДУЛЬНАЯ АРХИТЕКТУРА (НА ОСНОВЕ КОМПОНЕНТОВ)⁡
		// ⚪ ⁡⁣⁣⁢Описание: Приложение разбито на независимые компоненты (например, по фичам или страницам).⁡
		{
			// ✅ ⁡⁢⁢⁢Улучшенная поддержка кода.⁡
			// ✅ ⁡⁢⁢⁢Возможность переиспользования компонентов.⁡
			// ✅ ⁡⁢⁢⁢Более гибкая, чем монолит.⁡
			// ⛔ ⁡⁢⁣⁢Требует четкого разделения ответственности.⁡
			// ⛔ ⁡⁢⁣⁢Может привести к пропсам (prop drilling) без правильного управления состоянием.⁡
			// ⚡ ⁡⁢⁣⁣React (Next.js), Vue (Nuxt), Angular.⁡
			// ⚡ ⁡⁢⁣⁣Redux Toolkit, Zustand, Pinia (Vue).⁡
		}
		// 🔹 ⁡⁣⁢⁢FEATURE-SLICED DESIGN (FSD)⁡
		// ⚪ ⁡⁣⁣⁢Feature-Sliced Design (FSD) — это архитектурная методология для фронтенд-приложений, ориентированная на организацию кода по бизнес-логике и функциональным возможностям (features). Она помогает структурировать проект так, чтобы его было легко масштабировать, поддерживать и тестировать, разделяя ответственность между слоями. FSD особенно популярен в экосистеме React, но применим и к другим фреймворкам.⁡
		{
			// ✅ ⁡⁣⁣⁢⁡⁢⁢⁢Масштабируемость: Новые фичи добавляются без влияния на существующий код.⁡
			// ✅ ⁢⁡⁢⁢⁢Читаемость: Структура интуитивно понятна даже новым разработчикам.⁡
			// ✅ ⁡⁢⁢⁢Изоляция ответственности: Легко рефакторить или удалять фичи.⁡
			// ✅ ⁡⁢⁢⁢Тестируемость: Фичи и сущности можно тестировать изолированно.⁡
			// ✅ ⁡⁣⁣⁡⁢⁢⁢Гибкость: Подходит для проектов любого размера (от небольших до enterprise).⁡
			// ⛔ ⁡⁢⁣⁢Сложность внедрения: Требует строгого следования правилам и дисциплины в команде.⁡
			// ⛔ ⁡⁢⁣⁢Избыточность для мелких проектов: Для простых приложений структура может показаться слишком громоздкой.⁡
			// ⛔ ⁢⁡⁢⁣⁢Кривая обучения: Нужно время, чтобы освоить взаимодействие между слоями.⁡
			// ⛔ ⁡⁢⁣⁢Риск нарушения границ: При несоблюдении правил слои могут начать "протекать" (например, фичи напрямую обращаются к shared-коду, минуя entities).⁡ ⁡
			// ⚡ ⁡⁢⁣⁣Фреймворки: React, Vue, Angular.⁡
			// ⚡ ⁡⁢⁣⁣Стейт-менеджмент: Redux Toolkit, MobX, Effector.⁡
			// ⚡ ⁡⁢⁣⁣API-клиенты: React Query, RTK Query, Axios.⁡
			// ⚡ ⁡⁢⁣⁣Стилизация: CSS Modules, Styled Components, Tailwind.⁡
		}
		// 🔹 ⁡⁣⁢⁢МИКРОФРОНТЕНДЫ (MICRO FRONTENDS)⁡
		// ⚪ ⁡⁣⁣⁢Описание: Разделение приложения на независимые подприложения, которые могут разрабатываться и деплоиться отдельно.⁡
		{
			// ✅ ⁡⁢⁢⁢Независимые команды и технологии.⁡
			// ✅ ⁡⁢⁢⁡⁢⁢⁢Возможность обновлять части приложения без полного рефакторинга. Постепенная миграция legacy-кода.⁡
			// ✅ ⁡⁢⁢⁢Гибкость в выборе стека для каждого микрофронта.⁡
			// ✅ ⁡⁢⁢⁢Масштабируемость: Упрощение управления большими проектами за счет разделения на автономные модули.⁡
			// ✅ ⁡⁢⁢⁢Изолированность: Ошибки в одном микрофронтенде не ломают всё приложение (если реализовано правильно).⁡
			// ✅ ⁡⁢⁢⁢Гибкость развертывания:Каждая часть может деплоиться независимо, что ускоряет доставку фич.
			// ⛔ ⁡⁢⁣⁢Сложность в настройке (сборка, CI/CD).⁡
			// ⛔ ⁡⁢⁣⁢Возможные конфликты стилей/глобальных зависимостей.⁡
			// ⛔ ⁡⁢⁣⁢Overhead на межмикрофронтовое взаимодействие.⁡
			// ⛔ ⁡⁢⁣⁢Сложность интеграции: Необходимость согласовывать API, стили, навигацию между командами. Риск конфликтов версий зависимостей.⁡
			// ⛔ ⁡⁢⁣⁢Дублирование кода: Общие библиотеки (например, Lodash) могут загружаться несколько раз, увеличивая размер бандла.⁡
			// ⛔ ⁡⁢⁣⁢Производительность: Загрузка нескольких JS-бандлов может замедлить приложение. Возможны проблемы с SEO из-за динамического рендеринга.⁡
			// ⛔ ⁡⁢⁣⁢Согласованность UX: Разные команды могут создавать несогласованный интерфейс (например, кнопки с разными стилями).⁡
			// ⛔ ⁡⁢⁣⁢Сложности с типизацией. Например, при разработке на TypeScript в технологии Module Federation нет механизма, с помощью которого можно было бы передавать типы на другой хост.⁡
			// ⚡ ⁡⁢⁣⁣Iframe (устаревший, но простой вариант).⁡
			// ⚡ ⁡⁢⁣⁣СТАНДАРТИЗАЦИЯ⁡
			{
				// 🔹 ⁡⁣⁢⁢Единые гайдлайны по стилям (CSS-переменные, дизайн-системы вроде Storybook).⁡
				// 🔹 ⁡⁣⁢⁢Общие библиотеки компонентов (например, внутренний UI-kit).⁡
				// 🔹 ⁡⁣⁢⁢Использование монорепозитория (Turborepo, Nx) для контроля зависимостей.⁡
			}
			// ⚡ ⁡⁢⁣⁣ОПТИМИЗАЦИЯ ЗАГРУЗКИ⁡
			{
				// 🔹 ⁡⁣⁢⁢Ленивая загрузка микрофронтендов (например, с помощью Webpack Module Federation).⁡
				// 🔹 ⁡⁣⁢⁢Шеринг общих зависимостей (например, через externals в Webpack).⁡
				// 🔹 ⁡⁣⁢⁢Кэширование статических ресурсов.⁡
			}
			// ⚡ ⁡⁢⁣⁣ИНТЕГРАЦИОННЫЕ РЕШЕНИЯ⁡
			{
				// 🔹 ⁡⁣⁢⁢Фреймворки для микрофронтендов: Single-SPA, Qiankun, Piral.⁡
				// 🔹 ⁡⁣⁢⁢Единая маршрутизация (например, React Router с динамическим импортом).⁡
				// 🔹 ⁡⁣⁢⁢Централизованное управление состоянием (Redux, Zustand).⁡
			}
			// ⚡ ⁡⁢⁣⁣ТЕСТИРОВАНИЕ И CI/CD⁡
			{
				// 🔹 ⁡⁣⁢⁢Автоматизированные кросс-командные тесты (Cypress, Playwright).⁡
				// 🔹 ⁡⁣⁢⁢Согласованные pipelines для деплоя (GitHub Actions, GitLab CI).⁡
				// 🔹 ⁡⁣⁢⁢Линтеры и пре-коммит хуки для соблюдения стандартов.⁡
			}
			// ⚡ ⁡⁢⁣⁣БЕЗОПАСНОСТЬ И ИЗОЛЯЦИЯ⁡
			{
				// 🔹 ⁡⁣⁢⁢Использование Shadow DOM для CSS-изоляции.⁡
				// 🔹 ⁡⁣⁢⁢Санбоксирование (Песочница, например, Web Workers или iframe для критичных частей).⁡
				// 🔹 ⁡⁣⁢⁢Регулярные аудиты зависимостей (Dependabot, Snyk).⁡
			}
		}
		// 🔹 ⁡⁣⁢⁢АРХИТЕКТУРА НА ОСНОВЕ СЕРВЕРНОГО РЕНДЕРИНГА (SSR)⁡
		// ⚪ ⁡⁣⁣⁢Описание: Рендеринг происходит на сервере (Next.js, Nuxt, SvelteKit).⁡
		{
			// ✅ ⁡⁢⁢⁢Лучший SEO.⁡
			// ✅ ⁡⁢⁢⁢Быстрая загрузка первой страницы.⁡
			// ⛔ ⁡⁢⁣⁢Сложнее в разработке (гидрация, работа с API).⁡
			// ⛔ ⁡⁢⁣⁢Серверная нагрузка.⁡
			// ⚡ ⁡⁢⁣⁣Next.js (React), Nuxt (Vue), SvelteKit.⁡
		}
		// 🔹 ⁡⁣⁢⁢JAMstack (STATIC SITE GENERATORS)⁡
		// ⚪ ⁡⁣⁣⁢Описание: Статические сайты, генерируемые на этапе сборки (Gatsby, Astro).⁡
		{
			// ✅ ⁡⁢⁢⁢Высокая производительность.⁡
			// ✅ ⁡⁢⁢⁢Хостинг на CDN (дешево и масштабируемо).⁡
			// ⛔ ⁡⁢⁣⁢Не подходит для динамических данных без клиентского JS.⁡
			// ⛔ ⁡⁢⁣⁢Долгая пересборка при большом количестве страниц.⁡
			// ⚡ ⁡⁢⁣⁣Gatsby, Astro, Eleventy (11ty).⁡
		}
		// 🔹 ⁡⁣⁢⁢АРХИТЕКТУРА НА ОСНОВЕ СОБЫТИЙ (EVENT-DRIVEN)⁡
		// ⚪ ⁡⁣⁣⁢Описание: Компоненты общаются через события (CustomEvents, Pub/Sub).⁡
		{
			// ✅ ⁡⁢⁢⁢Низкая связанность.⁡
			// ✅ ⁡⁢⁢⁢Гибкость в масштабировании.⁡
			// ⛔ ⁡⁢⁣⁢Сложность отладки (поток событий).⁡
			// ⛔ ⁡⁢⁣⁢Возможны memory leaks.⁡
			// ⚡ ⁡⁢⁣⁣RxJS, Redux-Observable, NanoEvents.⁡
		}
		// 🔹 ⁡⁣⁢⁢СРАВНЕНИЕ АРХИТЕКТУР⁡
		{
			// ⁡⁢⁣⁣АРХИТЕКТУРА		МАСШТАБИРУЕМОСТЬ	СЛОЖНОСТЬ		SEO-ДРУЖЕСТВЕННОСТЬ			ПОДХОДЯЩИЕ ПРОЕКТЫ⁡
			// ⁡⁣⁣⁢Монолитная		Низкая				Низкая			Зависит от реализации		Маленькие проекты⁡
			// ⁡⁣⁣⁢Модульная		Средняя				Средняя			Да (если SSR)				Средние проекты⁡
			// ⁡⁣⁣⁢FSD				Высокая				Высокая			Да (если SSR)				Крупные продукты⁡
			// ⁡⁣⁣⁢Микрофронтенды	Высокая				Высокая			Да							Крупные продукты⁡
			// ⁡⁣⁣⁢SSR				Средняя				Средняя			Да							Контентные сайты⁡
			// ⁡⁣⁣⁢JAMstack			Высокая				Низкая			Да							Блоги, лендинги⁡
			// ⁡⁣⁣⁢Event-Driven		Средняя				Высокая			Нет							SPA с сложным UI⁡
		}
		// 🔹 ⁡⁣⁢⁢ВЫБОР АРХИТЕКТУРЫ⁡
		{
			// 💡 ⁡⁣⁣⁢Маленький проект 			→ Монолит или модульная.⁡
			// 💡 ⁡⁣⁣⁢Корпоративный портал 		→ Микрофронтенды.⁡
			// 💡 ⁡⁣⁣⁢Блог/Лендинг 				→ JAMstack (Gatsby, Astro).⁡
			// 💡 ⁡⁣⁣⁢Динамическое SPA 			→ Next.js/Nuxt (SSR/ISR).⁡
		}
	}
	// 🔳 ⁡⁢⁣⁣ПАТЕРНЫ ПРОЕКТИРОВАНИЯ (шаблоны)⁡
	{
		// ⚪ ⁡⁣⁣⁢Observer-паттерн — это шаблон проектирования, который позволяет один объект (субъект) уведомлять нескольких наблюдателей (подписчиков) об изменениях или событиях, не требуя от них прямого знания друг о друге.⁡
		{
			// ⚡ ⁡⁢⁣⁣Субъект. Это организация, ответственная за отслеживание определённого набора данных или условий и уведомление наблюдателей при возникновении существенных изменений.⁡
			// ⚡ ⁡⁢⁣⁣Наблюдатели. Это объекты, которые регистрируют интерес субъекта к получению обновлений при возникновении определённых событий или изменений.⁡
			// ⚡ ⁡⁢⁣⁣Метод уведомления. Субъект предоставляет метод уведомления, который позволяет ему информировать зарегистрированных наблюдателей о происходящих изменениях. Наблюдатели, в свою очередь, реализуют метод обновления, который вызывается субъектом при возникновении изменений.⁡
		}
	}
}
//! ⁡⁢⁣⁢БИБЛИОТЕКИ⁡
{
	// 🔳 ⁡⁢⁣⁣КЕШИРОВАНИЕ, ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ⁡
	{
		// ⭐ ⁡⁣⁣⁢React Query — библиотека для получения, кэширования, синхронизации и обновления «серверного» состояния в React-приложениях. Она предоставляет набор инструментов для обработки данных, их кэширования и обновления пользовательского интерфейса при изменении данных.⁡	⁡⁣⁣⁢Для подписки на запрос в компоненте или пользовательском хуке следует вызвать хук useQuery. Он принимает уникальный ключ запроса и функцию, возвращающую промис, который разрешается данными или выбрасывает исключение.⁡
		{
			// import { useQuery } from "react-query";
			function Todos() {
				const { data } = useQuery("todos", () =>
					fetch("/api/todos").then((res) => res.json()),
				);
				return data.map((todo) => <div key={todo.id}>{todo.text}</div>);
			}
		}
		// ⭐ ⁡⁣⁣⁢Bundle Analyzer — это инструмент для визуализации и анализа JavaScript-бандлов, созданных сборщиками вроде Webpack. Он создаёт интерактивную карту содержимого бандлов, позволяя увидеть размеры модулей, их состав и зависимости.⁡
		{
			// ⁡⁣⁢⁢ОПТИМИЗАЦИЯ РАЗМЕРА БАНДЛА⁡
			// ⁡⁣⁣⁢- Уменьшение времени загрузки приложения за счет удаления "тяжелых" или неиспользуемых библиотек. Пример: Обнаружение случайно подключенной 500-килобайтной библиотеки для форматирования дат, когда можно использовать нативную Intl.DateTimeFormat.⁡
			// ⁡⁣⁢⁢ПОИСК ДУБЛИКАТОВ⁡
			// ⁡⁣⁣⁢- Выявление одинаковых пакетов, подключенных несколько раз. Пример: lodash и lodash-es в одном проекте.⁡
			// ⁡⁣⁢⁢АНАЛИЗ ЭФФЕКТИВНОСТИ CODE SPLITTING⁡
			// ⁡⁣⁣⁢- Проверка, как разбиение кода на чанки влияет на размеры файлов. Пример: Убедиться, что React.lazy() корректно разделяет бандл⁡
			// ⁡⁣⁢⁢СРАВНЕНИЕ СБОРОК⁡
			// ⁡⁣⁣⁢- Сравнение размеров бандлов до и после оптимизаций. Пример: После замены moment.js на date-fns⁡
			// ⁡⁣⁣⁢Запуск⁡
			// npm run build
			// ⁡⁣⁣⁢После сборки в папке build/ появится файл bundle-report.html — откройте его в браузере⁡
			// ⁡⁣⁢⁢2. Source Map Explorer (универсальный инструмент)⁡
			// ⁡⁣⁣⁢Установка⁡
			// npm install --save-dev source-map-explorer
			// ⁡⁣⁣⁢Анализ бандла⁡
			// npx source-map-explorer build/static/js/main.*.js
		}
		// ⚪ ⁡⁣⁣⁡⁣⁢⁣ПОПУЛЯРНЫЕ БИБЛИОТЕКИ ДЛЯ ВИРТУАЛИЗАЦИИ В REACT⁡
		{
			// ⭐ ⁡⁣⁣⁢react-window⁡
			{
				// import { FixedSizeList as List } from "react-window";
				const Row = ({ index, style }) => <div style={style}>Row {index}</div>;
				const VirtualizedList = () => (
					<List height={400} itemCount={1000} itemSize={35} width={300}>
						{Row}
					</List>
				);
			}
			// ⭐ ⁡⁣⁣⁢react-virtualized⁡
			{
				// import { List } from "react-virtualized";
				const list = Array(1000)
					.fill()
					.map((_, index) => `Item ${index}`);
				const renderRow = ({ index, key, style }) => (
					<div key={key} style={style}>
						{list[index]}
					</div>
				);
				const VirtualizedList = () => (
					<List
						width={300}
						height={400}
						rowCount={list.length}
						rowHeight={35}
						rowRenderer={renderRow}
					/>
				);
			}
			// ⭐ ⁡⁣⁣⁢react-virtuoso (для динамических высот)⁡
			{
				// import { Virtuoso } from "react-virtuoso";
				const items = Array(1000)
					.fill()
					.map((_, i) => ({
						id: i,
						text: `Item ${i}`,
						height: 30 + Math.random() * 50, // Разная высота элементов
					}));
				const VirtualizedList = () => (
					<Virtuoso
						style={{ height: "400px" }}
						totalCount={items.length}
						itemContent={(index) => (
							<div style={{ height: `${items[index].height}px` }}>
								{items[index].text}
							</div>
						)}
					/>
				);
			}
		}
	}
	// 🔳 ⁡⁢⁣⁣МАРШРУТИЗАЦИЯ (ROUTING)⁡
	{
		// ⭐ ⁡⁣⁣⁡⁣⁣⁢React Router - Навигация между страницами в SPA(Single Page Application - это веб-приложение, которое загружает только одну HTML-страницу и динамически обновляет её содержимое без перезагрузки страницы при взаимодействии с пользователем. SPA использует технику AJAX для получения данных и обновления необходимой части страницы)⁡
		// ⭐ ⁡⁣⁣⁢Next.js - SSR, статическая генерация, маршрутизация на основе файлов. (Server-Side Rendering  — это метод отрисовки веб-страницы на сервере, а не в браузере. Файлы JavaScript или CSS не загружаются сразу, а отправляются пользователю уже сгенерированный HTML. Это ускоряет загрузку страниц и улучшает пользовательский опыт, особенно для пользователей со слабым интернетом)⁡
	}
	// 🔳 ⁡⁢⁣⁣РАБОТА С ФОРМАМИ (Hook Form, SWR, Formik)⁡
	{
		// ⭐ ⁡⁣⁣⁢React Hook Form - Валидация и управление формами с минимальным рендерингом.⁡
		{
			// import { useForm } from "react-hook-form";
			function Form() {
				const { register, handleSubmit } = useForm();
				const onSubmit = (data) => console.log(data);

				return (
					<form onSubmit={handleSubmit(onSubmit)}>
						<input {...register("name", { required: true })} />
						<button type="submit">Submit</button>
					</form>
				);
			}
		}
		// ⭐ ⁡⁣⁣⁢SWR (Stale-While-Revalidate) - Кэширование и автоматическое обновление данных.⁡
		{
			// import useSWR from "swr";
			function Profile() {
				const { data, error } = useSWR("/api/user", fetcher);
				if (error) return <div>Error</div>;
				if (!data) return <div>Loading...</div>;
				return <div>Hello {data.name}</div>;
			}
		}
		// ⭐ ⁡⁣⁣⁢Formik — популярная библиотека для управления формами в React-приложениях. Она упрощает работу с валидацией, отслеживанием состояний полей и обработкой отправки данных.⁡
		{
			// ✅ ⁡⁢⁢⁢Управление состоянием формы (значения полей, ошибки, статусы)⁡
			// ✅ ⁡⁢⁢⁢Автоматическая обработка сабмита и сброса формы⁡
			// ✅ ⁡⁢⁢⁢Интеграция с системами валидации (например, Yup)⁡
			// ✅ ⁡⁢⁢⁢Упрощение работы с комплексными формами⁡
			{
				// import { useFormik } from "formik";
				function LoginForm() {
					const formik = useFormik({
						initialValues: { email: "", password: "" },
						onSubmit: (values) => {
							console.log("Submitted:", values);
						},
					});
					return (
						<form onSubmit={formik.handleSubmit}>
							<input
								name="email"
								value={formik.values.email}
								onChange={formik.handleChange}
							/>
							<input
								type="password"
								name="password"
								value={formik.values.password}
								onChange={formik.handleChange}
							/>
							<button type="submit">Login</button>
						</form>
					);
				}
			}
			// ⭐ ⁡⁣⁣⁢Сравнение с React Hook Form⁡
			{
				// ⁡⁢⁣⁣КРИТЕРИЙ					FORMIK								REACT HOOK FORM⁡
				// ⁡⁣⁣⁢Подход					Компонентный						Хуки⁡
				// ⁡⁣⁣⁢Производительность		Перерендеры при изменении полей		Минимальные перерендеры⁡
				// ⁡⁣⁣⁢Валидация				Часто с Yup							Встроенная валидация + поддержка Yup⁡
				// ⁡⁣⁣⁢Сложность форм			Идеален для сложных форм			Лучше для простых/средних форм⁡
			}
		}
	}
	// 🔳 ⁡⁢⁣⁣UI-БИБЛИОТЕКИ И ФРЕЙМВОРКИ (Material-UI, TailwindCSS, Storybook)⁡
	{
		// ⭐ ⁡⁣⁣⁢Material-UI (MUI) - Готовые React-компоненты в стиле Material Design.⁡
		{
			// import { Button, TextField } from "@mui/material";
			function App() {
				return (
					<div>
						<TextField label="Name" variant="outlined" />
						<Button variant="contained" color="primary">
							Submit
						</Button>
					</div>
				);
			}
		}
		// ⭐ ⁡⁣⁣⁢Tailwind CSS - Utility-first CSS-фреймворк.⁡
		{
			<button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
				Button
			</button>;
		}
		// ⭐ ⁡⁣⁣⁢Storybook: изолированная разработка UI-компонентов⁡
		{
			// ⚡ ⁡⁢⁣⁣Storybook — инструмент для разработки компонентов пользовательского интерфейса в изоляции от основного приложения. Позволяет создавать интерактивную документацию, тестировать компоненты в разных состояниях и делиться ими с командой.⁡
			// 🔹 ⁡⁣⁢⁢Изолированная разработка компонентов без зависимостей от бизнес-логики приложения.⁡
			// 🔹 ⁡⁣⁢⁢Документирование всех вариантов использования компонента.⁡
			// 🔹 ⁡⁣⁢⁢Визуальное тестирование (поиск регрессий через сравнение снимков).⁡
			// 🔹 ⁡⁣⁢⁢Интерактивное демо для дизайнеров и разработчиков.⁡
			// 🔹 ⁡⁣⁢⁢Интеграция с инструментами (Figma, Chromatic, Testing Library).⁡
			// Документация Storybook https://storybook.js.org/
			// Примеры от сообщества https://storybook.js.org/showcase
			// Интеграция с Figma https://storybook.js.org/addons/storybook-addon-designs
		}
	}
	// 🔳 ⁡⁢⁣⁣UI KIT(User Interface Kit, ⁢Material-UI, ⁢Ant Design, ⁢Chakra UI, ⁢Bootstrap, ⁢Tailwind CSS + Headless UI⁡)⁡
	{
		// ⭐ ⁡⁣⁣⁢UI KIT (User Interface Kit(комплект, набор)) — это набор готовых компонентов (кнопки, формы, карточки и т.д.), стилей и инструментов для быстрого создания интерфейсов. Они экономят время, обеспечивают единый дизайн и часто включают accessibility-фичи(это функции, которые делают контент доступным для пользователей с различными физическими возможностями).⁡
		// 🔹 ⁡⁣⁢⁢СРАВНЕНИЕ UI KIT⁡
		{
			// ⁡⁢⁣⁣КРИТЕРИЙ			MATERIAL-UI				ANT DESIGN		CHAKRA UI	BOOTSTRAP		TAILWIND⁡
			// ⁡⁣⁣⁢Стиль			Material Design			Enterprise		Минимализм	Классика		Кастомизируемый⁡
			// ⁡⁣⁣⁢Кастомизация		Средняя					Сложная			Высокая		Низкая			Максимальная⁡
			// ⁡⁣⁣⁢Accessibility	Хорошо					Средне			Отлично		Средне			Зависит от вас⁡
			// ⁡⁣⁣⁢Размер бандла	Большой					Очень большой	Средний		Средний			Маленький⁡
			// ⁡⁣⁣⁢Лучшее для		Корпоративные проекты	Админ-панели	Стартапы	Быстрые MVP		Уникальный дизайн⁡
		}
		// 🔹  ⁡⁣⁢⁢Material-UI (MUI)⁡
		{
			// ✅ ⁡⁢⁢⁢Готовые компоненты: таблицы, меню, диалоги.⁡
			// ✅ ⁡⁢⁢⁢Кастомизация через sx-проп или темы.⁡
			// ✅ ⁡⁢⁢⁢Документация: https://mui.com/⁡
		}
		// 🔹 ⁡⁣⁢⁢Ant Design⁡
		{
			// ✅ ⁡⁢⁢⁢Богатая коллекция компонентов (включая сложные таблицы и графики).⁡
			// ✅ ⁡⁢⁢⁢Поддержка тем оформления.⁡
			// ✅ ⁡⁢⁢⁢Документация: https://ant.design/⁡
		}
		// 🔹 ⁡⁣⁢⁢Chakra UI⁡
		{
			// ✅ ⁡⁢⁢⁢Готовые пропсы для темного/светлого режима.⁡
			// ✅ ⁡⁢⁢⁢Акцент на accessibility (ARIA-атрибуты).⁡
			// ✅ ⁡⁢⁢⁢Документация: https://chakra-ui.com/⁡
		}
		// 🔹 ⁡⁣⁢⁢Bootstrap (React-версия)⁡
		{
			// ✅ ⁡⁢⁢⁢Сетка на flexbox.⁡
			// ✅ ⁡⁢⁢⁢Готовые классы для адаптива.⁡
			// ✅ ⁢⁡⁢⁢⁢Документация: https://react-bootstrap.github.io/⁡
		}
		// 🔹 ⁡⁣⁢⁢Tailwind CSS + Headless UI⁡
		{
			// ✅ ⁡⁢⁢⁢Полный контроль над стилями через классы Tailwind.⁡
			// ✅ ⁡⁢⁢⁢Гибкость для уникального дизайна.⁡
			// ✅ ⁡⁢⁢⁢Документация: https://tailwindcss.com/⁡
		}
	}
	// 🔳 ⁡⁢⁣⁡⁢⁣⁣УТИЛИТЫ (работа масивы, объекты, строки, даты)⁡
	{
		// ⭐ ⁡⁣⁣⁢Lodash - Работа с массивами, объектами, строками.⁡
		{
			// import _ from "lodash";
			const users = [{ id: 1 }, { id: 2 }];
			const ids = _.map(users, "id"); // [1, 2]
		}
		// ⭐ ⁡⁣⁣⁢date-fns - Манипуляции с датами.⁡
		{
			// import { format, addDays } from "date-fns";
			format(new Date(), "yyyy-MM-dd"); // "2023-09-20"
			addDays(new Date(), 5); // Дата +5 дней
		}
	}
	// 🔳 ⁡⁢⁣⁣АНИМАЦИЯ⁡
	{
		// ⭐ ⁡⁣⁣⁢Framer Motion - Продвинутая анимация элементов.⁡
		{
			// import { motion } from "framer-motion";
			<motion.div
				initial={{ opacity: 0 }}
				animate={{ opacity: 1 }}
				transition={{ duration: 1 }}
			>
				Анимированный элемент
			</motion.div>;
		}
	}
	// 🔳 ⁡⁢⁣⁡⁢⁣⁣ТЕСТИРОВАНИЕ (Jest + React Testing Library)⁡
	{
		// ⭐ ⁡⁣⁣⁢Jest + React Testing Library - Юнит-тесты и тесты компонентов.⁡
		{
			// import { render, screen } from "@testing-library/react";
			// import Button from "./Button";
			test("renders button with text", () => {
				render(<Button>Click me</Button>);
				expect(screen.getByText("Click me")).toBeInTheDocument();
			});
		}
		// ⭐
	}
	// 🔳 ⁡⁢⁣⁡⁢⁣⁣ВАЛИДАЦИИ (Joi, Yup, AJV, Zod, class-validator)⁡
	{
		// ⭐ ⁡⁣⁣⁢Joi (мощная библиотека для валидации схем) - для сложных схем (Node.js)⁡
		// ⭐ ⁡⁣⁣⁢Yup (легковесная альтернатива Joi) - для клиентской валидации⁡
		// ⭐ ⁡⁣⁣⁢AJV (валидация по JSON Schema) - для максимальной производительности⁡
		// ⭐ ⁡⁣⁣⁢Zod (TypeScript-ориентированная) - для TypeScript-проектов⁡
		// ⭐ ⁡⁣⁣⁢class-validator (с использованием декораторов) - для NestJS и TypeScript⁡
	}
	// 🔳 ⁡⁢⁣⁡⁢⁣⁡⁢⁣⁣КАК ВЫБИРАТЬ БИБЛИОТЕКИ? КАК НЕКОТОРЫЕ РАБОТАЮТ?⁡
	{
		// ⭐ ⁡⁣⁣⁡⁣⁣⁢Проектные требования: Для маленьких проектов подойдут Zustand или Context API, для крупных — Redux.⁡
		// ⭐ ⁡⁣⁣⁢Сообщество и поддержка: Проверяйте количество звезд на GitHub, частоту обновлений.⁡
		// ⭐ ⁡⁣⁣⁢Экосистема: Например, Next.js предоставляет SSR + маршрутизацию + сборку в одном решении⁡
		// ⭐ ⁡⁣⁣⁢Производительность: SWR и React Query оптимизируют работу с данными.⁡
		// ⭐ ⁡⁣⁣⁢Личные предпочтения: Некоторым нравится лаконичность React Hook Form, другим — подход Formik.⁡
		// ⁡⁢⁣⁣🔳 REDUX TOOLKIT(RTK) КАК РАБОТАЕТ В REACT ПРИЛОЖЕНИИ И С NEXTJS?⁡
		{
			// ⁡⁣⁢⁢1. Основные понятия Redux Toolkit (RTK):⁡
			{
				// ⁡⁣⁣⁢- Redux Toolkit (RTK): Официальный инструмент для работы с Redux, упрощающий настройку хранилища (store), создание редьюсеров (reducers) и действий (actions). Включает:⁡
				// ⁡⁢⁢⁢-- createSlice: Генерирует редьюсеры и действия автоматически.⁡
				// ⁡⁢⁢⁢-- configureStore: Настраивает хранилище с поддержкой middleware (например, Redux Thunk).⁡
				// ⁡⁢⁢⁢-- createAsyncThunk: Упрощает асинхронные операции.⁡
				// ⁡⁣⁣⁢- Next.js: Фреймворк для React с поддержкой SSR (Server-Side Rendering) и SSG (Static Site Generation). Позволяет рендерить страницы на сервере или во время сборки.⁡
			}
			// ⁡⁣⁢⁢2. Инициализация хранилища (Store)⁡
			{
				// ⁡⁣⁣⁢- В React (клиентский рендеринг):⁡
				// ⁡⁢⁢⁢-- Хранилище создается один раз при запуске приложения.⁡
				// ⁡⁣⁣⁢- В Next.js (SSR/SSG):⁡
				// ⁡⁢⁢⁢-- Хранилище должно создаваться заново для каждого запроса, чтобы избежать переиспользования состояния между разными пользователями⁡
				// ⁡⁢⁢⁢-- Используют библиотеку next-redux-wrapper:⁡
			}
			// ⁡⁣⁢⁢3. Серверный рендеринг (SSR) и гидратация (Hydration)⁡
			{
				// ⁡⁣⁣⁢- Гидратация — процесс «оживления» статичной HTML-страницы, полученной с сервера, на клиенте (подключение React и Redux).⁡
				// ⁡⁣⁣⁢- В Next.js:⁡
				// ⁡⁢⁢⁢-- Состояние, созданное на сервере, должно быть передано клиенту, чтобы избежать расхождений (hydration mismatch).⁡
				// ⁡⁢⁢⁢-- Пример передачи состояния:⁡
				// Страница (getServerSideProps)
				// Фуккцию нужно экспортировать
				const getServerSideProps = wrapper.getServerSideProps((store) => async () => {
					await store.dispatch(fetchData()); // Загрузка данных на сервере
					return { props: {} };
				});
				// ⁡⁢⁢⁢-- На клиенте next-redux-wrapper автоматически синхронизирует состояние.⁡
				// ⁡⁣⁣⁢- В React гидратация не требуется, так как нет SSR.⁡
			}
			// ⁡⁣⁢⁢4. Маршрутизация (Routing)⁡
			{
				// ⁡⁣⁣⁡⁣⁣⁢- В React используется клиентская маршрутизация (например, React Router). Состояние Redux сохраняется между переходами.⁡
				// ⁡⁣⁣⁢- В Next.js:⁡
				// ⁡⁢⁢⁢-- Маршрутизация основана на файловой системе (страницы в папке pages).⁡
				// ⁡⁢⁢⁢-- При переходе между страницами может происходить полная перезагрузка (если не используется next/link), что требует повторной инициализации состояния.⁡
				// ⁡⁢⁢⁢-- Для сохранения состояния между страницами используют next-redux-wrapper или контекст.⁡
			}
			// ⁡⁣⁢⁢5. Асинхронные операции (Data Fetching)⁡
			{
				// ⁡⁣⁣⁢- В React данные загружаются на клиенте (например, в useEffect):⁡
				useEffect(() => {
					dispatch(fetchData());
				}, []);
				// ⁡⁣⁣⁢- В Next.js данные можно загрузить на сервере:⁡
				// ⁡⁢⁢⁢-- В getServerSideProps (SSR) или getStaticProps (SSG).⁡
				// Функция экспортируется
				const getServerSideProps = wrapper.getServerSideProps((store) => async () => {
					await store.dispatch(fetchData()); // Данные загружаются на сервере
					return { props: { data: store.getState().data } };
				});
			}
			// ⁡⁣⁢⁢6. Инструменты разработчика и Middleware (DevTools and Middleware)⁡
			{
				// ⁡⁣⁣⁢- Redux DevTools работают одинаково в обоих случаях, если настроены корректно.⁡
				// ⁡⁣⁣⁢- Middleware (например, для логирования или асинхронных действий):⁡
				// ⁡⁢⁢⁢-- В React подключаются через configureStore.middleware.⁡
				// ⁡⁢⁢⁢-- В Next.js требуется дополнительная настройка из-за SSR:⁡
				const makeStore = () =>
					configureStore({
						reducer: rootReducer,
						middleware: (getDefaultMiddleware) => getDefaultMiddleware().concat(logger),
					});
			}
			// ⁡⁣⁢⁢Итоговые различия между React и Next.js с Redux Toolkit:⁡
			{
				// ⁡⁢⁣⁣АСПЕКТ						REACT							NEXT.JS⁡
				// ⁡⁣⁣⁢Инициализация store			Один раз на клиенте				Для каждого запроса (SSR)⁡
				// ⁡⁣⁣⁢Гидратация					Не требуется					Обязательна (синхронизация состояния)⁡
				// ⁡⁣⁣⁢Data Fetching				Клиентский (useEffect)			Серверный (getServerSideProps)⁡
				// ⁡⁣⁣⁢Маршрутизация				Клиентская (React Router)		Файловая система (pages/)⁡
				// ⁡⁣⁣⁢Библиотеки					react-redux						next-redux-wrapper⁡
			}
		}
	}
}
//! МЕТОДОЛОГИИ РАЗРАБОТКИ
{
	// 🔳 ⁡⁢⁣⁣МЕТОДОЛОГИИ РАЗРАБОТКИ В IT КОМПАНИЯХ⁡
	{
		// 🔹 ⁡⁣⁢⁢ГИБКИЕ МЕТОДОЛОГИИ (Agile) - Основаны на итеративном подходе, быстрой адаптации к изменениям и тесном взаимодействии с заказчиком.⁡
		{
			// ✅ ⁡⁢⁢⁢Scrum⁡
			{
				// ⚪ ⁡⁣⁣⁢Работа ведется короткими итерациями (спринтами, обычно 1–4 недели).⁡
				// ⚪ ⁡⁣⁣⁢Есть роли: Scrum-мастер (устраняет препятствия), Product Owner (определяет приоритеты), Development Team (разработчики).⁡
				// ⚪ ⁡⁣⁣⁢Основные артефакты: Product Backlog (список задач), Sprint Backlog (задачи на спринт), Increment (готовый функционал).⁡
				// ⚪ ⁡⁣⁣⁢Ритуалы: Daily Stand-up, Sprint Planning, Sprint Review, Retrospective.⁡
				{
					// ☑️ ⁡⁣⁢⁡⁣⁢⁢Daily Stand-up (стендап) — регулярная короткая встреча Agile-команды, которая призвана синхронизировать всех участников, обеспечивать прозрачность рабочего процесса.Традиционно стендап занимает 15 минут и проводится ежедневно в начале рабочего дня.⁡
					// ☑️ ⁡⁣⁢⁢Sprint Planning (планирование спринта) — это этап в методологии Scrum, на котором команда определяет, какие задачи будут выполнены в течение следующего спринта. Цель — определить объём работ, которые команда сможет выполнить за определённый период.⁡
					// ☑️ ⁡⁣⁢⁢Sprint Review (обзор спринта) — это мероприятие в методологии Scrum, которое проводится в конце каждого спринта. Во время встречи команда демонстрирует выполненную работу заинтересованным сторонам (стейкхолдерам) и собирает обратную связь.⁡
					// ☑️ ⁡⁣⁢⁢Ретроспектива спринта (Sprint Retrospective) — это итоговое собрание, проводимое в конце спринта, целью которого является анализ работы команды за прошедший период.⁡
				}
			}
			// ✅ ⁡⁢⁢⁢Kanban⁡
			{
				// ⚪ ⁡⁣⁣⁢Визуализация workflow с помощью Kanban-доски (столбцы: "To Do", "In Progress", "Done").⁡
				// ⚪ ⁡⁣⁣⁢Ограничение количества задач в работе (WIP Limit).⁡
				// ⚪ ⁡⁣⁣⁢Гибкое управление без жестких итераций.⁡
			}
			// ✅ ⁡⁢⁢⁢Extreme Programming (XP)⁡
			{
				// ⚪ ⁡⁣⁣⁢Акцент на качестве кода: парное программирование, TDD (Test-Driven Development), частые релизы.⁡
				// ⚪ ⁡⁣⁣⁢Гибкость в требованиях, короткие циклы обратной связи.⁡
			}
		}
		// 🔹 ⁡⁣⁢⁢КАСКАДНЫЕ (Waterfall) И ГИБРИДНЫЕ МОДЕЛИ⁡
		{
			// ✅ ⁡⁢⁢⁢Waterfall (Водопадная модель)⁡
			{
				// ⚪ ⁡⁣⁣⁢Жесткое разделение на этапы: анализ → проектирование → разработка → тестирование → внедрение.⁡
				// ⚪ ⁡⁣⁣⁢Подходит для проектов с четкими требованиями, но плохо адаптируется к изменениям.⁡
			}
			// ✅ ⁡⁢⁢⁢V-Model⁡
			{
				// ⚪ ⁡⁣⁣⁢Усовершенствованный Waterfall, где тестирование планируется параллельно с разработкой.⁡
			}
			// ✅ ⁡⁢⁢⁢Гибридные модели (Water-Scrum-Fall)⁡
			{
				// ⚪ ⁡⁣⁣⁢Сочетание Agile и Waterfall (например, планирование по Waterfall, а разработка по Scrum).⁡
			}
		}
		// 🔹 ⁡⁣⁢⁢DevOps и CI/CD - Не столько методология, сколько культура, объединяющая разработку и эксплуатацию:⁡
		{
			// ⚪ ⁡⁣⁣⁢Непрерывная интеграция (CI) – автоматическая сборка и тестирование кода.⁡
			// ⚪ ⁡⁣⁣⁢Непрерывная доставка (CD) – автоматическое развертывание в production.⁡
			// ⚪ ⁡⁣⁣⁢Используется в сочетании с Agile для ускорения выпуска релизов.⁡
		}
		// 🔹 ⁡⁣⁢⁢ДРУГИЕ ПОДХОДЫ⁡
		{
			// ⚪ ⁡⁣⁣⁢Lean (Бережливая разработка) – устранение потерь, оптимизация процессов.⁡
			// ⚪ ⁡⁣⁣⁢SAFe (Scaled Agile Framework) – Agile для больших команд и предприятий.⁡
			// ⚪ ⁡⁣⁣⁢RAD (Rapid Application Development) – быстрая разработка через прототипирование.⁡
		}
		// 🔹 ⁡⁣⁢⁢КАКУЮ МЕТОДОЛОГИЮ ВЫБРАТЬ?⁡
		{
			// ⚪ ⁡⁣⁣⁢Гибкие (Agile, Scrum, Kanban) – для динамичных проектов, где требования часто меняются.⁡
			// ⚪ ⁡⁣⁣⁢Waterfall – для проектов с фиксированными требованиями (например, госзаказы).⁡
			// ⚪ ⁡⁣⁣⁢DevOps – если важна автоматизация и частые релизы⁡
		}
	}
}