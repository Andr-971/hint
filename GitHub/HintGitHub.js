
// 🔳 ⁡⁢⁣⁡⁢⁣⁣ОСНОВНЫЕ КОМАНДЫ ДЛЯ НАЧАЛА РАБОТЫ С GIT⁡◘
{
	// 🔹 ⁡⁣⁢⁢Когда начинаете новый проект и хотите использовать Git для контроля версий.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Инициализирует новый локальный репозиторий Git в текущей папке.⁡
		// git init
		// git init [--bare] [<directory>]
		// ⁡⁢⁢⁢directory⁡ ⁡⁢⁣⁣указывает директорию, в которой создается локальный репозиторий. Если параметр не указан, репозиторий создается в текущей директории.⁡
		// ⁡⁢⁢⁢Основные флаги⁡:
		// --template позволяет указать кастомный шаблон инициализации
		// --separate-git-dir позволяет разместить репозиторий в другой директории
		// --bare определяет репозиторий как голый, без рабочей директории. Это используется для создания удаленного репозитория
	}
	// 🔹 ⁡⁣⁢⁡⁣⁢⁢Создать .gitignore(настроить)⁡
	{
		// touch .gitignore
		// git status --ignored // Проверить игнорируемые файлы
		// git rm -rf --cached <folder>
		{
			// rm — удалите файлы из системы контроля версий
			// -r -- делайте это рекурсивно
			// -f — принудительное удаление непустого каталога
			// --cached — на самом деле не удаляйте файлы, просто исключите их из системы контроля версий
		}
		// 🔹 ⁡⁣⁢⁢Основные правила синтаксиса⁡
		{
			// # - Комментарий (строка игнорируется)
			// / * - Заменяет любое количество символов (кроме /). # *.tmp Игнорировать ВСЕ файлы с расширением .tmp
			// / ? - Заменяет один символ. # ??test.txt # Игнорирует aatest.txt, но не test.txt
			// / в начале пути - Указывает на корень репозитория.# /build/  # Игнорирует ТОЛЬКО папку build в корне
			// / в конце - Игнорировать только папку (не файл). # cache/ # Игнорировать папку cache рекурсивно
			// /** - Рекурсивные подпапки. # **/temp    # Игнорирует папку temp в любой директории
			// / ! - Исключение из игнорирования. # !main.txt   # Но отслеживать main.txt
			// dist/ — игнорирует папку dist в любой директории проекта.
			// /dist/ — игнорирует только корневую папку dist.
			// / **/dist/ — игнорирует все папки dist в проекте (аналогично dist/).
		}
		// 🔹 ⁡⁣⁢⁢Очистить кеш, чтобы файлы не добавлялись в отслеживание⁡
		{
			// ✅ ⁡⁢⁢⁢Если файлы уже добавлены в Git, но их нужно игнорировать⁡
			{
				// ⚡ ⁡⁢⁣⁣Добавьте правила в .gitignore (если их нет).⁡
				// ⚡ ⁡⁢⁣⁣Удалите файлы из индекса Git, сохранив их на диске⁡
				{
					// # Для одного файла
					// git rm --cached имя_файла
					// # Для всей папки
					// git rm -r --cached имя_папки
					// # Для всех файлов, соответствующих паттерну (например, .tmp)
					// git rm --cached *.tmp
				}
				// ⚡ ⁡⁢⁣⁣Закоммитьте изменения⁡
				{
					// git add .gitignore
					// git commit -m "Игнорировать файлы"
				}
			}
		}
	}
    // 🔹 ⁡⁣⁢⁢Когда нужно скопировать проект с GitHub для работы.⁡
    {
		// ✅ ⁡⁢⁢⁢Что делает: Клонирует удалённый репозиторий (например, с GitHub) на ваш компьютер.⁡
		// git clone https://github.com/username/repo.git
		// git clone <URL репозитория> [<название локальной папки>]
	}
}
// 🔳 ⁡⁢⁣⁣ОСНОВНЫЕ КОМАНДЫ ДЛЯ РАБОТЫ С ИЗМЕНЕНИЯМИ⁡
{
	// 🔹 ⁡⁣⁢⁢Перед коммитом, чтобы проверить, какие файлы изменены.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Показывает текущее состояние репозитория (изменённые, добавленные, удалённые)⁡
		// git status
		// git status [-s] [--long] [--branch] [--porcelain] [--ignore-submodules[=<when>]]
		// -s или --short показывает краткую информацию о состоянии файлов в формате git diff --shortstat
		// --long показывает длинный формат состояния файлов, включая информацию о последнем коммите для каждого файла
		// --branch показывает текущую ветку и ее состояние
		// --porcelain показывает состояние файлов в машинно-читаемом формате, что полезно для автоматизации
		// --ignore-submodules позволяет игнорировать изменения в подмодулях
	}
	// 🔹 ⁡⁣⁢⁢Удалить все не закоммиченные данные⁡
	{
		// ✅ ⁡⁢⁢⁡⁢⁢⁢⁡⁢⁢⁢Что делает: удаляет все не закоммиченные данные из всех папок и файлов⁡
		// git reset --hard 
	}
	// 🔹 ⁡⁣⁢⁢После внесения изменений, перед коммитом.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Добавляет изменения в индекс (staging area) для последующего коммита.⁡
		// git add file.txt ⁡⁢⁣⁣– добавляет конкретный файл.⁡
		// git add . ⁡⁢⁣⁣– добавляет все изменения в текущей папке.⁡
	}
	// 🔹 ⁡⁣⁢⁢Когда изменения готовы к сохранению в истории.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Фиксирует изменения в истории Git с указанным сообщением.⁡
		// git commit -m "Исправил баг в авторизации"
		// git commit -amend "Исправил баг в авторизации" # Оставит прошлое сообщение
	}
	// 🔹 ⁡⁣⁢⁢Когда нужно посмотреть все комментарии⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: выводить все комментарии⁡
		// git log
		// git log --all --graph # ввиде веток
		// git log --all --graph --oneline # ввиде строки
		// q,Q, :q, :Q и ZZ # Выйти, раскладка en
		// --oneline показывает каждый коммит в одной строке, содержащей только его SHA-1 хеш и комментарий
		// --graph показывает историю коммитов в виде графа, что позволяет легко визуализировать разветвленную историю ветвлений и слияний
		// --author=<имя> показывает только коммиты, сделанные указанным автором
		// --since=<дата> показывает только коммиты, сделанные после указанной даты
		// --until=<дата> показывает только коммиты, сделанные до указанной даты
		// -n <количество> показывает только указанное количество последних коммитов
		// --grep=<строка> показывает только коммиты, содержащие указанную строку в своих комментариях
	}
    // 🔹 ⁡⁣⁢⁢Если забыли что-то добавить в последний коммит.⁡
    {
		// ✅ ⁡⁢⁢⁢Что делает: Позволяет изменить последний коммит (например, добавить забытый файл или исправить сообщение).⁡
		// git commit --amend -m "Новое сообщение коммита"
	}
	// 🔹 ⁡⁣⁢⁢Работа с .gitignore⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: удаляет из индекса git и добавляет обратно⁡
		// git rm -r --cached .
		// git add .
		// git commit -m "исправление .gitignore"
	}
}
// 🔳 ⁡⁢⁣⁡⁢⁣⁣РАБОТА С ВЕТКАМИ (branches)⁡
{
	// 🔹 ⁡⁣⁢⁢Чтобы проверить, в какой ветке вы находитесь.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Показывает список всех локальных веток.⁡
		// git branch // git branch -a // все ветки
		// -a показывает все ветки включая удаленные
		// -d удаляет ветку, данная команда удаляет только те ветки, которые были слиты в текущую ветку
		// -D удаляет ветку без проверки, были ли все её изменения слиты с текущей веткой
		// -m переименовывает текущую ветку
		// -r показывает все удаленные ветки
	}
	// 🔹 ⁡⁣⁢⁢Когда нужно создать новую ветку для фичи или багфикса.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Создаёт новую ветку, но не переключается на неё.⁡
		// git branch feature/login ⁡⁢⁣⁣- feature/login название ветки⁡
	}
	// 🔹 ⁡⁣⁢⁢Для перехода между ветками.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Переключается на указанную ветку.⁡
		// git checkout main
		// Основные флаги:
		// -b создать новую ветку и переключиться на нее. Например, git checkout -b new-branch создаст новую ветку с названием new-branch и переключится на нее
		// -f принудительно перезаписывает локальные изменения, которые не были зафиксированы. Этот флаг используется только в крайних случаях
		// -p: позволяет просмотреть и выбрать конкретные изменения для восстановления
		// <commit/branch>: позволяет переключиться на определенный коммит или ветку
	}
	// 🔹 ⁡⁣⁢⁢Удобный способ создать и переключиться на новую ветку.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Создаёт новую ветку и сразу переключается на неё.⁡
		// git checkout -b feature/dashboard ⁡⁢⁣⁣- feature/dashboard название созданной новой ветки⁡
	}
	// 🔹 ⁡⁣⁢⁢Удалить ветку в локальном git⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: удаляет ветку на компютере в проекте⁡
		// git branch -D Название ветки
	}
	// 🔹 ⁡⁣⁢⁢Удалить ветку в репозитории⁡
	{
		// ✅ ⁡⁢⁢⁡⁢⁢⁢Что делает: удаляет ветку в репозитории⁡
		// git push --delete origin название ветки
	}
	// 🔹 ⁡⁣⁢⁢Создать ветку на удалённом репозитории⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Создаёт ветку на удалённом репозитории⁡
		// git push --setupstream origin название ветки
	}
	// 🔹 ⁡⁣⁢⁢Когда нужно объединить изменения (например, после завершения работы над фичей).⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: преходит в main/master какая главная или по дефолту⁡
		// git checkout main/master
		// ✅ ⁡⁢⁢⁢Что делает: Вливает изменения из указанной ветки в текущую.⁡
		// git merge feature/login # Какую ветку нужно влить
		// git merge --continue # Продолжить если слияние было прервано
		// --no-ff отключает быстрое слияние, что создает коммит с объединением, даже если изменения можно было бы просто быстро применить
		// --abort отменяет текущее слияние и возвращает репозиторий в предыдущее состояние
	}
	// 🔹 ⁡⁣⁢⁢Перенести изменения только определённого коммита⁡
	{
		// ✅ ⁡⁢⁢⁢Находит коммит по указанному хешу, повторяет его изменения в текущей ветке, создаёт новый коммит с теми же изменениями⁡(перенести можно несколько комменов написав несколько хешей)
		// git cherry-pick b6e7ce82c8928792cd40e1b98b96595499c4fe22
	}
    // 🔹 ⁡⁣⁢⁢Для поддержания линейной истории (часто в Open Source).⁡
    {
		// ✅ ⁡⁢⁢⁢Что делает: Перемещает коммиты текущей ветки на вершину указанной ветки (альтернатива merge)
		// git rebase main
		// git rebase -i main
		// ⁡⁢⁢⁢Основные флаги
		// -i (или --interactive) запускает интерактивный режим, который позволяет изменять порядок, объединять или отбрасывать коммиты
		// -m (или --merge) используется, когда нужно перебазировать ветку слияния
		// --onto <branch>: перебазирует текущую ветку на указанную ветку
	}
}
// 🔳 ⁡⁢⁣⁣РАБОТА С УДАЛЁННЫМ РЕПОЗИТОРИЕМ (GitHub)⁡
{
	// 🔹 ⁡⁣⁢⁢Создать репозиторий из bash⁡
	{
		// ✅ ⁡⁢⁢⁢Проверить, под какой учётной записью вы авторизованы⁡
		// gh auth status
		// ✅ ⁡⁢⁢⁢Если вы не авторизованы или авторизованы не под той учётной записью, выполните⁡:
		// gh auth logout        # (опционально) выйти из текущей сессии
		// gh auth login

		// # ⁡⁢⁢⁢Создаём проект (например, Vite)⁡
		// npm create vite@latest my-react-app -- --template react-ts
		// cd my-react-app
		// # ⁡⁢⁢⁢Инициализируем Git⁡
		// git init
		// git add .
		// git commit -m "Initial commit"
		// # ⁡⁢⁢⁢Публикуем на GitHub⁡
		// gh repo create
		
		// gh repo create имя-репозитория --public   # или --private
	}
	// 🔹 ⁡⁣⁢⁢Чтобы проверить, куда отправляются (push) и откуда забираются (pull) изменения.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Показывает список подключённых удалённых репозиториев⁡ 
        // git remote -v
    }
    // 🔹 ⁡⁣⁢⁢Подключиться к определённому аккаунту⁡
    {
		// ✅ ⁡⁢⁢⁡⁢⁢⁢Что делает: Подключает к определённому аккаунту и репозиторию⁡
		// git remote set-url origin git@github.com-Andr-971:Andr-971/hint.git
		// git remote set-url origin git@github.com-Andr-137:Andr-137/hint.git
    }
    // 🔹 ⁡⁣⁢⁢Получить клон с определённого аккаунта и репозитория⁡
    {
		// ✅ ⁡⁢⁢⁢Что делает: Вытигивает репозиторий с определённого аакаунта⁡
		// ⁡⁢⁢⁢# Для первого аккаунта⁡
		// git clone git@github.com-Andr-971:Andr-971/repo.git
		// ⁡⁢⁢⁢# Для второго аккаунта⁡
		// git clone git@github.com-Andr-137:Andr-137/repo.git
	}
    // 🔹 ⁡⁣⁢⁢После коммитов, чтобы обновить удалённую ветку(отправить в репозиторий).⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Отправляет локальные изменения в удалённый репозиторий (GitHub)⁡
		// git push origin main(название ветки) / git push origin HEAD:master # ⁡⁢⁢⁢На дефолтную ветку master⁡
		// git push <remote> <branch>
		// ⁡⁢⁢⁢remote имя удаленного репозитория, куда вы хотите отправить изменения, branch  — это название ветки, которую вы хотите отправить.⁡
		// ⁡⁢⁢⁢Основные флаги⁡:
		// -u или --set-upstream устанавливает отслеживание для ветки, что позволяет вам использовать git push и git pull без указания имени удаленного репозитория и названия ветки
		// -f или --force заставляет Git принудительно заменить удаленную ветку измененной локальной веткой, даже если это приведет к потере данных
		// -n или --dry-run позволяет вам протестировать команду git push, не отправляя реальных изменений в удаленный репозиторий
		// -v или --verbose выводит дополнительную информацию о процессе отправки изменений
	}
	// 🔹 ⁡⁣⁢⁢Чтобы обновить локальную ветку перед работой.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Забирает изменения из удалённого репозитория и сливает их с локальной веткой⁡
		// git pull origin main
		// git pull [<options>] [<repository> [<refspec>]]
		// ⁡⁢⁢⁢Основные флаги⁡
		// --rebase выполняет перебазирование текущей ветки на самый новый коммит вместо создания слияния merge. Рекомендуется использовать этот флаг, если вы хотите, чтобы история коммитов оставалась простой и понятной
		// --no-rebase отменяет перебазирование и выполняет слияние вместо этого
		// --no-commit предотвращает создание автоматического коммита после выполнения слияния. Это дает возможность внести дополнительные изменения перед фиксацией изменений
		// --ff-only выполнить слияние только в том случае, если это можно сделать быстрым перемещением ветки вперед fast-forward
		// --no-ff выполнить слияние только как коммит слияния merge commit, даже если это можно сделать быстрым перемещением ветки вперед
	}
    // 🔹 ⁡⁣⁢⁢Чтобы проверить изменения перед merge или rebase удалённого репозитория.⁡
    {
		// ✅ ⁡⁢⁢⁢Что делает: Загружает изменения из удалённого репозитория, но не сливает их⁡
		// git fetch
		// git fetch [<remote>] [<refspec>]
		// remote имя удаленного репозитория Git (например, origin). Если не указан, Git использует имя origin по умолчанию
		// refspec имена веток и тегов, которые вы хотите получить из удаленного репозитория. Если не указан, Git получает все ветки и теги
	}
}
// 🔳 ⁡⁢⁣⁣ОТМЕНА ИЗМЕНЕНИЙ⁡
{
	// 🔹 ⁡⁣⁢⁢Если случайно испортили файл и хотите вернуть исходное состояние.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Отменяет изменения в файле (до состояния последнего коммита)⁡
		// git restore file.txt
		// --soft — откатывает историю, но оставляет изменения в рабочей области.
		// --mixed (значение по умолчанию) — откатывает историю и рабочую область, но оставляет файлы неизменёнными.
		// --hard — полностью откатывает историю, рабочую область и файлы.
	}
	// 🔹 ⁡⁣⁢⁢Если нужно полностью сбросить всё до последнего коммита.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Отменяет все изменения в рабочей директории⁡ ⁡⁢⁣⁢(опасно!)⁡.
		// git reset --hard HEAD
	}
    // 🔹 ⁡⁣⁢⁢Если нужно отменить уже запушенный коммит.⁡
    {
		// ✅ ⁡⁢⁢⁢Что делает: Создаёт новый коммит, отменяющий изменения указанного коммита.⁡
		// git revert abc1234
		// git revert --no-commit d3b3d43
		// ⁡⁢⁢⁢Основные флаги⁡:
		// --no-commit: применить изменения, но не создавать новый коммит. Это позволяет вам проверить изменения перед тем, как закоммитить их
		// -m parent-number: используется, если коммит имеет несколько родительских коммитов (как в случае с объединением веток). Он указывает, какой из родительских коммитов следует использовать при выполнении операции отмены
	}
}
// 🔳 ⁡⁢⁣⁣ПРОСМОТР ИСТОРИИ И ИЗМЕНЕНИЙ⁡
{
	// 🔹 ⁡⁣⁢⁢Чтобы посмотреть, кто и какие изменения вносил.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Показывает историю коммитов.⁡
		// git log --oneline --graph  ⁡⁢⁣⁣# компактный вывод с графом веток⁡
	}
    // 🔹 ⁡⁣⁢⁢Чтобы увидеть, что именно было изменено.⁡
    {
		// ✅ ⁡⁢⁢⁢Что делает: Показывает разницу между текущими изменениями и последним коммитом.⁡
		// git diff имя_файла / хеш_коммита
		// git diff - простой запуск команды покажет различия между вашей рабочей копией и последним коммитом
		// git diff --cached - покажет различия между вашим индексом и последним коммитом
		// git diff HEAD - покажет различия между вашей рабочей копией и последним коммитом
		// git diff HEAD~2 HEAD - покажет различия между двумя последними коммитами
		// git diff --stat - покажет статистику изменений для каждого файла
		// git diff --color - покажет различия с использованием цветов
	}
}
// 🔳 ⁡⁢⁣⁣ВРЕМЕННОЕ СОХРАНЕНИЕ ИЗМЕНЕНИЙ (STASH)⁡
{
	// 🔹 ⁡⁣⁢⁢Если нужно срочно переключиться, но коммитить ещё рано.⁡
	{
		// ✅ ⁡⁢⁢⁢Что делает: Временно сохраняет изменения, чтобы переключиться на другую ветку.⁡
		// git stash
	}
    // 🔹 ⁡⁣⁢⁢Когда можно продолжить работу.⁡
    {
		// ✅ ⁡⁢⁢⁢Что делает: Возвращает сохранённые изменения из stash.⁡
		// git stash pop
	}
}
// 🔳 ⁡⁢⁣⁣ПОМОЩЬ⁡
{
	// git < команда > --help;
	//  git difftool просто запускает внешнюю утилиту сравнения для показа различий в двух деревьях, на случай если вы хотите использовать что-либо отличное от встроенного просмотрщика git diff.
	//  git rm используется в Git для удаления файлов из индекса и рабочей директории. Она похожа на git add с тем лишь исключением, что она удаляет, а не добавляет файлы для следующего коммита.
	//  git mv — это всего лишь удобный способ переместить файл, а затем выполнить git add . для нового файла и git rm для старого.
	//  git clean используется для удаления мусора из рабочей директории. Это могут быть результаты сборки проекта или файлы конфликтов слияний.
	//  git mergetool просто вызывает внешнюю программу слияний, в случае если у вас возникли проблемы слияния.
	//  git tag используется для задания постоянной метки на какой-либо момент в истории проекта. Обычно она используется для релизов.
	//  git archive используется для упаковки в архив указанных коммитов или всего репозитория.
	//  git submodule используется для управления вложенными репозиториями. Например, это могут быть библиотеки или другие, используемые не только в этом проекте ресурсы. У команды submodule есть несколько под-команд — add, update, sync и др. — для управления такими репозиториями.
	//  git show отображает объект в простом и человекопонятном виде. Обычно она используется для просмотра информации о метке или коммите.
	//  git shortlog служит для подведения итогов команды git log. Она принимает практически те же параметры, что и git log, но вместо простого листинга всех коммитов, они будут сгруппированы по автору.
	//  git bisect — это чрезвычайно полезная утилита для поиска коммита в котором впервые проявился баг или проблема с помощью автоматического бинарного поиска.
	//  git blame выводит перед каждой строкой файла SHA-1 коммита, последний раз менявшего эту строку и автора этого коммита. Это помогает в поисках человека, которому нужно задавать вопросы о проблемном куске кода.
	//  git grep используется для поиска любой строки или регулярного выражения в любом из файлов вашего проекта, даже в более ранних его версиях.
}